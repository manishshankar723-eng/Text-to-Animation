# RULEBOOK.md — every rule this app learned from a live test

> **Read the section for the area you are about to change, BEFORE you change it.**
> Then add or update a row when your fix lands. This file is how a fix made in
> August survives an edit made in October.

`AGENTS.md` is still the source of truth for *what the project is* and *what
happened when*. But its Work Log is 22,000 lines of chronology, ordered by date,
and **nobody reads 22,000 lines before touching one prompt string**. So the same
box got un-grown four times and the same end card was fixed twice.

This file is the other index: **ordered by SCENARIO, not by date.** Small enough
to read in full. If you are about to edit the concept prompt, section A tells you
in thirty seconds what already lives there and must not be lost.

---

## Ise kaise padhein (plain Hinglish)

Ye file ek **niyam ki kitaab** hai. Har baar jab testing mein koi problem milti
hai aur hum use theek karte hain, ek nayi line yahan add hoti hai.

Agla agent (Claude / Codex / Gemini) kaam shuru karne se pehle apne area ki list
padhta hai — isliye purana fix dobara nahi tootta.

Har niyam par ek **status** likha hai:

| Status | Matlab |
|---|---|
| **PAKKA** | Code mein enforce hai (Python/JS). Silently tut nahi sakta — test fail ho jayega. |
| **GUZARISH** | Sirf AI ko di gayi request (prompt rule). AI maan bhi sakta hai, nahi bhi. **Live run se hi pata chalega.** |
| **OPEN** | Problem pata hai, abhi theek nahi hui. |

⚠ **PAKKA aur GUZARISH ka farak sabse zaroori hai.** Agar kisi cheez ka status
GUZARISH hai, to "ho gaya" mat samajhiye jab tak ek asli board ban kar na dekh lein.

---

## Protocol for agents (required)

1. **Before you edit** — read the section covering the file you are opening.
   A rule here is a decision that was already paid for once, in a real test.
2. **While you edit** — do not delete a rule to make room for yours. If two
   rules genuinely conflict, say so in your reply and let the user choose.
3. **When your fix lands** — add a row. New rules get the next free ID in that
   section. Write what the user actually saw, not what the code does.
4. **When a rule is superseded** — strike it (`~~R~~`) and add the replacement
   with a note saying which one it replaces. **Never delete history**; a rule
   that came back is the most useful thing in this file.
5. **Be honest about status.** A prompt change is GUZARISH until a live run
   says otherwise. Marking it PAKKA is how a fix gets forgotten twice.

---

## A · Concept — a brief or an idea becomes the card the user approves

`script_concept.py` · pinned by `tests/script_concept_check.py`

> **Live-run log for this section.** A GUZARISH rule earns confidence one real board at a time, and one pass is evidence, not proof — record each run here rather than promoting the rule.
>
> - **2026-08-28, Hinglish Ganesh Chaturthi brief, 40s, re-run from a file.** A1 ✅ (arc ended on *Bhagwan ka aashirwad* and so did scene 6) · A2 ✅ (all five beats had a scene) · A3 ✅ (scene 6 was Ganesh ji alone, filling the frame — the shot that was missing entirely the run before) · A4 ✅ (opened on a child's eyes lighting up, not on laying out marigolds). ⚠ **And the same run is where A10/A11 came from:** the visarjan the brief asked for had gone. Close-up-heavy framing (4 of 6 scenes) noted and deferred to the review step by the user.
> - **2026-08-28, run 3, the same brief again after A10/A11 were added.** A1 ✅ · A2 ✅ (six beats, six scenes, one to one) · **A10 ✅ and A11 ✅ — the visarjan is back, it is scene 5 of 6, and the resolution lands after it, with *"muskaan aur thodi udaasi ke saath"* carrying the feeling.** ⚠ **But A3 ❌ and A4 ❌ REGRESSED, and both had passed in run 2.** Ganesh ji is carried, prayed to, sung around and immersed — and never once framed alone; the hero shot run 2 ended on is simply gone. And scene 1 is *"ek bacche ke haath rangoli bana rahe hain … phool aur diye sajaye ja rahe hain"* — the decorating shot from run 1, back verbatim, under a first beat that is literally *Ghar ki taiyari*.
> - ⚠ **NO SINGLE RUN HAS YET HAD ALL SIX RIGHT.** Run 2 had the hero shot and the opening and lost the visarjan; run 3 has the visarjan and lost the hero shot and the opening. Six rules are now competing for the same attention in one instruction, and **adding a seventh is not obviously the fix** — the next move is more likely to be making A3 and A4 harder to skip (a named slot in the scene list, or a deterministic check) than another paragraph of prose.

| # | Scenario | The rule | Status |
|---|---|---|---|
| **A1** | Writing `key_scenes` | **The scene list has to END on the last beat of `story_direction`.** *Found: a Ganesh Chaturthi arc ending "-> Aashirwad bana rehta hai" whose six scenes stopped at the visarjan — the film would have ended on the loss.* Six beats + six scenes looks like a match, which is exactly what hid it. `concept_to_brief()` also states the closing beat outright when the scenes drop it. | **PAKKA** (handoff) + **GUZARISH** (card) |
| **A2** | Writing `key_scenes` | **Every step of the arrow chain gets at least one scene.** A direction and a scene list that disagree describe two different films. | GUZARISH |
| **A3** | Writing `key_scenes` | **Whatever the film is ABOUT is seen alone, at least once** — not held, not touched, not glimpsed past a shoulder. *Found: a film entirely about an idol that never once framed it.* | GUZARISH |
| **A4** | Runtime ≤ 90s, no feed words | **The opening moves, and nothing else does.** A 40-second film has no room for a run-up, so scene 1 is already the film. ⚠ **This is NOT the hook rule** — the rest of the arc keeps its order and still lands its ending. | GUZARISH (trigger is PAKKA) |
| **A5** | Brief says reel / shorts / viral / tiktok / instagram | **Hook first — strongest image in scene 1, reorder the film if you must.** ⚠ The trigger is what the user **typed**, never the 9:16 they clicked, and "short film" is deliberately excluded. Overrules A4. | GUZARISH (trigger is PAKKA) |
| **A6** | The concept step fails | **It must BLOCK, and must not fall through to breaking the raw brief down as a script.** ⚠ Opposite of `/script-intake`, which fails OPEN on purpose — a dead classifier must not stop a board, a dead concept step must. | PAKKA |
| **A7** | An approved concept | **Goes through `plan_agent.write_script()`, never straight to shots.** The review step and every "FROM YOUR SCRIPT · LINE 12" need a real script to point at. | PAKKA |
| **A8** | Any generated field | **Same language and same script as the user wrote in.** Hinglish in Latin letters stays Hinglish in Latin letters — no switch to Devanagari, no switch to English. | GUZARISH |
| **A9** | Approving | **The length on the card travels all the way.** `concept_seconds()` → the writer → `break_down_script()`. *Found: a 30s concept boarded as 29 shots / 1m 04s, every extra panel paid for.* | PAKKA |
| **A10** | Any beat the user's own material named | **An event they named is as fixed as a character they named.** A farewell, a first day, a visarjan, an unboxing — it gets a beat in the direction AND a scene of its own. Shorten the film by tightening scenes, **never by deleting one of theirs.** ⚠ Dropping a beat does not feel like contradicting the user, it feels like *tightening* — which is exactly what makes it the easy mistake. | GUZARISH |
| **A11** | A story with a difficult moment in it | **Do not smooth the hard beat away.** The goodbye, the letting go, the thing that costs something — that is where the film's feeling comes from, and a scene list of only pleasant moments has nothing in it to feel. Keep it, put it late, and let the resolution land **after** it rather than instead of it. *Found: a re-run that kept anticipation, arrival, devotion, joy and blessing — and quietly dropped the visarjan.* | GUZARISH |

---

## B · Breakdown — a script becomes shots

`script_breakdown.py` · pinned by `tests/shot_density_check.py`

| # | Scenario | The rule | Status |
|---|---|---|---|
| **B1** | Reading the script | **Know what you are reading.** `_is_beat_script()` routes it: a script from `write_script()` is *one line = one shot, merge repeats*; pasted prose keeps *split into beats*. | PAKKA (routing) |
| **B2** | Over the runtime budget | **Argue, never clamp.** The model is told to MERGE; truncating to a budget deletes the END of the story. The review chip goes amber when it did not. | PAKKA (chip) + GUZARISH (merge) |
| **B3** | A `(V.O.)` or speech-only line | **Never gets a panel of its own.** It attaches to a shot that already exists. `_SPEECH_RULE`. | GUZARISH |
| **B4** | Three shots framed the same way | **Ask for a different framing — do NOT merge them.** ⚠ They are different beats and merging deletes story. `_VARIETY_RULE` says this outright. | GUZARISH |

---

## C · Image prompts — what the model is allowed to draw

`gemini_client.py`

| # | Scenario | The rule | Status |
|---|---|---|---|
| **C1** | Any panel | **No lettering, ever.** `_SINGLE_FRAME_RULE` forbids the image model all writing — asking for it returns gibberish, and it is paid for. | GUZARISH |
| **C2** | An end card / title card | **Its WORDS go to `dialogue` under `ON SCREEN`, never into `description`** — and the description may not name the subject either (no *"graphic card"*, no *"title card"*, and the word *"text"* may not appear at all). ⚠ **Enforced in Python**: `strip_lettering()` runs where the breakdown writes the sentence AND on the one line every panel passes through, so an older board cannot slip one past. *Fixed twice — the first rule banned the phrasings and never touched the subject.* | **PAKKA** |
| **C3** | `movement` / `duration_seconds` | **Never reach an image prompt.** A still frame cannot show a move or a length; asking gets motion blur and arrows drawn into the panel. They travel like `dialogue` does. | PAKKA |

---

## D · Money, locale and brand

`market.py`, `brand.py`, `server/branding.py`

| # | Scenario | The rule | Status |
|---|---|---|---|
| **D1** | The board form | **Ask the LANGUAGE only.** Country, currency and units are derived from it, from the account, or from the script. ⚠ **English is deliberately NOT in the language→country table** — mapping it to the US is how an Indian creator's promo got priced in dollars. Spanish and Arabic are absent for the same reason. | PAKKA |
| **D2** | Market unknown | **No legible price and no currency symbol anywhere.** A wrong `$` is worse than none. `_LANGUAGE_ONLY_RULE`. | GUZARISH |
| **D3** | A brand logo | **Composited, never generated.** The model draws a flat magenta placeholder and `brand.stamp()` pastes the uploaded PNG in, so the mark is bit-identical in every panel because it IS the same file. ⚠ **No upload means NO logo** — one 28-panel promo carried four different generated logos. | PAKKA |
| **D4** | The APP'S OWN name and mark (not a customer's) | ⚠ **It is an admin-panel field, and NO SCREEN MAY KEEP A COPY OF IT.** *Asked for directly: "mai chahta hun Aniwala icon and Aniwala Ai Studio text ko mai admin panel se change kar sakun … jaha jaha ye icon dikhe waha update ho jaye … ek baar mai update ho jaye sab jagah."* The name and an uploaded logo live in `server/branding.py` (ONE row, read by the public `GET /public/branding`, written only in **Admin → Brand**); the browser holds them in the module store `client/src/branding.js` and every screen reads it through `useBranding()` / `<Logo/>`. ⚠ **Do not confuse it with D3** — that is a CUSTOMER's logo composited into a board, per account, behind a token. This one is the product's own, public, and there is exactly one of it. ⚠ The tab title and the favicon count as "everywhere", and so does the AI assistant's own brief (`_system_instruction()` in the four prompt modules) — an assistant that names the old product inside the renamed product is the same bug wearing a hat. ⚠ **AND THE LOGO IS TWO FILES, ONE PER THEME** — see **E20**. Pinned by `tests/branding_check.py`, whose LAST section greps `client/src` and fails if the string is hard-coded anywhere a customer can see it. | PAKKA |

---

## E · Screens — the form, the board, cast and props

`client/src/components/`

| # | Scenario | The rule | Status |
|---|---|---|---|
| **E1** | Any box the user must EDIT | **It grows to its text — use `GrowTextarea`.** ⚠ **Fixed four separate times** on four screens (shot description, image prompt, cast description, board tile). If you add a `<textarea rows={n}>` on a field that holds model output, you are about to make it five. ⚠ **AND THE HEIGHT MATHS IS `scrollHeight + border + 1`, ALL THREE TERMS.** The border because `box-sizing: border-box` means a height of exactly `scrollHeight` gives the text a content box smaller than the text. **And the `+ 1` because `scrollHeight` is an INTEGER and a line box usually is not** — 13px at `line-height: 1.33` is 17.3px a line, so two lines are 34.6 and the browser answers 34, and `overflow: hidden` eats the 0.6px the last line's descenders sit in. *Found by `admin_fields_check.py` on the offer form's bullet box: "content box 34.0px, needs 34.6px".* ⚠ The banner box beside it passed only because a `min-height` happened to be taller than the fault — **a `min-height` is not a fix**, it is a place the fault hides. `GrowTextarea.jsx` and `GrowText` in `admin/fields.jsx` carry the identical maths on purpose; change one and you change both. | PAKKA |
| **E2** | Any control that spends money | **It must state what it will do before it is pressed**, and be disabled when it would do nothing. *Found: "Restyle all" opened on a hard-coded "Comic" and would have redrawn twelve panels in a style nobody chose, and billed for every one.* | PAKKA |
| **E3** | Building a new button / modal / panel | **Read how the existing workflows build one and copy it exactly.** This repo has exactly two runtime dependencies (`react`, `react-dom`) — there is no component library to fall back on, and small mismatches are a repeated complaint. | PAKKA (convention) |
| **E4** | Explaining a control | **Helper text goes in the element's own `title` (hover).** Visible label stays 3–5 words. Native `title`, not a new tooltip component. | PAKKA (convention) |
| **E5** | Offering a paid reference | **Say how many shots it is worth.** A cast card carries `1 shot` (warn colour) or `7 shots` so skipping is an informed choice, not a guess. ⚠ Guessing "hands only" from the wording would be a guess; the shot count is a fact. | PAKKA |
| **E9** | Re-opening a saved artefact from a library | ⚠ **Load the WORK behind it, not just the artefact.** *Found: opening a finished board restored the display settings and the job id and nothing else, so review / cast / props had no content and ← was deliberately wired to the library because there was nowhere else with any — "beech ka page nahi aa raha hai".* And **stamp the up-to-date signature** while you are at it, or the free route back to panels that already exist disappears and the only way back is paying to draw them again. Fail soft: a dead lookup keeps the old behaviour, never removes the pictures. | PAKKA |
| **E7** | A step that can be reached going FORWARD | ⚠ **It must stay reachable going back.** *Found: once a board existed, the review step collapsed to Regenerate + Back to your storyboard and the cast/props screens became unreachable — the exact screens where a wrong character or a drifting prop is fixed.* A finished artefact is when those screens matter MOST, not least. Offer the forward path always; hide it only when the destination would be empty. | PAKKA |
| **E8** | A comma-separated text field bound to an array | ⚠ **Split on the separator and NOTHING else, and join with the bare separator.** Filtering the empty piece eats the separator as it is typed; trimming each piece eats the SPACE inside a value — *"Ganesh idol" could only be typed as "Ganeshidol"*. Both were shipped, minutes apart, and both were caught only in a browser. Clean at the consumers, never on keystroke. | PAKKA |
| **E6** | A list where ORDER is the meaning | **Add and delete are not enough — it needs ↑ / ↓ too.** *Found: a scene added to the concept card to fill a real gap landed at position 7, belonged at position 3, and could not be moved.* ⚠ Copy the shot cards' controls exactly — `title="Move up"` / `"Move down"`, disabled at either end, **and the handler no-ops at the ends as well** so a keypress that beats the re-render cannot wrap scene 1 to the bottom. `moveKeyScene()` mirrors `moveShot()`. | PAKKA |
| **E10** | A SECOND screen that lists the account's work | ⚠ **Read the groups out of `client/src/dashboard_feed.js`; do not map the six workflows again.** `Home.jsx` carried that array with its own warning on it — *"ORDER MATCHES THE SIDEBAR … or Recent work quietly stops showing it (which is exactly how Image to Video went missing)"* — and that warning was written about ONE list. Explore made it two, so the array, `useCovers`, `useDashboard`, `statusClass` and `formatDate` moved into the shared module and Home now imports them back. A workflow added, renamed or hidden is a change in `Sidebar.jsx` + `features.py` + that one file, never in a screen. | PAKKA |
| **E11** | Any new screen that ADVERTISES a workflow (banner, tile, chip) | ⚠ **Ask the question that suits the SIDE OF THE SIGN-IN it is on.** Behind the sign-in: it waits for `/auth/me/entitlements`, exactly like the rail does — the account's own WORK may be drawn while that answer is in flight (failing open is right: "not answered yet" must never read as "you have nothing"), but a banner or tile for a workflow an administrator has HIDDEN is the *"hidden feature that reappears on every reload"* fault the sidebar already paid for. ⚠ **In front of it, entitlements are the WRONG question and there is no token to ask it with** — a public page must read `GET /public/workflows`, which answers what the product SELLS rather than what one customer has BOUGHT. Explore was moved from the first to the second when it became the marketing page; `Landing.jsx` owns `useLiveWorkflows()` and both public screens share it, so they can never advertise different sets. | PAKKA |
| **E12** | Moving WHERE THE APP OPENS | ⚠ **Change `LANDING_NAV` in `App.jsx` and nothing else — it is the one place.** FIVE paths land somebody in the app (a returning session, a fresh sign-in, a sign-out, an account switch, and leaving the admin panel) and every one of them used to name the destination itself. The bug that shape produces is *"it opens on the right page UNLESS you switched account"*, which nobody reports as one bug. `?admin` still wins over it: a link has to land where it points. ⚠ **Today it is `"home"`, and it used to be `"explore"`** — *"any logged in user must not see explore ... after login we know how our page which is home must look"*. Explore changed sides entirely (see **E24**), so the front door moved with it. | PAKKA |
| **E13** | Anything that SWAPS CONTENT IN PLACE (a carousel slide, a tab body, a rotating banner) | ⚠ **Every state must leave the box the SAME HEIGHT — measure it, do not eye it.** *Found: the Explore billboard took each workflow's first sentence, and those run 40 to 176 characters — so reaching Image to Animatic Image grew the banner by two lines, and because the two billboards share a grid row it took the NEIGHBOUR down with it and then snapped back. "image to animatics image ka panel bara ho jata hai kyun ismai text jayada hai."* Two halves: feed it copy written short (`stage.body` in `Landing.jsx`, 67-115 chars across all six) **and** clamp the lines in CSS, so a rename cannot bring the flinch back. ⚠ And trim a trailing full stop rather than appending one — gluing "." onto a sentence that had one printed *"never drifts.."*. Pinned by walking every slide and comparing `bounding_box()` in `explore_mount_check.py`; **proved to FAIL on the old code first** (232, 254, 232, 233). | PAKKA |
| **E14** | Anything that INTERRUPTS — a pop-up, a promo card, a nudge nobody asked for | ⚠ **Four rules, and it does not ship without all four.** (1) **ONE at a time** — the offer pop-up takes only the FIRST promoted offer, so two live discounts are still one card. (2) **Closing it means closed**, remembered per browser and ⚠ **keyed by the offer's id**, so a NEW offer still gets its turn — a single "seen the popup" flag would have silently killed every future promotion. (3) **It arrives AFTER the page**, not with it: a card that is simply there on the first frame is part of the layout and gets scrolled past. (4) **It is NOT a modal** — nothing dimmed, nothing trapped, Escape and a ✕ both close it. And it FAILS CLOSED: no offer, an unreadable endpoint, or the admin switch off ⇒ nothing is drawn, never a "loading your discount". | PAKKA |
| **E15** | Anything a customer can be SHOWN but not edit — an offer card, a banner, a nudge | ⚠ **Its words belong to the admin panel, not to the JSX.** The Explore pop-up's heading, its bullet lines, its small print and its button words are five fields on the offer row (`popup*` in `offers.py`), typed in **Sales → Offers**, capped server-side, and every one of them optional — an offer created before the fields existed still pops up, headed by its own `label` and `summary`. ⚠ The bullet box is a `GrowTextarea` (**E1**) and is split on the newline **only on the way out** (**E8**). | PAKKA |
| **E16** | Reaching for the gold gradient | ⚠ **Gold means THE ACTION on that screen — one per screen, and never on navigation.** Explore's first workflow tile shipped as a gold slab, copied from the reference; asked to stop being one: *"ye 3 button ka colour ek jaisa rakho, ismai golden hata do."* The rail's order is the owner's own pipeline, so painting whatever happens to be first as the recommended one says something nobody meant — and gold stops meaning "press this" the moment a tile you merely navigate with is wearing it. | PAKKA |
| **E17** | Shortening a name to fit a narrow place | ⚠ **Give EVERY workflow its short name, including the ones switched off — and keep the long one on hover.** The collapsed rail grew a label under each glyph and the names did not fit; the words were chosen one by one (*"Script to Storyboard ka only Storyboard, Image to Animatic Image ko Animatics, Video Editor ka Editor"*) and the same reply asked for the rest: *"jo hide mai kiya hai uska bhi kar hi dena, to mai hide wale ko on karun to ye kaam fir nahi karwana pare."* Three of the six are hidden today and one click from returning. `WORKFLOW_SHORT` in `Sidebar.jsx` names all six; `shortLabel()` falls back to the label's LAST WORD for a workflow an administrator adds later. Both names sit in the DOM at once and CSS picks one — swapping the text on collapse would put the layout in charge of the copy. | PAKKA |
| **E18** | Any label that can be ellipsised | ⚠ **Measure it in a browser, and measure it with a `Range` — `scrollWidth > clientWidth` LIES here.** A shrink-to-fit block inside a centred flex column never gets a scroll extent, so the obvious test reported green on a rail that was visibly printing *"Storyboa…"*. `document.createRange().selectNodeContents(el)` measures the words; the element's own rect is what they must fit inside. ⚠ **And it was proved to FAIL first**, at 70px, where it named all three offenders. `text-overflow: ellipsis` is the one failure mode that looks like a rendering glitch rather than a bug, so nobody reports it. | PAKKA |
| **E20** | Anything a customer READS that a developer wrote | ⚠ **Move it into the admin panel, and leave what it does today as the FALLBACK.** Explore's two billboards were built from the workflow list — heading from a workflow's name, body from its landing-page pitch, artwork from its own glyph — so the one part of the app whose job is to say something could only be changed by a redeploy, and could not carry a picture at all. *"This banner should be change aur hide by the admin, of it text and image."* `server/banners.py` + the **Banners** tab now own the words, the picture, the order and the switch. ⚠ **AN EMPTY STORE IS THE SHIPPED STATE, NOT AN OUTAGE** — with no live banner in a slot the page draws exactly what it drew before, and hiding the last one is the same code path. The panel adds a voice; it never becomes a prerequisite for the page rendering. | PAKKA |
| **E21** | Adding an uploaded picture to anything | ⚠ **Copy `branding.py`: a NEW ID PER UPLOAD, the row repointed BEFORE the old file is deleted, and the id checked against a regex on the way back out.** The id is the cache — a new upload is a new URL, so every browser, proxy and open tab picks it up with nobody reasoning about headers, and overwriting one path is exactly how a picture change fails to appear for the person who most needed to see it. ⚠ Serve only ids the store actually HOLDS, or a well-formed name reads whatever else ever landed in that directory. ⚠ And normalise to **WEBP at 1280px** for a banner (PNG at 512 is the logo's rule and would put a megabyte on the front page), keeping RGBA so a cut-out does not arrive inside a black rectangle. | PAKKA |
| **E22** | A form field that is empty because it has not been touched yet | ⚠ **Do not print the complaint until somebody has typed.** The banner form opened with *"A banner needs a heading"* already in red under an untouched box — an accusation, on a screen somebody came to CREATE something on. A disabled action button is the signal until then; the sentence appears once the form is `touched`. | PAKKA |
| **E23** | A masonry wall of pictures that leaves a hole | ⚠ **The cause is the RATIO SPREAD, not the packing — bound the ratio, and cap the column count by the item count.** *Found, reported twice: "gallery me lamba 9:16 board wala column bagal me khali jagah chhod raha hai."* Measured, seven projects gave columns ending at 483, 483, 323, 323 and **154** — the last two thirds empty. ⚠ **SHORTEST-COLUMN PACKING WOULD NOT HAVE HELPED**: a 9:16 board is 3.2× the height of a 16:9 one in the same column and nothing can fill the gap it leaves. ⚠ **AND FEWER COLUMNS MAKES IT WORSE, WHICH IS THE TRAP** — fewer columns are WIDER columns, and the tall card grows from 466px to 843px. `wallAspect` pulls every card into 4:5..16:9 (a 9:16 board loses ~30% of its height, evenly, top and bottom) and `wallColumns` stops five columns holding one card each. ⚠ **THIS IS THE ONE SCREEN THAT CROPS** and it is a deliberate exception to the libraries' rule: `aspectStyle` protects a THUMBNAIL you identify a project by at 86px; this is a wall with a caption under every card. Pinned by measuring the columns in Chromium, and **proved to fail on the old code first** (32%). | PAKKA |
| **E19** | A new screen that prints the app's NAME or draws its MARK | ⚠ **`useBranding()` for the words, `<Logo/>` for the mark — never a string literal and never a second copy of the SVG.** The name was typed into eight components and the mark drawn in two more, so renaming the app was a code change and the icon could never be the owner's own logo. Both are one save in **Admin → Brand** now, and the save lands live in the open tab because the panel writes straight into the store (`setBrand`). ⚠ **The uploaded logo wins over the drawn one inside `Logo.jsx`**, which is why every caller gets it for free — do not branch on it yourself. ⚠ And the built-in mark in `Logo.jsx` is NOT dead code: it is what a deployment with no upload shows, what **Remove logo** goes back to, and what is drawn before the branding call answers. The grep in `tests/branding_check.py` is what stops the ninth screen going stale. | PAKKA |
| **E20** | Any UPLOADED IMAGE that sits on a themed surface (a logo, a watermark, a supplied icon) | ⚠ **It needs ONE FILE PER THEME, and the swap belongs in CSS off `<html data-theme>` — not in React state.** *Found the day the logo upload shipped: a white wordmark read perfectly on the dark rail and disappeared completely in light mode — "jab mai dark mode mai hun to mera logo dikh raha hai magar jab light mode mai karta hun to mera logo white mai merge ho raha hai."* ⚠ **The drawn marks do NOT have this problem and that is what hid it**: `Logo.jsx`, `Icon.jsx` and `WorkflowIcon.jsx` are painted in `currentColor` and re-colour themselves for free, so an uploaded picture is the FIRST thing in this app that cannot. ⚠ **Either slot must fill in for the other** — one upload is a complete answer, because the commonest mark has its own colours and reads on both; a second file is for when it genuinely cannot. ⚠ **CSS, not JavaScript**, because the landing page, the sign-in card and the public storyboard viewer are drawn OUTSIDE the app shell and never see its theme state — the same three screens that forced the brand store to be a module rather than a Context. `base.css` hides `.brand-mark-light` by default and swaps under `:root[data-theme="light"]`; the unconditional default matters, or a missing `data-theme` would draw BOTH marks side by side. ⚠ **The favicon is the one exception** and does need JS — a `<link rel=icon>` cannot be styled — so `branding.js` watches `data-theme` with a `MutationObserver` rather than asking the four places that flip the theme to each remember. ⚠ **And the admin preview shows BOTH GROUNDS AT ONCE, with the colours hard-coded from `theme.css`** rather than `var(--panel)`: previewing a logo on whatever theme the panel happens to be in is exactly how this shipped white-on-white in the first place. | PAKKA |
| **E24** | Deciding which SIDE of the sign-in a screen lives on | ⚠ **A screen that SELLS is logged-out only; a screen that WORKS is logged-in only. One screen must not try to be both.** *Found: Explore was the signed-in front door — billboards, workflow tiles, and every project the account owned as one picture wall — and it was asked to become the marketing page instead: "the page we created on explore should be used to market ... any logged in user must not see explore which is happening right now ... the explore page is going to be only used for getting users, nothing more than that".* ⚠ **MOVING IT IS THREE CHANGES, AND ALL THREE OR NONE.** (1) Take the row out of `Sidebar.jsx` **and** the branch out of `App.jsx` — a page with no door that the shell can still be navigated to is a blank screen waiting for a stray nav string. (2) Move `LANDING_NAV` (**E12**). (3) Re-source every fetch: a public screen may read ONLY `/public/*`, because it is drawn before a token exists. ⚠ **AND ITS CONTENT HAS TO CHANGE WITH IT.** The wall was the customer's OWN work, which is an empty grid to a stranger on the one screen whose whole job is to make them want to sign up — so it became `server/showcase.py`, admin-curated. ⚠ The old reason for having **no public gallery still stands word for word**: nothing in this app is shared by default, so the work on that wall is only ever work an administrator chose to put there, never a customer's. | PAKKA |
| **E25** | A control on a PUBLIC page that leads into the product | ⚠ **It opens the sign-in, and it CARRIES what the person clicked.** *"if anyone clicks anywhere to use and create any workflow we must give a user first to login and then use."* Out there is nothing to navigate to, so a control wired to `onNavigate` looks perfectly right and does nothing at all — every one of them takes `onSignIn(workflowId)` instead. ⚠ **AND THE SECOND HALF IS THE HALF THAT GETS DROPPED**: `App.jsx` holds the id in `pendingWorkflow` and `onAuthed` opens it. Clicking "Script to Storyboard", typing a password and arriving on a generic dashboard is how an interested visitor is lost in the two screens between the click and the account. ⚠ **SANITISE IT ON THE WAY THROUGH** — `asWorkflow()`. One of those ids is a banner's `cta_target`, which an administrator TYPED and the server only shape-checks; "admin", "explore" and a plain typo all pass that check, and a nav nothing matches is a blank page as somebody's first screen after signing up. ⚠ A discount pop-up on a public page gates too: `POST /billing/coupon` is signed-in only and would 401 in front of a prospect, so the code is readable and copyable and the button asks for an account. | PAKKA |
| **E26** | Putting VIDEO on a page | ⚠ **The card carries a POSTER; only the viewer carries a player.** *"the videos or images should be clickable and be able to use it properly play."* Twenty-four `<video>` elements on the page everybody lands on is twenty-four downloads before anyone has clicked anything — on a phone that is the page not loading. The clip is fetched when it is asked for. ⚠ **KEY THE `<video>` BY ITS ITEM.** React reuses one element when only `src` changes, and a player whose src changes underneath it keeps the old buffer, the old `currentTime` and sometimes the old AUDIO — so stepping from one film to the next leaves you listening to both. A new key is a new player. ⚠ **`controls` + `autoPlay` + `playsInline`, and do NOT mute to force the autoplay through.** A browser refusing to autoplay an unmuted clip is the right outcome: `controls` means the play button is already under the pointer, so a blocked autoplay costs one click — muting it costs a silent film nobody realises has sound. ⚠ **Serve it with `FileResponse` and store it AS IT ARRIVED.** Range requests are what let a browser seek instead of pulling the whole file down first; a re-encode on the request path is the change somebody makes when an upload feels slow, and it is the change that breaks the one screen where watching the film IS the demo. ⚠ **There is no `ffprobe` on an `imageio-ffmpeg` install**, so a poster cannot be grabbed off a clip and its shape cannot be measured — ask for both (a still upload, an aspect dropdown), and measure only what Pillow is already holding. | PAKKA |
| **E27** | Adding a TAB to the admin panel | ⚠ **Add it to the field sweep in `tests/admin_fields_check.py` in the same commit — the sweep is a hand-written list, not a loop.** *Found while adding Showcase: the heading said "the six tabs" and it walked six while the panel had SEVEN. Brand and Banners had never been swept once, and they are the two tabs most full of typed copy — which is the exact thing that file measures.* ⚠ **AND OPEN THE CREATE FORM WHILE YOU ARE THERE.** Every one of these panels keeps its form collapsed behind a ＋ button, so a sweep that does not press it measures an empty screen and passes by having nothing to look at — which is how the offer form's clipped bullet box survived. ⚠ Wait on `.admin-card`, not on a row selector: a fresh install has no rows and the sweep would hang. | PAKKA |

---

| **E28** | A wall, gallery or list that accepts a VIDEO | ⚠ **It makes its own thumbnail. Do not ship a screen that asks for the still as a second upload.** *Reported: "when i upload video from admin panel but when i see explore page so no thumbnail show in my upload video"* — with a screenshot of a bare grey card. Nothing was broken: the poster field, the route and the "Add still" button all existed and worked, and **not one thing on the page said a second upload was expected**, so the feature was invisible and the card looked like a bug. ⚠ **THE REASON IT WAS BUILT THAT WAY WAS A WRONG FACT, AND IT IS WORTH KNOWING WHY**: the code said a frame could not be grabbed because *"there is no `ffprobe` on an `imageio-ffmpeg` install"*. True, and irrelevant — **`ffprobe` INSPECTS, `ffmpeg` EXTRACTS**, and `imageio-ffmpeg` ships ffmpeg, which `animatic.ffmpeg_exe()` had been locating for the exporter all along. A capability the repo already had was written off in a comment and then designed around. ⚠ **BUT THE OLD REASONING WAS RIGHT ABOUT BLACK**: films open on black, so probe a little way IN (1.0s, 2.5s, 0.5s, 0.0s) and **refuse a frame that comes back black** rather than shipping a wall of black rectangles — no poster falls back to the glyph, which is honest. ⚠ **THE GRAB FILLS AN EMPTY SLOT ONLY.** A still a person chose outranks frame one and must survive a re-upload of the clip. ⚠ **AND IT RUNS OFF THE EVENT LOOP AND FAILS SOFT** — `run_in_threadpool`, because the handler is `async def` and the file may be 96MB; and a thumbnail is never worth failing an upload over. Pinned by `tests/showcase_check.py` §11 with **real clips built by ffmpeg** (colour, portrait, all-black, and bytes that do not decode) — a fake MP4 could not have asked any of those questions. | PAKKA |
| **E29** | A page whose whole job is to be SHOWN to people | ⚠ **Give it an address.** *Asked as "where is ,my marketing explore page"* — after a VS Code task named MARKETING PAGE opened on the sales page instead, because Explore was internal state (`authView`), not a URL. A shop window you cannot send anybody a link to is a shop window facing a wall: it could only be reached by landing on `/`, reading the sales page and finding "See the work". ⚠ **COPY `?admin`, DO NOT INVENT A ROUTER** — one `syncUrlFlag` helper writes both, `replaceState` not `pushState` (see the note on `ADMIN_PARAM`; there is nothing to answer a Back button). ⚠ **AND A PUBLIC ADDRESS MUST KNOW WHO IT IS NOT FOR**: `?explore` is honoured for a VISITOR and dropped the moment somebody signs in, because `LANDING_NAV` is the standing decision that a signed-in customer never sees Explore again. The effect watches `authed` AS WELL AS the view — signing in does not touch the view, so without it the address would still read `?explore` while the customer is looking at their own Home. | PAKKA |

| **E30** | Two controls side by side where one changes the THING and the other only its COVER | ⚠ **Say which is which on the control itself — the type chip beside them is not enough.** *Reported with a screenshot: "maine admin panel se image daala hai — magar jab explore pe kholta hun to video khul raha hai, image nahi. Aisa kyun?"* The row said **VIDEO · 16:9** and the wall was serving the right file for the right item — proved end to end, two clips, byte-for-byte. Nothing was broken: **"Replace still" had set the COVER, and the item was still a video**, so clicking it played the video exactly as designed. ⚠ **THE FAULT IS THAT THE TWO BUTTONS LOOK LIKE VARIANTS OF ONE ACTION.** "Replace file" and "Replace still" sit next to each other, same size, same style, one word apart — and only ONE of them changes what a visitor sees when they click a card. Both tooltips now lead with the distinction (**"THE THING ITSELF — what opens when somebody clicks this card"** against **"ONLY THE COVER — it does NOT change what opens"**), per the standing rule that the hint goes on hover and the visible label stays short. ⚠ **AND THIS IS THE SECOND REPORT IN A ROW FROM THE SAME ROW OF BUTTONS** — E28 was the still nobody knew to upload, this is the still that got uploaded instead of the file. When a screen produces two "nothing is broken" reports running, the screen is what is broken. | PAKKA |

| **E31** | Two tabs in the panel that edit ONE screen | ⚠ **Make it one tab with sections inside it — and when a comment has to explain that two tabs are the same page, that is the design telling you.** *Asked for outright: "admin panel banners and showcase both section same work kar raha hai explore page ke liye — so tum ek explore ka hi banao aur uske under banner and showcase rakho."* The old tab strip had already half-admitted it: the note above `showcase` read *"IMMEDIATELY AFTER BANNERS, because they are the SAME PAGE … two tabs apart is two tabs too far"* — **adjacency was the workaround, not the fix.** ⚠ **NEITHER CHILD WAS REWRITTEN**: `AdminExplore` is a segmented strip and a switch, and `AdminBanners` / `AdminShowcase` still own their note box, their card and their create form. Merging their markup would have turned two working editors into one half-tested one, over two stores, two routes and two upload rules that are genuinely different things sharing a page. ⚠ **REUSE THE PANEL'S OWN `admin-segment`**, which Activity, Sales, Features and the user detail already use — a second strip that merely resembled them is the mismatch this repo keeps paying for. ⚠ **AND MOUNT ONE AT A TIME**, not both hidden with CSS: each half fetches on mount and holds an open create form, so a half-typed banner surviving a trip to Showcase and back is state nobody expects and nobody tests. | PAKKA |

| **E32** | A screen needs a rail, a header or a panel the app ALREADY HAS | ⚠ **Give the existing component a MODE. Do not build a second one that looks like it — not even for a page with a different shell.** *Paid for in one round trip*: the public Explore page got a hand-built `xp-rail` copied from `.sidebar`'s measurements, and it came back the same day — *"mai chahta hun ki ye sab waisa hi dikhe jaise user login kar ke dikhta hai — jaise abhi missing hai collapse bar and logo and AI Studio name."* **A copy is never finished being close.** It shipped without the brand mark, the app name and the collapse toggle, and every one of those would have come free from the real component. ⚠ **THE MODE SUBTRACTS, IT DOES NOT RE-DESCRIBE**: `publicMode` on `Sidebar` removes the avatar and the account menu — the two things a visitor has no version of — and swaps the word on the gold button at the foot. Everything else is the same markup and the same stylesheet, so the two can no longer drift. ⚠ **AND THE SUBTRACTION HAS ITS OWN BUGS — LOOK FOR THEM.** Removing the avatar removed the `margin-left: auto` that pushed the whole brand row's buttons right, so the collapse toggle tucked itself against the app's name; `.sidebar-public .sb-collapse` takes the job over. **Whatever a mode hides, check what that element was CARRYING for its neighbours.** ⚠ **A DIFFERENT SHELL IS NOT A REASON TO FORK.** Explore has no `.shell` around it, which is what made a copy tempting; the answer was to give the page `.shell`'s own grid (280px / 92px / one column under 820px) rather than a second rail. | PAKKA |

| **E33** | Putting a rail beside a page that has many rows | ⚠ **Give the page ONE wrapper element and make the grid two cells — do not place the rows individually and span the rail with `grid-row: 1 / -1`.** *Shipped and reported the same hour: "pahla image pura blank hai, magar jab scroll kiya to tab aaya ye content — uper mai blank kyun dikh raha hai."* ⚠ **`1 / -1` DOES NOTHING WITHOUT `grid-template-rows`.** A negative grid line counts back from the end of the **EXPLICIT** grid; an implicit grid has none, so `-1` resolves to line 1, the span collapses to a SINGLE row, and that row is as tall as the rail — 100dvh. Everything sharing it is stretched down a screen-tall cell and the rest of the page starts below the fold. ⚠ **AND THE THING THAT MOVED WAS THE HEIGHT, NOT THE TOP.** The header's box still began at y=0; it was STRETCHED to 900px, and `.landing-nav`'s own `align-items: center` floated its links half way down the blank screen. A check written as `nav.top < 160` passes on the broken page. **Measure the height.** ⚠ **THE ANSWER WAS ALREADY IN THE REPO**: `.shell` + `.shell-main` is exactly this, two cells of one row, and had been since it was written. The objection that a wrapper would orphan eight listed selectors was wrong twice over — they were ONE rule, and re-pointing them was one edit. ⚠ **G7, SHARPENED: A SOURCE CHECK IS NOT A LAYOUT CHECK.** `npm run build` passed, and thirty assertions in `showcase_check.py` about that grid were all TRUE while the page was visibly broken — two of them asserted the very declarations that CAUSED it. Geometry is measured in Chromium by `tests/explore_layout_check.py`, which reproduces the blank screen in-page (`display: contents` on the wrapper plus the old span) and must catch it. | PAKKA |

| **E34** | A rail beside a page — and the tests around a component that gained a MODE | ⚠ **The rail must carry a row for the page it is standing on, marked active.** *Asked for by name: "explore ka button kyun nahi dikh raha hai, ye page kahan se khul raha hai? home ke upar explore button daalo."* The public rail listed Home and the workflows and never named **Explore**, which is the page it was drawn on — so nothing was highlighted, and pressing Home left for the sales page with no way back but a link buried in its nav. A rail that cannot say "you are here" is a rail with a one-way door in it. ⚠ **AND HERE IS THE PART THAT BITES TWICE: ADDING A MODE ROTS EVERY "NOT IN FILE" ASSERTION.** Three checks failed the moment this row landed, all of them written as *"the string X does not appear in this component"* — a fair proxy for a rule that was once absolute (**nobody signed in may see Explore**) and meaningless once the SAME component draws both sides. **Re-assert the rule as "behind the guard", not "absent from the file"**: the row exists exactly once and `{publicMode && (` sits above it — and prove the new check fails with the guard removed, which this one was. ⚠ The other two had grown a third branch and were matching a whole handler as one string; they name the three destinations now, which is stronger than the line they replaced. See also **E32** (give the component a mode) and **G7** (a source check is not a rendered screen). | PAKKA |

| **E35** | Artwork on a PUBLIC page that belongs to a workflow | ⚠ **Draw it from the live workflow list, and let the owner upload the picture — never hard-code the tiles.** *Asked for outright: "mai chahta hun ki ye four icon ke jagh mai image lagun so admin panel mai landing page ka fuction bano so mai image dall sakun har workflow ka … jo live hai uska dikhe image yaha pe aur jo hide hai uska nhi dikhe."* The landing hero's four tiles were four hard-coded lines of JSX holding four hand-drawn SVGs, in pipeline order, with **nothing in the file saying which drawing belonged to which workflow** — so hiding a workflow in the admin panel left its picture on the front page, and the biggest artwork on the page every prospect lands on could not be changed without a developer. ⚠ **THE FALLBACK CHAIN IS THE RULE, NOT THE UPLOAD.** Three rungs, in this order: the uploaded picture, then the drawing (`ART_BY_ID`, keyed by workflow id), then the workflow's glyph — **and the glyph needs the server's emoji passed in as `fallback`, because `WorkflowIcon` renders NOTHING for an id it has no glyph for.** Miss that last rung and a workflow launched after the build gets a blank white tile. ⚠ **AND THE VISIBILITY FILTER GOES ON THE SERVER TOO.** The client already draws only what `/public/workflows` named, which is enough to make the page correct — and leaves a picture of a switched-off workflow sitting in a public JSON payload. `landing.public_payload()` drops it. ⚠ The store is keyed by **workflow id, not by a row id an admin invents** (the difference from `banners.py`), which is what makes a seventh workflow need no plumbing; the tile CAP is a layout number and the panel has to SAY when a picture is stored but not drawn, or an operator stares at a good thumbnail and an unchanged page. Pinned by `tests/landing_art_check.py` — including the hide→un-hide round trip, because a "hidden" that also deletes the file is a different product. See also **E3** (copy the existing panel), **E31** (one tab per screen). | PAKKA |
| **E36** | A rail row that is CENTRED when the rail is collapsed | ⚠ **A `margin-left: auto` written for the wide rail must be scoped `:not(.collapsed)`.** In a COLUMN flex box `margin-left: auto` still eats the free space on the left and pushes the button to the RIGHT edge — it does not switch itself off. *Found on Explore: the collapse-bar toggle sat off-centre under a centred logo, because `.sidebar-public .sb-collapse` gave it `margin-left: auto` to push it right in the WIDE public rail (a visitor has no avatar to do that job) and the same rule kept firing once the rail became a column.* Now `.sidebar-public:not(.collapsed) .sb-collapse` — collapsed, `.sb-brand`'s own `align-items: center` is left to do it. | PAKKA |
| **E37** | The SAME component drawing a signed-in screen and a public one | ⚠ **A row that means two different things on the two sides gets a `publicMode` guard — never a deletion — and the tests must assert its ABSENCE on one side and its PRESENCE on the other.** *Found on Explore: the visitor's rail carried **Home** directly under Explore AND the page's own nav carried a second **Home** link four rows away, both meaning "leave for the sales page" — "not need to show home buttun in explore page", reported with a picture of each.* Signed in, Home is the front door (`LANDING_NAV`); signed out, Explore IS the page. So Home is behind `{!publicMode && (` and Explore behind `{publicMode && (`, a mirrored pair in `Sidebar.jsx`. ⚠ **One-sided checks are how this breaks:** `'onNavigate("home")' not in sidebar` would pass with the app's first row deleted, so `showcase_check.py` pins the row's count AND its guard, and `explore_layout_check.py` asserts no Home on the public rail while `explore_mount_check.py` asserts Home is row one inside the app. ⚠ **Remove BOTH copies of a word or the fix reads as half-done.** ⚠ Explore now offers no door backwards at all (`syncExploreUrl` uses `replaceState`, so browser Back does not go there either) — deliberate: it is a shop window whose exits are forwards. | PAKKA |
| **E38** | A page laid out for a laptop, opened on a 27" monitor | ⚠ **A fixed `max-width` is only half a responsive page — give the cap a RANGE, and cap the CARDS separately from the page.** *Reported with a screenshot of a 2560px display: "make it screen responsive either 27 inch screen and what so ever".* Explore's rows were pinned at a flat `1560px`, so a 27" screen showed the same narrow column with ~700px of dead ground either side — and the picture wall INSIDE that column stretched its two cards to ~1000px each, because the wall's column count is capped by the ITEM count (`wallColumns`), and multi-column stretches whatever columns it has to fill its box. So the page looked small and the cards looked enormous at the same time. ⚠ **THE OLD NUMBER BECOMES THE FLOOR, NOT THE ANSWER**: `--xp-max: clamp(1560px, 88vw, 2200px)` — every screen up to ~1770px lays out byte-identically, so nothing already measured moves, and only the big screen changes. ⚠ **AND THERE IS STILL A CEILING** — `none` gives a 2400px line of body text and a nine-column wall. ⚠ **CAP THE WALL, NOT THE CARD**: a `max-width` on a card leaves a hole beside it in a column it no longer fills; narrow the multi-column box instead (`.xp-page > .xp-gallery`), with a 72rem floor so laptops are untouched. ⚠ **AND A CAPPED ROW MUST SPELL ITS LEFT EDGE OUT** — its neighbours are CENTRED in the page column, so `margin-inline: auto` centres the narrower box on its own width and it sits inboard of the heading above it. ⚠ **Responsive rule 4 cuts both ways**: growing the page without re-cutting the billboards turns the top of the screen into a letterbox strip, so there is a `min-width: 1800px` stage for their height. Measured in Chromium at 2560, 1920, 1440, 1100, 760 and 390; `tests/explore_layout_check.py` takes the 2560 measurements now and is GREEN on the real page (row 2200px, wall 1152px, card 497px), with the flat cap put back as the proof it can fail (row → 1560px, card → 701px). ⚠ **AND A CHECK THAT SKIPS ITSELF WHEN ITS FIXTURE IS MISSING IS A CHECK THAT DOES NOT RUN.** The card assertions were written behind `if wall and card:` with a friendly `else: print(...)`, and this file boots EMPTY stores — so the wall was never on screen and the ballooning card was never measured, while the section printed green. **Seed the fixture and assert its presence**; never let a missing fixture read as a pass. | PAKKA |
| **E39** | Handing this app's cut to ANOTHER editor (Premiere, Resolve, Avid, FCP) | ⚠ **A project file is a RECIPE, not the food — so the media travels with it or the user gets a timeline of red "Media Offline" rectangles.** `GET /animatics/{id}/interchange` answers with a **ZIP**: the FCP7 XML plus a `media/` folder holding every picture, clip and sound the XML names, and `media=false` is the deliberate exception for somebody who already holds the footage. ⚠ **And the `<pathurl>`s are RELATIVE unless the user says where they will unzip** — only an absolute `file://localhost/…` re-links with nothing asked, and at export time nobody knows where the zip will land; relative costs ONE "Locate" in Premiere's dialog, and because every file is in one folder, locating one finds them all. The optional "Unzip folder" field writes the absolute form for the user who does know. | PAKKA |
| **E40** | An export that cannot carry everything on screen | ⚠ **Say what will be LOST, by name and by count, BEFORE the download — not after.** A project-exchange file holds the CUT (which clip, where, how long, on which track, the source window, the audio and its level); no format on earth holds this app's WebGL grades, LUTs, masks, blend modes, fourteen transition shapes, text clips or shape clips. `GET /interchange/preview` costs nothing and touches no media, and `ProjectFileModal` prints its list — *"3 colour grades · 2 masks · 1 text clip"* — because "some effects won't transfer" is a sentence people skip and a counted list is a decision they can make. ⚠ **A clip whose file has gone is NAMED too**, in red, not silently dropped. | PAKKA |
| **E41** | Converting this editor's milliseconds into an NLE's frames | ⚠ **ONE conversion, in ONE function, rounding half AWAY FROM ZERO.** `interchange.ms_to_frames` — and **not Python's `round`**, which rounds half to EVEN and would send one cut down and the next up; the drift is invisible for ten clips and is a lip-sync bug for a hundred. ⚠ **And the spans are computed from the FULL clip list before anything is filtered** (`animatic_render.frame_spans`): a clip with no `start_ms` sits after the last clip *on its own track*, so dropping a hidden or file-less clip first slides every later clip on that row and exports a different film. ⚠ `speed` is deliberately NOT exported — the source window is forced to the timeline length and the speed change is reported as a loss, because a clip of the wrong footage is worse than a clip somebody has to re-speed. | PAKKA |
| **E42** | Exporting the same cut in more than one format | ⚠ **ONE model, one writer per format — never a second reading of the timeline.** `interchange.build_sequence` folds the project once; `FORMATS` maps a name to a writer and `format_losses` to that format's own ceiling. ⚠ **AND THE CEILING IS ADDED TO THE BASE LOSSES, NEVER SUBSTITUTED FOR THEM** — an EDL loses the grades *and* every upper video row, so the preview endpoint takes the format and the dialog re-asks when the dropdown changes. A dropdown that changes the download but not the warning is worse than no dropdown: the user reads the Premiere caveat and receives the EDL. ⚠ **One file per format on disk** (`<title>-edl.zip`), the same rule `_video_path` follows for mp4/gif/png — a single `<title>.zip` means fetching the EDL destroys the XML fetched a minute earlier. ⚠ **The `media/` folder is IDENTICAL in all three zips**; only the document at the top changes. | PAKKA |
| **E43** | Writing a file another APPLICATION will parse (not a person) | ⚠ **Match the target's real constraints, not the ones that look reasonable.** Three paid for here, each of which produces a file that looks fine and fails silently: **(a) ExtendScript is ES3** — `{in: 0}` is a *syntax error*, so an After Effects script with a reserved word as a key does not load at all (no comp, no message); the data block says `srcIn`, and the generated file is **pure ASCII** because ExtendScript decides a file's encoding from a BOM and the host locale — the project title reaches AE through `json.dumps`, which escapes it. **(b) An EDL reel name is EIGHT characters**, and older systems truncate rather than complain, so two files differing after character eight conform as one tape; the real filename goes in `* FROM CLIP NAME:`. **(c) EDL events sort by record time with VIDEO first on a tie** — sorting the channel as a *string* puts `A` before `V`, so a film whose voiceover starts at 00:00:00:00 opens with an audio event, which is legal and wrong to every human reading it. | PAKKA |
| **E44** | Bringing ANOTHER editor's cut into this one | ⚠ **It ADDS onto new rows; it never replaces the open timeline** — an import that overwrote what was on screen would be the most destructive button in the editor, and rows are cheap. ⚠ **The server produces the material, the CLIENT decides the timeline** (`import_storyboard`'s contract): `track` and `layer_id` come back RELATIVE (`0,1,2…` / `_import_0`) because only the browser knows which rows are free, and every imported audio lane gets a real row — `layer_id: ""` is the DEFAULT lane and would pile an import into whatever is already there. ⚠ **ONE `flush` = ONE undo step**, which is the actual safety net: the frames, the rows, the sounds, the transitions and the library cards go up in a single write, so Ctrl+Z takes the whole import back out. ⚠ **The patch key is `audioTracks`, not `audio_tracks`** — the API answers in snake_case and the DOCUMENT is camelCase, so the copy-paste spelling is an unknown key: the import looks perfect and the sound is gone on the next reload. Pinned, and *proved to read False when broken*, in `tests/interchange_check.py` §12. | PAKKA |
| **E45** | Media that a project file names but the user did not bring | ⚠ **A labelled COLOUR CARD in the right place, never a silent omission.** A project file names files by a path on the machine that wrote it; a browser cannot read that path, so the footage has to be attached (or the user brings back a `.zip` exported from here, which already holds both). Leaving the unmatched clips out hands back a timeline with holes and no way to see what is missing or where; a card carrying the clip's own name keeps the CUT whole, makes every gap visible, and is fixed by dropping the real file on that row. Every one is named in `placeholders` and printed in the dialog **before** anything is added. ⚠ **Sound gets NO placeholder** — a silent picture card is readable, a silent audio clip is a lie you cannot see. ⚠ **Matching is by FILENAME, case-insensitively, and by the stem as well**, because Windows and macOS disagree about case and a transcode leaves `shot_03.mov` in the XML with `shot_03.mp4` on disk. | PAKKA |
| **E46** | A round trip out to a project file and back | ⚠ **It is FRAME-exact, not millisecond-exact, and expecting the millisecond back is the bug.** This editor holds time in ms; every NLE holds it in frames. At 25 fps a clip placed at 500ms is at frame 12.5, which does not exist — it exports as frame 13 and returns as 520ms, and exports as frame 13 again. Nothing drifts; it snaps once. A test asserting the original millisecond fails on correct code, so assert **whole frames, and within ONE frame of where it started**. ⚠ **Reading in goes through MILLISECONDS on purpose** (`frames_to_ms` at the document's OWN rate), which is what lets a 25fps XML open correctly in a 24fps project — converting frames straight to frames would nail somebody else's rate onto this film. | PAKKA |
| **E47** | Reading a file format whose owner has never published it (`.prproj`, `.aep`) | ⚠ **The refusal stays the DEFAULT answer, and the guess is the SECOND one — offered only after the refusal has been read.** A `.prproj` is Premiere's own save file: no specification, and the structure moved again in Premiere 2026. So `read_document` refuses it with a sentence naming a route that always works (*File › Export › Final Cut Pro XML*), and `_read_prproj` runs only on `experimental=True`, which the dialog sends only after showing that sentence. ⚠ **An experimental reader offered as a checkbox up front is one most people tick without ever seeing the reliable door beside it** — that is why it is a button under the error and not an option above the file picker. ⚠ **But the door must still LOOK like a door.** It shipped as a `.btn ghost`, which draws neither border nor background, so the only thing on that panel there was to press read as a line of text — caught from a screenshot during the first live test. A bordered `.btn`; never `primary`, which would put the guess level with the reliable route and undo the ordering this rule is about. ⚠ **It fails LOUDLY and PARTIALLY, never quietly and completely**: it walks `ObjectRef` references rather than a fixed XPath (a `.prproj` is a flat pile of objects, so a clip's position is four hops from the clip), and when the track layout is unrecognisable it falls back to one row per media type and SAYS the rows are wrong rather than refusing. ⚠ **Every import it produces carries “best-effort, check it against Premiere” as `warnings[0]`, and the dialog badges the report `best guess` for as long as it is on screen** — a caveat only in a list somebody scrolls past is not a caveat at the moment they press Add. ⚠ **And the tests cannot prove it works.** The fixture in `tests/interchange_check.py` §8c is a hand-built imitation and there is no writer to round-trip against; green there means the reader behaves as it claims on the shape it claims, NOT that it opens a real Premiere project. Say that in the release note. | PAKKA (the policy) / OPEN (never run against a real `.prproj`) |
| **E48** | Accepting a COMPRESSED file through an upload limit | ⚠ **The upload cap counts the compressed bytes, so the only place that can stop a decompression bomb is the reader.** `MAX_UPLOAD_BYTES` passed a few megabytes of gzip that unpacks to gigabytes; `interchange._gunzip_capped` decompresses with an explicit ceiling (`PRPROJ_MAX_XML_BYTES`) and refuses past it, because XML of this shape packs at better than 1000:1. ⚠ **A cap added at the route would be the wrong place** — it would have to guess the ratio for every format; the reader knows what it is unpacking. Same rule for any zip, gzip or archive this app is handed. | PAKKA |
| **E49** | Reading a SETTING (frame rate, size) out of somebody else's project file | ⚠ **Take it from the part of the file you are actually importing, never from “anywhere in the document”.** Found by review, not by a run: `_read_prproj` picked the right sequence and then read the rate off the FIRST one in the file, because Premiere keeps the rate on a track-group object several hops from the sequence and the first version only looked one hop. A project holding a 30fps sequence above the 24fps one being imported read every clip **25% adrift** — and a wrong rate does not look like an error, it looks like a film. `_prproj_rate_near` now walks that sequence's own subgraph and never crosses into another `Sequence`; both fallbacks (first rate in the file, then the project's own fps) are **warned about**. ⚠ **And when a best-effort reader gives up, say THAT, not “the file was empty”** — clips found whose positions could not be read now raise a refusal naming the real reason, instead of falling through to the route's “There was nothing on that timeline to bring in”. Pinned in `tests/interchange_check.py` §8c with a two-sequence fixture whose rates differ, so a document-wide search cannot pass. | PAKKA |
| **E50** | Handing imported clips to a Pydantic response model | ⚠ **Clamp to the schema's bounds inside `to_project` and COUNT what you changed — a value outside them is not a bad clip, it is a 500 with no message.** The import route builds `AnimaticFrame` / `AnimaticAudio` straight out of what `to_project` returns, and those cap a clip at TEN MINUTES (`le=600_000`) and forbid a negative start (`ge=0`). This app makes animatics; somebody else's timeline does not know that. ⚠ **The files that tripped it were ORDINARY** — a fifteen-minute interview take, a music bed across a whole reel, a record-in before the sequence starts — and every one reached the user as *Internal Server Error*, whole import lost, nothing naming the clip. ⚠ **It was NOT a `.prproj` bug**: the clamp is in `to_project`, so it had been true of the FCP7 XML and EDL readers since Phase 3 and nothing had caught it. Clamped and reported now (*“2 clips were longer than… and were shortened”*), and ⚠ **the length is taken from the ORIGINAL start before the start is clamped**, or a clip that began before the sequence gets STRETCHED instead of moved. `tests/interchange_check.py` §8d validates every clip `to_project` returns against the real models, **and proves the models would have rejected the unclamped values** — without that last line the section would pass just as happily on a build with no bounds at all. | PAKKA |
| **E51** | Putting two buttons side by side, ANYWHERE in the app | ⚠ **They are one pair and they must be one size — same height, same top edge, and a shared width floor so the short label is not a stub beside the long one.** Reported from a screenshot, and it is a REPEAT complaint: *“jab do buttun saath mai hai to size barabar hona chaiye”*. ⚠ **The cause is the same every time and it is not in the dialog** — `.btn.primary` in `base.css` carries a global `margin-top: 1.1rem` because it is normally the last control in a FORM. Dropped into a ROW it pushes the gold button DOWN, the ghost beside it stretches to the taller line, the two end up BOTTOM-aligned, and the ghost reads as a bigger control. Measured on the import dialog: identical 37.2px heights, tops 8.8px apart. ⚠ **`margin-top: 0` alone is not the fix** — a gold primary carries its own glow and heavier weight, so beside a ghost it still reads larger. Copy the `.export-modal-actions` rule whole: `inline-flex` + centred + `gap` + a fixed `height` + zeroed vertical padding + `margin-top: 0` + `box-sizing: border-box`, and a `min-width` floor. ⚠ **A width FLOOR, never `flex: 1`** — a primary's label grows (*“Add 12 clips to the timeline”*) and an equal share would blow the Cancel button up to match it. ⚠ **This has now been written nine times in CSS and never once here**, which is why it keeps coming back: check `.btn.primary` whenever you add a button row, and pin it — `tests/interchange_check.py` §12 guards this one, and the guard is proved to fail without it. | PAKKA |
| **E52** | Writing a reader for a format with no published spec | ⚠ **A HAND-WRITTEN FIXTURE PROVES ONLY THAT YOU CAN READ YOUR OWN FIXTURE. Get one real file before you believe a single green test.** The `.prproj` reader shipped with 43 passing checks and then found 167 clips in the user's first real Premiere project and could not place ONE of them. Three things were wrong, each fatal on its own, and all three were invisible to a fixture built from the same guess as the code: **(a) Premiere uses TWO kinds of reference** — `ObjectRef` (numeric `ObjectID`) and `ObjectURef` (GUID `ObjectUID`) — and the link from a track group to its TRACKS is the GUID kind, so following only the numeric one finds no tracks at all and drops to the flat route; **(b) the times are NESTED, not referenced** — `<VideoClipTrackItem><ClipTrackItem><TrackItem><End>` is one element with no refs in between, so a reader looking for direct children or following refs reads nothing; **(c) a `<Start>` of zero is simply not written** — 17 of the 167 had none, and requiring Start AND End together throws away every clip at the head of a track. Also learned: **there is no `<Sequence>` object at all** (a sequence IS a `VideoTrackGroup` + `AudioTrackGroup`), the sequence rate is `<FrameRate>` on the track group in ticks-per-frame, and **the same `<FrameRate>` tag carries 44100 and 48000 on audio streams**, so a rate must be sanity-checked before it is believed. ⚠ **The honest move when you cannot get a real file is to say the tests prove behaviour and not compatibility** — which §8c did say, and it was right to. Now BOTH shapes are pinned: the real one, and the ref-shaped one another Premiere version might write. ⚠ **Two more the real file taught, both cosmetic-looking and both real:** a Premiere TITLE carries a `<FilePath>` holding an internal NUMBER (`1196574294`), which became a “missing media” line naming a file nobody can find — a value with no separator and no extension is an id, not a path; and sound that is found but whose files did not arrive gets **no placeholder by design** (E45), so the summary reads “0 sounds on 0 rows” and looks like a file with no audio — it now says how many were found and could not be brought in. | PAKKA |
| **E53** | A picker that collects files the user will need ALL of | ⚠ **It must ADD, not replace — a browser file picker can only ever see inside ONE folder at a time.** Reported live: *“maine ek image select kiya to sirf wahi image aaya… kya ek ek kar ke choose karna hai?”* One Premiere project's media sat in `Images/`, `Videos/` and `Audio/`, and `setMedia(Array.from(e.target.files))` threw the previous folder away on every pick — silently, so it read as the import losing pictures. Now it merges, **de-duplicated by NAME** (which is what the server matches on, so the same file from two folders is one file), shows a running count, and offers **`webkitdirectory`** so a whole folder tree is one click. ⚠ **Filter to real media on the CLIENT** — a folder pick otherwise uploads the project file, every Premiere auto-save and any stray document, megabytes the user waits for so the server can refuse them one by one. ⚠ **And give a way back** (Clear): a list that only grows is a trap. | PAKKA |
| **E54** | Telling “the server said no” apart from “the server never answered” | ⚠ **Mark the transport failure with a FLAG on the error, not a sentence to match on.** Seen live: uvicorn runs with `--reload`, a source edit restarted it, and the `.prproj` experimental read died mid-flight. The dialog counted that as the offer having been taken and hid the button — leaving a network error and no way back to the control just pressed. A backend that never answered said NOTHING about the file, so the offer must survive it. `api.js`'s `request()` now sets `err.offline = true` and the caller reads the flag; matching on the message would break silently the first time somebody rewords a sentence written for a human. | PAKKA |
| **E55** | A browser upload that fails instantly, with ZERO bytes sent | ⚠ **READ THE NETWORK TAB BEFORE BLAMING THE SERVER: `0 B transferred` in 1–3ms means the request never left the machine, so it is not a size, a timeout, or a backend fault.** Two different causes were found behind the same *“Can't reach the server”* message, and the message itself sent the investigation the wrong way twice. **(1)** uvicorn runs `--reload`; editing a `.py` file restarted it mid-request and the retry only covered 1.4s against a 3–5s restart (fixed: doubling backoff, ~10s). **(2)** With the backend healthy the whole time, ONE attached file was blocked before a byte was sent — `net::ERR_ALPN_NEGOTIATION_FAILED`, 1ms, 0 bytes — by security software scanning the upload. ⚠ **ONE bad file kills the WHOLE multipart request**, so 26 files failed and 25 of them were innocent; renaming it changed nothing (content, not name) and it was not even referenced by the project. ⚠ **Bisect the attachment to find it** — one file at a time proved it in 26 requests — and tell the user to narrow the folder down, because *“check the backend”* is advice that cannot work here. ⚠ **And reproduce it in a REAL browser** (Playwright + `set_input_files` + `fetch`): `requests` and `TestClient` both sailed through the same 35MB — only Chrome sees the block. | PAKKA |
| **E56** | Reading a resolved clip back out of the project list — ANYWHERE | ⚠ **Look it up by `id`. `picture.index` is a position in the FILTERED array `sceneAt` was given, and every consumer holds the UNFILTERED one.** `AnimaticEditor` hands `sceneAt` its `shown` document — hidden picture rows removed — so the instant a row is switched off `shown.frames` is shorter, every index after it shifts down, and `frames[picture.index]` names a different clip. ⚠ **THIS WAS FIXED TWICE AND MISSED ONCE, because the third site fails SILENTLY.** `ProgramCanvas` (`framesById`) and `currentIndex` were both corrected when the monitor drew the wrong picture; `useMonitorVideo` was not, and there a wrong clip means the cue carries some other clip's id, `videoElsRef` has no element under it, the loop skips, and the <video> that IS on screen is **never told to play**. Nothing throws, nothing logs, the element is the right one and the texture is re-uploaded every frame — it just holds frame 0 forever. Reported after a Premiere import as *“video ka sirf ek thumbnail jaisa dikhta hai… pura clip mein ek image jaisa”*, with the images and the audio on the same timeline playing perfectly; the project had **six of its eight picture rows hidden** (`hidden_lanes`), which is saved state, so it survived every reload. ⚠ **A resolved picture always carries its clip's `id`** (`resolve()` spreads the clip first) and an id survives being filtered out of an array in a way an index cannot — so build a `framesById` map and never index. ⚠ **Pinned in `tests/monitor_video_check.py`, which drives a REAL H.264 file in Chromium and runs the same scene twice — once plain, once with a switched-off row ahead of the clip.** The plain case passed throughout the bug; only the hidden one catches it. ⚠ **And assert the movement as a DELTA** (`currentTime` ran on by more than a second), never as `currentTime > 1`: an uncued element keeps whatever time the previous case left it at, which reads as “playing” to any absolute test. | PAKKA |

| **E57** | A placeholder for media that did not arrive, on a row ABOVE the bottom one | ⚠ **It goes on the timeline, named — and it draws NOTHING. An opaque card on an upper row hides the whole film behind it.** E45 is still right where it was written (the BOTTOM row, where the frame would otherwise be empty and the gap invisible); one row up the clip is an OVERLAY, and blanking it black is not a marker, it is a lid. ⚠ **This is not an edge case, it is what EVERY Premiere import does.** A Title, a Graphic and an Adjustment Layer have no media file to attach and never will, so every one of them arrives unmatched — a real project brought in 44, three of them at FULL LENGTH over the cut, and the result previewed and exported as 68 seconds of black. ⚠ **And it did not read as “the picture is black”, it read as “the TEXT is broken”** (*“audio, image and video show but text not show”*): where the lettering had been there was now a black rectangle, so the user went looking for a text bug. Two separate faults wearing one symptom. ⚠ **INVISIBLE IS NOT OMITTED** — the clip keeps its place, its own name and its row, stays in `placeholders`, and `opacity` is an ordinary editable field, so anyone who wants to see the gap drags it back up. ⚠ **“Bottom” is the lowest row that HAS CLIPS, not row 0** — a sequence whose V1 is empty still lists V1, and counting lanes instead of clips puts the black screen straight back. ⚠ **And SAY IT**: the warning names titles and Graphics explicitly and points at the Text tool, because “attach the missing files” sends someone hunting for files that do not exist. ⚠ **THE LINE THAT SAID “NO IMPORT CARRIES LETTERING” WAS WRONG AND IS NOW E59.** It read: *not `.prproj`, and not the Final Cut Pro XML this app recommends instead… never tell a user their text will come across.* It was believed because only the clip's `<Name>` was ever looked at, and every Premiere title is named “Graphic”. **A `.prproj` carries the words, the font, the size and the position** — see E59. What this row still governs is a clip with genuinely nothing in it. ⚠ **And an Adjustment Layer is no longer imported at all**: it is an empty holder for colour effects no format carries, so the only thing it contributed was a full-length invisible clip the user had to find and delete. Pinned in `tests/interchange_check.py` §8e, which holds both halves at once — a fix that DROPPED the clips would pass “it is not black” and fail that section. | PAKKA |

| **E58** | Reading a clip's SOURCE WINDOW (its in point) out of a `.prproj` | ⚠ **`<InPoint>` IS NESTED, exactly like `<Start>`/`<End>` — and reading it as a direct child is a fault you CANNOT SEE on a timeline of whole takes.** Premiere writes `<AudioClip><Clip Version="18"><InPoint>`, a GRANDCHILD of the clip object; `_prproj_int` reads a direct child, so every clip answered None, `to_project` turned that into 0, and **every clip played its file from the beginning**. ⚠ **E52 CAUGHT THIS SHAPE FOR THE TIMES AND THE FIX WAS NOT CARRIED ACROSS TO THE IN POINT** — same file, same nesting, same function, one line apart. ⚠ **AND IT LOOKED PERFECT ON THE FIRST REAL IMPORT**: four video clips, four separate files, every in point genuinely 0 — the data confirmed “in_ms is 0” and that was read as evidence the reader was RIGHT. It is only visible when ONE FILE IS USED MORE THAN ONCE: the same project's voiceover was one mp3 razored into 23 pieces with the silences cut out, and all 23 restarted the recording (*“har clip audio ka starting hi play ho raha hai… pura audio sunai nahi de raha”*). ⚠ **SO TEST IT WITH A FILE USED TWICE.** A single clip proves nothing — its in point is 0 whether the reader found it or invented it. `tests/interchange_check.py` §8f uses three clips off one file at RISING in points with gaps between them, and there is no value a reader that found nothing could return that would pass. ⚠ **`MasterClip` carries no `<InPoint>`** (verified against a real project: only `VideoClip` and `AudioClip` do, both at the same depth), so searching descendants cannot pick up the bin's in point by mistake. ⚠ **And pin BOTH shapes** — §8c's flat `<Clip><InPoint>` still has to read, or fixing the real file breaks the one another Premiere version might write. | PAKKA |



| **E59** | Reading the LETTERING out of a `.prproj` — a Premiere title is not an empty clip | ⚠ **THE WORDS ARE IN THE FILE. THIS APP TOLD USERS FOR MONTHS THEY WERE NOT, AND A 40-CAPTION SCRIPT WAS RETYPED ON THAT ADVICE.** Every title, caption and lower third in a Premiere project is a clip **named “Graphic”** with no media file — so a reader that looks only at `<Name>` sees forty identical empty clips, makes forty invisible placeholders, and prints *“any LETTERING they held has to be typed again with the Text tool.”* ⚠ **LOOK ONE LEVEL IN: `<VideoFilterComponent><MatchName>AE.ADBE Text`**, reached from the track item through `ClipTrackItem → ComponentOwner → Components → VideoComponentChain`. The text is there TWICE and **which one you trust matters**: `<InstanceName>` is Premiere naming the layer after its own text (free to read, and STALE the moment somebody renames the layer), while the `Source Text` param's base64 `<StartKeyframeValue>` is the string itself. **Read the blob first, `<InstanceName>` only as a fallback**, and build the fixture with the two DISAGREEING or the test cannot tell which one you used. ⚠ **READ THE BLOB FOR ITS STRINGS AND NOTHING ELSE.** It is a FlatBuffer: every field is addressed through a vtable, so field N is at a different byte offset in every record — 83 real records were measured and no two agreed. What never moves is `<uint32 length><bytes><NUL>`; two of those appear in order, **[0] the font's PostScript name, [1] the text**. ⚠ **AND THE SCAN MUST STEP OVER A STRING IT ACCEPTS**, not advance one byte, or a caption's own bytes get re-read as another string's length. ⚠ **ONE GRAPHIC CAN HOLD SEVERAL TEXT LAYERS** — that is what an `AE.ADBE Graphic Group` is (a title with a subtitle). The first draft returned the FIRST text component and stopped; a real clip's group began with an EMPTY layer, so that clip imported as no text at all while its words sat in the very next component. **Read every layer at the shallowest level, and treat an empty one as “nothing here”, never as an answer.** ⚠ **STOP AT THAT LEVEL — DO NOT KEEP DESCENDING.** The MASTER clip behind a graphic carries a chain of its own holding the text it was first made with, and in the reference project **all 82 captions share one master clip**, so a deeper walk returns the same wrong sentence 82 times — a timeline that is full, plausible and completely wrong. ⚠ **THE FILL COLOUR IS NOT IN A `.prproj`.** Looked for in all three places it could be: no colour `<Param>` on the component (its eighteen are Source Text, Transform, Position, Scale, Horizontal Scale, Rotation, Opacity, Anchor Point, start, end, Parent W/H/Rotation); no float in 0..1, no `00 00 80 3F` and no `FF FF FF FF` in any of the 83 blobs; and the `<PremiereFilterPrivateData>` that would carry a serialised appearance is **EMPTY — a `BinaryHash` attribute and no body** (184 of 206 self-closing), because Premiere keeps that payload outside the project XML. **Use this app's default and SAY SO in the report** rather than inventing a white that might have been yellow. Same wall for a shape's `Appearance`, so a shape comes in at the right time and zero opacity. ⚠ **PREMIERE STORES A TEXT LAYER'S LEFT EDGE; THIS APP WANTS THE CENTRE.** The size comes from the plain-XML `Scale` (50% → 45px at 1080p, `PRPROJ_TEXT_SIZE_PER_SCALE = 0.9`) and the constant is **fitted, not guessed**: for a row of centred captions `left + half the line's width` must return to 0.5, and solving that over 78 real captions of 10–46 characters gives 0.4956 ± 0.013, where 0.85 and 0.95 give 0.4866 and 0.5046. Pinned in `tests/interchange_check.py` §8g. | PAKKA |

| **E60** | What an imported Premiere ROW becomes, and what the row is CALLED | ⚠ **A PREMIERE ROW IS NOT A PICTURE ROW.** In a real 8-row project only three held film: two held captions, two a drawn bar, one an Adjustment Layer. Turning every lane into a picture row is where *“audio, image and video show but text not show”* came from — and it also hands the client “this import needs 8 picture rows”, which draws empty rows above the cut, because **picture clips are addressed by NUMBER (`AnimaticFrame.track`) while everything else is addressed by `layer_id`**. ⚠ **SORT EVERY CLIP FIRST, ONCE, THEN RENUMBER**: text → an `AnimaticTextClip` on a text row, shape → an `AnimaticShape` on a shapes row, Adjustment Layer → nothing, picture → a frame; and only the lanes that still hold a picture get a track number, compacted from zero. ⚠ **“Lowest row” for the E57 opacity rule then means the lowest row that holds a PICTURE**, or a caption row under the film claims it and every missing file above goes invisible. ⚠ **AN IMPORT MAY NEVER WRITE TO `CAPTION_LAYER_ID`.** That row belongs to ✨ Auto captions, which **replaces everything on it on every run** — an imported title parked there is destroyed by a transcription the user paid for, silently. Mint `_import_text_N` and lock it on both sides (server never emits it; the client filters it again). ⚠ **AND A CLIP THAT RESOLVED TO A FILE IS ALWAYS A PICTURE**, whatever it is called — somebody's footage really can be named `Adjustment Layer.mov`, and a filename is not permission to delete a shot. ⚠ **NAME THE ROW BY WHAT IS ON IT, NOT BY THE FILE.** Imported rows were named after the sequence — eleven rows all reading “8_MCP_Model Context Prot…”, which distinguishes nothing and repeats the project title; worse, “…audio” appeared on rows of pictures. Use the same words and the same numbering a hand-added row gets (`Video`, `Images`, `Text`, `Text 2`, `Shapes`, `Audio`), skipping names already on the timeline, and let the server say which picture rows are stills (`video_lane_kinds`). ⚠ **AND DO NOT ASSERT A COUNT WHERE A SET WILL DO**: the source check for “every new row is seated” read `count(...) == 2` and went red the day text and shape rows arrived, for a reason that had nothing to do with what it tests. Pinned in `tests/interchange_check.py` §8h and §12. | PAKKA |
## F · Waiting, progress and drafts

`BreakdownProgress.jsx`, `ScriptToStoryboard.jsx`, `StoryboardLibrary.jsx`

| # | Scenario | The rule | Status |
|---|---|---|---|
| **F1** | Two chained calls, one wait | **ONE ring, mounted once.** Two elements or two `key`s are two instances and the second climbs from zero — reported as "a bar finished and a new one started". A non-final call hands off **where it stands**, with no sprint to a ceiling. | PAKKA |
| **F2** | A long wait | **The fill decelerates and never parks.** Rate is set by the distance still to go, so it is still creeping a minute in; it stops at 96, and **paints 100 for 220ms before handing over** or React never renders it. ⚠ 99% is the worst number to park on — it reads as stuck, not working. | PAKKA |
| **F3** | An unfinished board from an earlier session | **Offered, never taken.** A banner carrying its SHOT COUNT (which is what makes a stale board recognisable before opening it), with Resume and a Discard that asks first. ⚠ A first-mount-wins flag does NOT work — StrictMode mounts twice in dev. | PAKKA |
| **F4** | Anything the user has EDITED BY HAND | **It must survive a refresh.** ⚠ The test is not "can we rebuild it" but **"does rebuilding give back the same thing"** — a concept re-generated from the same brief is a *different film*, so the card was unrecoverable and every hand edit with it. *Found after four rounds of testing in one afternoon: the script box was saved and the concept card was not.* It rides on the SAME draft row as the text it came from — one draft, one row — and `null` clears it on approve and Start over. Compare **by value** before saving; the card is a new object every keystroke. ⚠ **Restoring it must NOT reopen the screen — see F5.** ~~Offer it on the form~~ was tried and `workflow_mount_check.py` proved that unreachable (the only route to the form is "New storyboard", which clears the concept on the way past); ~~reopen it on every fresh page load~~ was the fix for that and was worse. The offer belongs on the **library**, which is the screen a cold start actually reaches. | PAKKA |
| **F5** | Opening a workflow with unfinished work saved | **The front door is the front door. NOTHING auto-opens a deeper screen — unfinished work waits in the LIBRARY as a row, and the user chooses.** *"jab user open kar raha hai script to storyboard to first page khulna chahiye, nhi ki ye page."* The concept card was reopening itself on every fresh page load, so the workflow opened on two different screens depending on how old the tab was. ⚠ **AND CHECK THAT THE SAVED THING IS STILL ALIVE.** Nothing ever cleared a SPENT concept — `api.clearScriptDraft` was written and never called from anywhere — so the card being reopened was usually the plan for a film already generated, exported and forgotten weeks earlier. A draft is now dropped the moment it becomes the next stage (`finishBreakdown` → `setConcept(null)`), or the library grows a second row for the same film. ⚠ A row that offers work is only honest if discarding it clears the **server** slot too, or it is back on the next load. This is the SAME rule as F3, learned twice; F3 was fixed and the concept card kept overriding it. | PAKKA |
| **F6** | A "still loading / still slow" warning | **DISARM THE TIMER WHEN THE REQUEST FINISHES — unsetting the flag is not the same thing.** The library's 10-second banner was cleared only in the effect's CLEANUP (which runs on unmount, not on the request settling); the `finally` merely set `slow` back to false, which does nothing to a timer that has not fired yet. A list that answered in 300ms left a live timer behind, and ten seconds later — on a screen already showing every board — up came *"Still loading your storyboards … usually a database it needs (MongoDB) being unreachable"*, and it stayed. *"mai bar bar ye notice dekh rah hun."* ⚠ **The banner ACCUSED the database and the database was innocent** — measured while chasing it: Mongo 0.01s, the size walk 0.03s. A warning that fires after the thing it warns about has SUCCEEDED teaches the user to ignore the banner on the day it is telling the truth. ⚠ A static read of the file cannot see a timer fire — pinned in Chromium, `workflow_mount_check.py` [8], and the check was **proved to fail on the old code first**. Compare `BreakdownProgress.jsx`, which guards its own slow timer on `doneRef.current` and got this right. | PAKKA |

---

## G · Working on this repo

| # | Scenario | The rule | Status |
|---|---|---|---|
| **G1** | Patching any file | **Build the new bytes, write a temp file, rename over the target.** ⚠ Never open the target for writing before the new content exists — that destroyed ~442 lines of un-committed `AGENTS.md` once. | PAKKA (convention) |
| **G2** | Finishing a change | **Do not run the Playwright browser suite unless asked.** Run the relevant `tests/*_check.py` instead. | PAKKA (convention) |
| **G3** | Reporting a prompt change | **A prompt is a request, not a guarantee.** Say GUZARISH, and say that only a live run can confirm it. Never write "fixed" for a prompt-only change. | PAKKA (convention) |
| **G4** | Reaching for a library | **`client/package.json` has exactly two runtime deps and that is a decision.** Every widget here is hand-written. | PAKKA |
| **G5** | A file the user just restored by hand | **Ask before `git checkout` / overwrite.** | PAKKA (convention) |
| **G6** | Adding state to a hook's dependency array | ⚠ **Declare the `useState` ABOVE the hook that lists it.** A dependency array is evaluated DURING RENDER and `const` is not hoisted, so a state declared further down throws `Cannot access 'x' before initialization` on the first render — **a white page.** *Found by shipping one.* | PAKKA |
| **G9** | Arming a one-shot effect from an async handler | ⚠ **Use state, not a ref.** A ref does not re-render, so arming it AFTER the value the effect keys on has already settled means the effect ran once against a null ref and never runs again — the work silently never happens. *Shipped and caught in a browser; no build and no source check could see it.* | PAKKA |
| ~~**G8**~~ | Every reply, while the user is testing | ⚠ **Carry the WHOLE open list in the sign-off, not just this turn's leftovers.** Asked for outright: *"jo jo issue and bug mil raha hai sab mujhe REMAINING mein batana."* A bug reported once and then dropped from the summary reads as fixed. The table above is the source — read it before writing the sign-off, and add to it the moment something new is found. | ~~PAKKA~~ — **superseded by G10** |
| **G10** | Every reply, while the user is testing — **replaces G8** | ⚠ **REMAINING is THIS TURN's leftovers, kept short. The whole open list lives in the "Still open" table below and is handed over WHEN ASKED, not every time.** G8 said the opposite and was right at the time — a bug dropped from the sign-off reads as fixed. It was overruled by the person reading it: *"remaining mai pura nhi do, pahle jaise dete the waise do — ye bahut jayda ho ja raha ek baar dekhne mai. tum rulebook mai likh ke rakho, jab mai mangunga to dena."* ⚠ **THE OBLIGATION DID NOT GO AWAY, IT MOVED**: anything found and not fixed still gets a row in "Still open" the moment it is found, or the sign-off becomes the only record and G8's fault comes back through the other door. | PAKKA (convention) |
| **G11** | Starting the app to work on ONE page | ⚠ **Three pages, three ports, but ONLY ONE API server (8000) - ever.** `.vscode/tasks.json` now starts each page on its own Vite port (workflow **5173**, admin **5174**, marketing **5175**) so stopping one leaves the other two up, and because the browser scopes the signed-in session PER PORT you can be an admin on 5174 and an ordinary user on 5173 at once, with 5175 permanently signed out - which is the only state the marketing page is ever seen in. ⚠ **The backend is NOT duplicated per page, and must not be**: `server/main.py`'s startup sweep closes out every job it finds QUEUED, so a second server booting would kill the jobs the first one is in the middle of running. The shared-server task is port-guarded and idempotent for exactly this reason. ⚠ **`--strictPort` on every client task is load-bearing, not tidiness** - without it a busy 5173 makes Vite silently fall to **5174**, which is the admin panel's address, and two pages then fight over one port with no error anywhere. Stop with the matching STOP task (`.vscode/stop-port.ps1`, kills by PORT, never by process name, so nothing else in node or python on the machine can be hit). | PAKKA |
| **G12** | Patching a source file from a script | ⚠ **Match an EXACT string, or walk LINES. Never a multi-line regex — and never `re.S`.** *Paid for in this repo, not read somewhere*: a pattern meant to replace two tab entries in `AdminPanel.jsx` began `[ ]*//.*NEXT TO BRAND` under `re.DOTALL`, so `.*` ran from the FIRST comment in the file down to the anchor and **took the header, every import and six of the nine tabs with it** — the file went 5,360 → 3,143 characters in one silent, successful-looking write. ⚠ **G1 DID NOT CATCH THIS AND CANNOT.** Temp-file-then-rename protects against a half-written file; it happily renames a *complete* file with the wrong contents into place. G1 is about the WRITE, this is about the MATCH. ⚠ **ASSERT THE COUNT, NOT THE PRESENCE**: `s.count(old) == 1` before replacing, and after the edit assert that everything which must have SURVIVED is still there — every tab id, every import, every render branch. A patch that only checks what it added cannot see what it removed. ⚠ **AND THE RECOVERY IS `git show HEAD:<path>`, NOT `git checkout`** (G5): rebuild from the committed text, re-apply the edit, then `git diff` and read it — the diff must contain the intended change and nothing else. That is what proved no uncommitted work had been lost. | PAKKA |
| **G13** | Writing a `tests/*_check.py` that drives the real API | ⚠ **Pin EVERY store path AND `API_OUTPUT_DIR` into a temp directory, and set them BEFORE the first `server` import**, because `server/config.py` reads the environment at import time. ⚠ **Getting a variable's NAME wrong is silent** — `config.py` falls back to its default and the test writes into the developer's own workspace. Paid for twice in one afternoon: `.local_usage.json` defaults to the **git-tracked** repo root, so the first run of `interchange_check.py` spent two of the developer's monthly projects and left two fake accounts in a tracked file; and `OUTPUT_DIR` is really `API_OUTPUT_DIR`, so eight real project folders were written into `output/` before an assertion happened to look for one of them on disk. ⚠ **The failure is not a crash — it is a green suite that quietly edits the repo**, so after a new API test check `git status` AND `output/`, not just the tally. ⚠ **An assertion that reads the disk is what catches this**; one that only reads the HTTP response never will. | PAKKA (convention) |
| **G14** | A test that re-runs a guard against a deliberately broken copy | ⚠ **Each row must carry the SOURCE it guards, not share one.** `interchange_check.py` §12 hard-coded `apply_src` as the good source for every guard in the loop, so the first guard written about a different file read `False` on correct code and reported ITSELF broken — a red line pointing at the wrong thing, which is worse than no line. The loop now takes `(name, good, broken, guard)`. ⚠ **And the guard is only worth writing if the fault it catches would ACT NORMAL** — e.g. `onClick={readFile}` where `readFile(experimental)` is expected: React passes the click event, an event object is truthy, and every ordinary read silently asks the server to guess. | PAKKA (convention) |
| **G7** | Any change to a screen | ⚠ **`npm run build` passing is not evidence that the screen renders**, and neither is a `tests/*_check.py` — those read the file as TEXT. esbuild never evaluates the module. Run `tests/workflow_mount_check.py` (starts Vite itself, no backend, ~1 min) after touching a workflow component, and **never report a UI change as working on a green build alone.** | PAKKA |

---

## Still open — known, reported, NOT fixed

⚠ Carried over from the user's own testing list. Marked ✅ where re-verified in
the repo, and left unmarked where it is the user's report taken at face value.

| Area | What is wrong | Verified |
|---|---|---|
| Concept | A typo like "virl" for "viral" silently switches off A5, and nothing on screen says the hook rule did not fire. A decision on how to surface it is pending. | ✅ keyword-only trigger |
| Props step | `StoryboardAssets.jsx` has no `N shots` badge — the cast step (E5) has one; props does not. | ✅ |
| Concept card | ⚠ **E1, SITE FIVE.** The key-scene rows in `ScriptToStoryboard.jsx` are `<input>` — one line, no growth — and these are *the lines that become the panels*. A 150-character scene scrolls off the right edge and cannot be read without dragging inside the box. Seen on run 3, scene 5. | ✅ |
| Concept | No rule says what a GOOD call-to-action looks like; the prompt only says to use the CTA the creator asked for. | ✅ |
| Board layout | The fourth card sits alone in a row. | — |
| Board layout | Small cards stretch. ⚠ **Half-fixed by accident**: pinning the draw button to the tile foot moved the empty space above the button instead of below it. The stretch remains. | — |
| Board | Draft name changes on its own; thumbnail reverts; cast description does not save. | — |
| Board | "Ganesha idol" name ties between a cast entry and a prop. | — |
| Board | Shot 5 has no speaker; the PDF slug line is wrong. | — |
| Script writer | ⚠ **A PROP CAN BE IN TWO PLACES AT ONCE.** The first full run put the Ganesh idol in the *living room* (scenes 2, 5) and the *puja room* (scenes 1, 3, 4) — carried into one and standing in the other. `location` drives the background of every image, so the same idol would have been drawn in two rooms. **Nothing in the prompt says a thing stays where it was put.** | ✅ |
| Breakdown | All 15 shots came back `movement: static`. Not a rule breach — the rule says most shots should be — but the same rule asks for a slow push-in on *"a realisation or a reaction landing"*, and *"Ananya's eyes light up"* is exactly that. Under-applied, not absent. | ✅ |
| Cast step | ⚠ **A REFERENCE CAME BACK WITH TWO PEOPLE IN IT** — MOTHER rendered as a mirrored pair. A reference is injected into every panel that character appears in, so one bad sheet corrupts **8 panels**, and the user is the only thing standing between it and the spend. `qa.py` audits the finished board for magenta placeholders, greyscale and aspect — **it cannot see a doubled figure**, and nothing checks a reference BEFORE it is used. | ✅ |
| Concept | **A3 and A4 regress on a freshly generated card** — see the live-run log in section A. Diagnosed, not fixed: A2 ("every beat gets a scene") beats A4 because the arc's own first beat is *Ghar ki taiyari*, and A3 has no room because six beats fill six scenes. The fix is to reach the STORY DIRECTION, not to add a seventh paragraph. | ✅ |
| Board | ⚠ **THE FILM'S SUBJECT DRIFTED ACROSS 15 PANELS.** The Ganesh idol is drawn differently in shots 3, 5, 10 and 13 — every CHARACTER is consistent because each got a reference, and the idol got none. ~~The breakdown returned an empty asset list.~~ ⚠ **THAT DIAGNOSIS WAS WRONG, and only the props field (E8) showed it**: the breakdown HAD listed them — shot 1 carries *"Puja Room, marigold garlands, oil lamps"*, shot 3 *"Ganesh idol, Living Room"*. So the props step was reachable all along and no reference was ever drawn there. **Why is not established**: nothing on the review step showed that assets existed or that they were worth a reference, so the step could be walked past without knowing what was being skipped. The props step still has no `N shots` badge and no equivalent of the cast page's warning. | ✅ |
| Review step | ⚠ **CHANGING `location` DOES NOT REWRITE THE IMAGE PROMPT.** Both reach the panel, and the prompt text wins: shot 11's location was set to Puja Room while its description still read "in the living room", and the panel came back a living room. The field looks like the fix and is only half of it. | ✅ |
| Cast step | A vague reference description lets the outfit drift even WITH a reference — ANANYA's says "a colorful traditional lehenga", and she wears three different outfits across the 15 panels. The colours have to be named. | ✅ |
| Cast / props steps | ⚠ **THE SAVED-REFERENCE MAP IS KEYED TWO DIFFERENT WAYS.** A draft's map is written by `saveRefFields`, so its keys are already `refKey`'d (trimmed, lower-cased); a BOARD's map is whatever `StoryboardCast` sent up, which is the character's own name — `ANANYA`. Both steps look themselves up lower-cased, so a board's map matched **nothing**: four empty cards and a "(skip refs)" button over references already paid for and sitting on the server. Normalised inside `restoreSavedRefs` now, which is the one place both paths pass through — but the two producers still disagree. | ✅ |
| Board | ⚠ **A PROP REFERENCE HOLDS IN A SCENE AND SLIPS IN A CLOSE-UP.** First board drawn WITH a `Ganesh idol` reference: shots 3, 6, 10, 11, 12, 13 and 14 all show the same small clay idol — the reference plainly worked. **Shot 5, the hero close-up where the idol fills the frame, came back a different, grander idol** (taller jewelled crown, different skin tone). Two things stack: a frame filled by the object gives the model the most room to reinterpret, and that shot's own description says *"majestically … serene and grand"* — adjectives that ASK for a grander object than the reference. ⚠ So the shot that most needs the reference is the one it holds worst, and nothing warns. | ✅ |
| Board | Reported the other way round the same day and worth keeping: **a prop/background reference OVERRULES the prompt text.** Shot 11's description still read *"in the living room"* while its props named `Puja Room`, and the panel came back a puja room. Useful — but it means the two can disagree with no sign on screen of which one won. | ✅ |
| Tests | `tests/plan_script_check.py` fails with **403 on POST /plans** for a new account. Confirmed NOT ours by running it on a stashed clean tree. Nobody has looked at it. | — |
| Tests | `tests/profile_check.py` fails its last line — *"only the real account remains (got 324)"*. It counts the Mongo users collection and expects 1; **324 throwaway accounts from earlier test runs were never cleaned up.** Environment residue, not code — but it makes the suite read red, which is how a real failure gets ignored. | ✅ |

---

**Last updated:** 2026-08-30 — **E38**: a fixed `max-width` is only half a responsive page. Explore was pinned at 1560px, so a 27" monitor got a narrow column with 700px of dead ground either side and two picture cards blown up to 1000px inside it. Give the cap a RANGE with the old number as its FLOOR (nothing under a laptop width moves), keep a ceiling, and cap the WALL separately from the page — spelling its left edge out, because every row around it is centred.

**Previously:** 2026-08-30 — **E34**: a rail must carry a row for the page it is standing on, marked active — the public rail never named Explore, so nothing was highlighted and Home was a one-way door. ⚠ And **adding a MODE to a shared component rots every "string X is not in this file" assertion**: three went stale at once. Re-assert the rule as *behind the guard*, not *absent from the file*, and prove the new check fails without the guard.

**Previously:** 2026-08-30 — **E33**: a rail beside a many-row page needs ONE wrapper and a two-cell grid, never per-row placement plus `grid-row: 1 / -1` — which **does nothing without explicit rows** and collapsed the page into a single 100dvh row, opening on a blank screen. ⚠ The thing that moved was the header's HEIGHT, not its top, so `nav.top < 160` would have passed on the broken page. ⚠ And **a source check is not a layout check**: `npm run build` and thirty green source assertions coexisted with the bug, two of them asserting the declarations that caused it. New `tests/explore_layout_check.py` measures the geometry and reproduces the blank screen to prove it can still fail.

**Previously:** 2026-08-30 — **E32**: a screen that needs a rail the app already has gets a MODE on the existing component, never a look-alike. Paid for in one round trip — the public page's hand-built rail came back the same day missing the mark, the name and the collapse toggle, all three of which were free from the real one. ⚠ And watch what the mode HIDES: dropping the avatar dropped the `margin-left: auto` that was holding the brand row's right edge.

**Previously:** 2026-08-30 — **E31** and **G12**. **E31**: two panel tabs that edit ONE screen become one tab with sections — Banners and Showcase are now sections of an **Explore** tab, asked for outright, and the old strip's own comment had already admitted they were the same page. **G12** is paid for in this session: a multi-line regex under `re.S`, aimed at two lines of `AdminPanel.jsx`, ate the header, the imports and six of nine tabs. **Match an exact string or walk lines; assert the count before, and assert the survivors after.** G1 cannot catch this — it protects the write, not the match. Also this session: the public Explore page gained a left rail, every row a sign-in gate carrying its workflow id.

**Previously:** 2026-08-30 — section E gained **E30**: two controls side by side, one changing the THING and one only its COVER, must say which is which ON the control. *"maine admin panel se image daala hai — magar video khul raha hai, image nahi"* — nothing was broken (proved end to end: each item serves its own file, byte for byte); "Replace still" had set the cover and the item was still a video. Second report in a row from the same row of buttons, after **E28**.

**Previously:** 2026-08-29 — section E gained **E28** and **E29**. **E28**: a wall that accepts video **makes its own thumbnail** — reported as *"no thumbnail show in my upload video"*, and the cause was a wrong fact written into a comment (`ffprobe` inspects, **ffmpeg extracts**, and the repo already shipped ffmpeg), so a capability that existed was designed around. Black frames are still refused. **E29**: a page whose job is to be shown to people **gets an address** — `?explore`, copied from `?admin`, dropped on sign-in. Both pinned in `tests/showcase_check.py` (§11 with real ffmpeg-built clips, §12 on the route).

**Previously:** 2026-08-29 — section G gained **G11**: the VS Code tasks were split into THREE independent start/stop pairs, one per page (workflow 5173, admin 5174, marketing 5175), over ONE shared API server — asked for directly: *"divide my task.json in three part for start and stop … which makes it easy for me to handle."* ⚠ The reason the backend is not split with them is in the rule: a second server on boot closes out the first one's in-flight jobs. New `.vscode/stop-port.ps1` (kills by port, pure ASCII like `start-app.ps1`, and for the same cp1252 reason). Verified end-to-end: Vite bound to 5174, answered HTTP 200, and the STOP task freed the port.

**Previously:** 2026-08-29 — section E gained **E10** and **E11** from the
Explore page: a second dashboard was built, and the six workflow groups were
moved out of `Home.jsx` into `dashboard_feed.js` rather than copied. Pinned by
the new Chromium `tests/explore_mount_check.py` (114 checks, both themes, the rail, the admin
screens and the whole signed-in shell included). **E12** followed the same day: the app now OPENS on
Explore, from one named constant rather than from five separate handlers. Then
**E13** (a slide may not resize the page it sits on) and **G10**, which strikes
**G8**: the sign-off carries this turn's leftovers, and the full open list is
handed over on request instead of every time. Then the offer pop-up brought
**E14** (how an interruption has to behave), **E15** (its words live in the admin
panel) and **E16** (gold means the action, never navigation). Then the collapsed
rail grew names under its icons: **E17** (short names for every workflow, hidden
ones included) and **E18** (measure an ellipsis with a `Range`, not with
`scrollWidth`). Then the billboards themselves moved into the panel: **E20**
(customer-facing copy belongs to the admin, with today's behaviour as the
fallback), **E21** (how to add an uploaded picture safely) and **E22** (don't
complain about a field nobody has touched). **E23** closed the masonry hole
the wall had been reported for twice.

**Previously:** 2026-08-28 — section A gained A1–A4, then A10–A11,
across three live runs of one Hinglish Ganesh Chaturthi brief. ⚠ **No run
has yet had all six right**: run 2 held A1–A4 and dropped the visarjan; run
3 holds A1/A2/A10/A11 and regressed on A3 and A4. Then two things the
testing itself exposed, both now **PAKKA**: **E6** (a list whose order is
its meaning needs ↑/↓, not just add and delete) and **F4** (the concept
card survives a refresh, because re-generating it returns a different
film — which had been costing a whole re-test every time anything shipped).

⚠ Still unlooked-at at the user's request: the close-up-heavy framing,
deferred until the review step can be read.

⚠ **`tests/workflow_mount_check.py` has now caught FOUR faults** that a green
build and a full green Python suite both shipped: the white page, an offer link
on a screen the only route to which destroys what it offers, a props field that
ate the space inside "Ganesh idol", and a signature stamp armed on a ref that
never fired. Run it after touching a workflow component.

⚠ **G6 and G7 were both bought with a white page.** The concept-draft
change built clean, passed every Python check, and rendered nothing.
`tests/workflow_mount_check.py` exists now and caught a SECOND fault the
same afternoon — an offer link on a screen the only route to which
destroys the thing being offered.
