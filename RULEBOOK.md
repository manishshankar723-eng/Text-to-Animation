# RULEBOOK.md — every rule this app learned from a live test

> **Read the section for the area you are about to change, BEFORE you change it.**
> Then add or update a row when your fix lands. This file is how a fix made in
> August survives an edit made in October.

`AGENTS.md` is still the source of truth for *what the project is* and *what
happened when*. But its Work Log is 22,000 lines of chronology, ordered by date,
and **nobody reads 22,000 lines before touching one prompt string**. So the same
box got un-grown four times and the same end card was fixed twice.

This file is the other index: **ordered by SCENARIO, not by date.** Small enough
to read in full. If you are about to edit the concept prompt, section A tells you
in thirty seconds what already lives there and must not be lost.

---

## Ise kaise padhein (plain Hinglish)

Ye file ek **niyam ki kitaab** hai. Har baar jab testing mein koi problem milti
hai aur hum use theek karte hain, ek nayi line yahan add hoti hai.

Agla agent (Claude / Codex / Gemini) kaam shuru karne se pehle apne area ki list
padhta hai — isliye purana fix dobara nahi tootta.

Har niyam par ek **status** likha hai:

| Status | Matlab |
|---|---|
| **PAKKA** | Code mein enforce hai (Python/JS). Silently tut nahi sakta — test fail ho jayega. |
| **GUZARISH** | Sirf AI ko di gayi request (prompt rule). AI maan bhi sakta hai, nahi bhi. **Live run se hi pata chalega.** |
| **OPEN** | Problem pata hai, abhi theek nahi hui. |

⚠ **PAKKA aur GUZARISH ka farak sabse zaroori hai.** Agar kisi cheez ka status
GUZARISH hai, to "ho gaya" mat samajhiye jab tak ek asli board ban kar na dekh lein.

---

## Protocol for agents (required)

1. **Before you edit** — read the section covering the file you are opening.
   A rule here is a decision that was already paid for once, in a real test.
2. **While you edit** — do not delete a rule to make room for yours. If two
   rules genuinely conflict, say so in your reply and let the user choose.
3. **When your fix lands** — add a row. New rules get the next free ID in that
   section. Write what the user actually saw, not what the code does.
4. **When a rule is superseded** — strike it (`~~R~~`) and add the replacement
   with a note saying which one it replaces. **Never delete history**; a rule
   that came back is the most useful thing in this file.
5. **Be honest about status.** A prompt change is GUZARISH until a live run
   says otherwise. Marking it PAKKA is how a fix gets forgotten twice.

---

## A · Concept — a brief or an idea becomes the card the user approves

`script_concept.py` · pinned by `tests/script_concept_check.py`

> **Live-run log for this section.** A GUZARISH rule earns confidence one real board at a time, and one pass is evidence, not proof — record each run here rather than promoting the rule.
>
> - **2026-08-28, Hinglish Ganesh Chaturthi brief, 40s, re-run from a file.** A1 ✅ (arc ended on *Bhagwan ka aashirwad* and so did scene 6) · A2 ✅ (all five beats had a scene) · A3 ✅ (scene 6 was Ganesh ji alone, filling the frame — the shot that was missing entirely the run before) · A4 ✅ (opened on a child's eyes lighting up, not on laying out marigolds). ⚠ **And the same run is where A10/A11 came from:** the visarjan the brief asked for had gone. Close-up-heavy framing (4 of 6 scenes) noted and deferred to the review step by the user.
> - **2026-08-28, run 3, the same brief again after A10/A11 were added.** A1 ✅ · A2 ✅ (six beats, six scenes, one to one) · **A10 ✅ and A11 ✅ — the visarjan is back, it is scene 5 of 6, and the resolution lands after it, with *"muskaan aur thodi udaasi ke saath"* carrying the feeling.** ⚠ **But A3 ❌ and A4 ❌ REGRESSED, and both had passed in run 2.** Ganesh ji is carried, prayed to, sung around and immersed — and never once framed alone; the hero shot run 2 ended on is simply gone. And scene 1 is *"ek bacche ke haath rangoli bana rahe hain … phool aur diye sajaye ja rahe hain"* — the decorating shot from run 1, back verbatim, under a first beat that is literally *Ghar ki taiyari*.
> - ⚠ **NO SINGLE RUN HAS YET HAD ALL SIX RIGHT.** Run 2 had the hero shot and the opening and lost the visarjan; run 3 has the visarjan and lost the hero shot and the opening. Six rules are now competing for the same attention in one instruction, and **adding a seventh is not obviously the fix** — the next move is more likely to be making A3 and A4 harder to skip (a named slot in the scene list, or a deterministic check) than another paragraph of prose.

| # | Scenario | The rule | Status |
|---|---|---|---|
| **A1** | Writing `key_scenes` | **The scene list has to END on the last beat of `story_direction`.** *Found: a Ganesh Chaturthi arc ending "-> Aashirwad bana rehta hai" whose six scenes stopped at the visarjan — the film would have ended on the loss.* Six beats + six scenes looks like a match, which is exactly what hid it. `concept_to_brief()` also states the closing beat outright when the scenes drop it. | **PAKKA** (handoff) + **GUZARISH** (card) |
| **A2** | Writing `key_scenes` | **Every step of the arrow chain gets at least one scene.** A direction and a scene list that disagree describe two different films. | GUZARISH |
| **A3** | Writing `key_scenes` | **Whatever the film is ABOUT is seen alone, at least once** — not held, not touched, not glimpsed past a shoulder. *Found: a film entirely about an idol that never once framed it.* | GUZARISH |
| **A4** | Runtime ≤ 90s, no feed words | **The opening moves, and nothing else does.** A 40-second film has no room for a run-up, so scene 1 is already the film. ⚠ **This is NOT the hook rule** — the rest of the arc keeps its order and still lands its ending. | GUZARISH (trigger is PAKKA) |
| **A5** | Brief says reel / shorts / viral / tiktok / instagram | **Hook first — strongest image in scene 1, reorder the film if you must.** ⚠ The trigger is what the user **typed**, never the 9:16 they clicked, and "short film" is deliberately excluded. Overrules A4. | GUZARISH (trigger is PAKKA) |
| **A6** | The concept step fails | **It must BLOCK, and must not fall through to breaking the raw brief down as a script.** ⚠ Opposite of `/script-intake`, which fails OPEN on purpose — a dead classifier must not stop a board, a dead concept step must. | PAKKA |
| **A7** | An approved concept | **Goes through `plan_agent.write_script()`, never straight to shots.** The review step and every "FROM YOUR SCRIPT · LINE 12" need a real script to point at. | PAKKA |
| **A8** | Any generated field | **Same language and same script as the user wrote in.** Hinglish in Latin letters stays Hinglish in Latin letters — no switch to Devanagari, no switch to English. | GUZARISH |
| **A9** | Approving | **The length on the card travels all the way.** `concept_seconds()` → the writer → `break_down_script()`. *Found: a 30s concept boarded as 29 shots / 1m 04s, every extra panel paid for.* | PAKKA |
| **A10** | Any beat the user's own material named | **An event they named is as fixed as a character they named.** A farewell, a first day, a visarjan, an unboxing — it gets a beat in the direction AND a scene of its own. Shorten the film by tightening scenes, **never by deleting one of theirs.** ⚠ Dropping a beat does not feel like contradicting the user, it feels like *tightening* — which is exactly what makes it the easy mistake. | GUZARISH |
| **A11** | A story with a difficult moment in it | **Do not smooth the hard beat away.** The goodbye, the letting go, the thing that costs something — that is where the film's feeling comes from, and a scene list of only pleasant moments has nothing in it to feel. Keep it, put it late, and let the resolution land **after** it rather than instead of it. *Found: a re-run that kept anticipation, arrival, devotion, joy and blessing — and quietly dropped the visarjan.* | GUZARISH |

---

## B · Breakdown — a script becomes shots

`script_breakdown.py` · pinned by `tests/shot_density_check.py`

| # | Scenario | The rule | Status |
|---|---|---|---|
| **B1** | Reading the script | **Know what you are reading.** `_is_beat_script()` routes it: a script from `write_script()` is *one line = one shot, merge repeats*; pasted prose keeps *split into beats*. | PAKKA (routing) |
| **B2** | Over the runtime budget | **Argue, never clamp.** The model is told to MERGE; truncating to a budget deletes the END of the story. The review chip goes amber when it did not. | PAKKA (chip) + GUZARISH (merge) |
| **B3** | A `(V.O.)` or speech-only line | **Never gets a panel of its own.** It attaches to a shot that already exists. `_SPEECH_RULE`. | GUZARISH |
| **B4** | Three shots framed the same way | **Ask for a different framing — do NOT merge them.** ⚠ They are different beats and merging deletes story. `_VARIETY_RULE` says this outright. | GUZARISH |

---

## C · Image prompts — what the model is allowed to draw

`gemini_client.py`

| # | Scenario | The rule | Status |
|---|---|---|---|
| **C1** | Any panel | **No lettering, ever.** `_SINGLE_FRAME_RULE` forbids the image model all writing — asking for it returns gibberish, and it is paid for. | GUZARISH |
| **C2** | An end card / title card | **Its WORDS go to `dialogue` under `ON SCREEN`, never into `description`** — and the description may not name the subject either (no *"graphic card"*, no *"title card"*, and the word *"text"* may not appear at all). ⚠ **Enforced in Python**: `strip_lettering()` runs where the breakdown writes the sentence AND on the one line every panel passes through, so an older board cannot slip one past. *Fixed twice — the first rule banned the phrasings and never touched the subject.* | **PAKKA** |
| **C3** | `movement` / `duration_seconds` | **Never reach an image prompt.** A still frame cannot show a move or a length; asking gets motion blur and arrows drawn into the panel. They travel like `dialogue` does. | PAKKA |

---

## D · Money, locale and brand

`market.py`, `brand.py`, `server/branding.py`

| # | Scenario | The rule | Status |
|---|---|---|---|
| **D1** | The board form | **Ask the LANGUAGE only.** Country, currency and units are derived from it, from the account, or from the script. ⚠ **English is deliberately NOT in the language→country table** — mapping it to the US is how an Indian creator's promo got priced in dollars. Spanish and Arabic are absent for the same reason. | PAKKA |
| **D2** | Market unknown | **No legible price and no currency symbol anywhere.** A wrong `$` is worse than none. `_LANGUAGE_ONLY_RULE`. | GUZARISH |
| **D3** | A brand logo | **Composited, never generated.** The model draws a flat magenta placeholder and `brand.stamp()` pastes the uploaded PNG in, so the mark is bit-identical in every panel because it IS the same file. ⚠ **No upload means NO logo** — one 28-panel promo carried four different generated logos. | PAKKA |
| **D4** | The APP'S OWN name and mark (not a customer's) | ⚠ **It is an admin-panel field, and NO SCREEN MAY KEEP A COPY OF IT.** *Asked for directly: "mai chahta hun Aniwala icon and Aniwala Ai Studio text ko mai admin panel se change kar sakun … jaha jaha ye icon dikhe waha update ho jaye … ek baar mai update ho jaye sab jagah."* The name and an uploaded logo live in `server/branding.py` (ONE row, read by the public `GET /public/branding`, written only in **Admin → Brand**); the browser holds them in the module store `client/src/branding.js` and every screen reads it through `useBranding()` / `<Logo/>`. ⚠ **Do not confuse it with D3** — that is a CUSTOMER's logo composited into a board, per account, behind a token. This one is the product's own, public, and there is exactly one of it. ⚠ The tab title and the favicon count as "everywhere", and so does the AI assistant's own brief (`_system_instruction()` in the four prompt modules) — an assistant that names the old product inside the renamed product is the same bug wearing a hat. ⚠ **AND THE LOGO IS TWO FILES, ONE PER THEME** — see **E20**. Pinned by `tests/branding_check.py`, whose LAST section greps `client/src` and fails if the string is hard-coded anywhere a customer can see it. | PAKKA |
| **D5** | Any capability that calls a Google model | ⚠ **ONE CAPABILITY, ONE KEY — a shared provider switch means one lapsed project takes the whole app down together.** *Paid for live:* the GCP project's billing was switched off, Vertex AI answered **403 PERMISSION_DENIED / `CONSUMER_INVALID`** to every call, and the ✨ AI Editor printed that raw JSON at the user for typing *"add transition image layer 10 every each clip"*. Nothing was wrong with the chat — `TEXT_PROVIDER` pointed **every** text capability at one dead project, so the cheapest conversational call in the product died beside the expensive render pipeline that actually needed that project. The chat has `CHAT_PROVIDER` / `CHAT_MODEL` / `GEMINI_KEY_CHAT` now, each falling back to the shared text setting. ⚠ **THE KEY IS ALSO THE SWITCH**: pasting `GEMINI_KEY_CHAT` moves the chat on its own, because a key that changes nothing until you remember a second line is exactly the *"I set the key and it STILL says 403"* bug. `CHAT_PROVIDER` still outranks it, so a rollback deletes nothing. ⚠ **AND THE CLIENT CACHE MUST BE KEYED ON THE KEY, NOT JUST THE PROVIDER** — `script_breakdown.get_client` cached one client per provider, so two capabilities on `gemini` would share whichever was built first and one would silently bill the other's key: **a billing bug that looks like nothing at all, because both keys work.** ⚠ **`gemini-2.5-flash` IS CLOSED TO NEW DEVELOPER API KEYS** — *"no longer available to new users"*, delivered as a **404**, which reads like a typo in a model name; the Developer API default is `gemini-3.5-flash` (verified live), Vertex keeps 2.5. Pinned by `tests/chat_provider_check.py`. | PAKKA |

---

## E · Screens — the form, the board, cast and props

`client/src/components/`

| # | Scenario | The rule | Status |
|---|---|---|---|
| **E1** | Any box the user must EDIT | **It grows to its text — use `GrowTextarea`.** ⚠ **Fixed four separate times** on four screens (shot description, image prompt, cast description, board tile). If you add a `<textarea rows={n}>` on a field that holds model output, you are about to make it five. ⚠ **AND THE HEIGHT MATHS IS `scrollHeight + border + 1`, ALL THREE TERMS.** The border because `box-sizing: border-box` means a height of exactly `scrollHeight` gives the text a content box smaller than the text. **And the `+ 1` because `scrollHeight` is an INTEGER and a line box usually is not** — 13px at `line-height: 1.33` is 17.3px a line, so two lines are 34.6 and the browser answers 34, and `overflow: hidden` eats the 0.6px the last line's descenders sit in. *Found by `admin_fields_check.py` on the offer form's bullet box: "content box 34.0px, needs 34.6px".* ⚠ The banner box beside it passed only because a `min-height` happened to be taller than the fault — **a `min-height` is not a fix**, it is a place the fault hides. `GrowTextarea.jsx` and `GrowText` in `admin/fields.jsx` carry the identical maths on purpose; change one and you change both. | PAKKA |
| **E2** | Any control that spends money | **It must state what it will do before it is pressed**, and be disabled when it would do nothing. *Found: "Restyle all" opened on a hard-coded "Comic" and would have redrawn twelve panels in a style nobody chose, and billed for every one.* | PAKKA |
| **E3** | Building a new button / modal / panel | **Read how the existing workflows build one and copy it exactly.** This repo has exactly two runtime dependencies (`react`, `react-dom`) — there is no component library to fall back on, and small mismatches are a repeated complaint. | PAKKA (convention) |
| **E4** | Explaining a control | **Helper text goes in the element's own `title` (hover).** Visible label stays 3–5 words. Native `title`, not a new tooltip component. | PAKKA (convention) |
| **E5** | Offering a paid reference | **Say how many shots it is worth.** A cast card carries `1 shot` (warn colour) or `7 shots` so skipping is an informed choice, not a guess. ⚠ Guessing "hands only" from the wording would be a guess; the shot count is a fact. | PAKKA |
| **E9** | Re-opening a saved artefact from a library | ⚠ **Load the WORK behind it, not just the artefact.** *Found: opening a finished board restored the display settings and the job id and nothing else, so review / cast / props had no content and ← was deliberately wired to the library because there was nowhere else with any — "beech ka page nahi aa raha hai".* And **stamp the up-to-date signature** while you are at it, or the free route back to panels that already exist disappears and the only way back is paying to draw them again. Fail soft: a dead lookup keeps the old behaviour, never removes the pictures. | PAKKA |
| **E7** | A step that can be reached going FORWARD | ⚠ **It must stay reachable going back.** *Found: once a board existed, the review step collapsed to Regenerate + Back to your storyboard and the cast/props screens became unreachable — the exact screens where a wrong character or a drifting prop is fixed.* A finished artefact is when those screens matter MOST, not least. Offer the forward path always; hide it only when the destination would be empty. | PAKKA |
| **E8** | A comma-separated text field bound to an array | ⚠ **Split on the separator and NOTHING else, and join with the bare separator.** Filtering the empty piece eats the separator as it is typed; trimming each piece eats the SPACE inside a value — *"Ganesh idol" could only be typed as "Ganeshidol"*. Both were shipped, minutes apart, and both were caught only in a browser. Clean at the consumers, never on keystroke. | PAKKA |
| **E6** | A list where ORDER is the meaning | **Add and delete are not enough — it needs ↑ / ↓ too.** *Found: a scene added to the concept card to fill a real gap landed at position 7, belonged at position 3, and could not be moved.* ⚠ Copy the shot cards' controls exactly — `title="Move up"` / `"Move down"`, disabled at either end, **and the handler no-ops at the ends as well** so a keypress that beats the re-render cannot wrap scene 1 to the bottom. `moveKeyScene()` mirrors `moveShot()`. | PAKKA |
| **E10** | A SECOND screen that lists the account's work | ⚠ **Read the groups out of `client/src/dashboard_feed.js`; do not map the six workflows again.** `Home.jsx` carried that array with its own warning on it — *"ORDER MATCHES THE SIDEBAR … or Recent work quietly stops showing it (which is exactly how Image to Video went missing)"* — and that warning was written about ONE list. Explore made it two, so the array, `useCovers`, `useDashboard`, `statusClass` and `formatDate` moved into the shared module and Home now imports them back. A workflow added, renamed or hidden is a change in `Sidebar.jsx` + `features.py` + that one file, never in a screen. | PAKKA |
| **E11** | Any new screen that ADVERTISES a workflow (banner, tile, chip) | ⚠ **Ask the question that suits the SIDE OF THE SIGN-IN it is on.** Behind the sign-in: it waits for `/auth/me/entitlements`, exactly like the rail does — the account's own WORK may be drawn while that answer is in flight (failing open is right: "not answered yet" must never read as "you have nothing"), but a banner or tile for a workflow an administrator has HIDDEN is the *"hidden feature that reappears on every reload"* fault the sidebar already paid for. ⚠ **In front of it, entitlements are the WRONG question and there is no token to ask it with** — a public page must read `GET /public/workflows`, which answers what the product SELLS rather than what one customer has BOUGHT. Explore was moved from the first to the second when it became the marketing page; `Landing.jsx` owns `useLiveWorkflows()` and both public screens share it, so they can never advertise different sets. | PAKKA |
| **E12** | Moving WHERE THE APP OPENS | ⚠ **Change `LANDING_NAV` in `App.jsx` and nothing else — it is the one place.** FIVE paths land somebody in the app (a returning session, a fresh sign-in, a sign-out, an account switch, and leaving the admin panel) and every one of them used to name the destination itself. The bug that shape produces is *"it opens on the right page UNLESS you switched account"*, which nobody reports as one bug. `?admin` still wins over it: a link has to land where it points. ⚠ **Today it is `"home"`, and it used to be `"explore"`** — *"any logged in user must not see explore ... after login we know how our page which is home must look"*. Explore changed sides entirely (see **E24**), so the front door moved with it. | PAKKA |
| **E13** | Anything that SWAPS CONTENT IN PLACE (a carousel slide, a tab body, a rotating banner) | ⚠ **Every state must leave the box the SAME HEIGHT — measure it, do not eye it.** *Found: the Explore billboard took each workflow's first sentence, and those run 40 to 176 characters — so reaching Image to Animatic Image grew the banner by two lines, and because the two billboards share a grid row it took the NEIGHBOUR down with it and then snapped back. "image to animatics image ka panel bara ho jata hai kyun ismai text jayada hai."* Two halves: feed it copy written short (`stage.body` in `Landing.jsx`, 67-115 chars across all six) **and** clamp the lines in CSS, so a rename cannot bring the flinch back. ⚠ And trim a trailing full stop rather than appending one — gluing "." onto a sentence that had one printed *"never drifts.."*. Pinned by walking every slide and comparing `bounding_box()` in `explore_mount_check.py`; **proved to FAIL on the old code first** (232, 254, 232, 233). | PAKKA |
| **E14** | Anything that INTERRUPTS — a pop-up, a promo card, a nudge nobody asked for | ⚠ **Four rules, and it does not ship without all four.** (1) **ONE at a time** — the offer pop-up takes only the FIRST promoted offer, so two live discounts are still one card. (2) **Closing it means closed**, remembered per browser and ⚠ **keyed by the offer's id**, so a NEW offer still gets its turn — a single "seen the popup" flag would have silently killed every future promotion. (3) **It arrives AFTER the page**, not with it: a card that is simply there on the first frame is part of the layout and gets scrolled past. (4) **It is NOT a modal** — nothing dimmed, nothing trapped, Escape and a ✕ both close it. And it FAILS CLOSED: no offer, an unreadable endpoint, or the admin switch off ⇒ nothing is drawn, never a "loading your discount". | PAKKA |
| **E15** | Anything a customer can be SHOWN but not edit — an offer card, a banner, a nudge | ⚠ **Its words belong to the admin panel, not to the JSX.** The Explore pop-up's heading, its bullet lines, its small print and its button words are five fields on the offer row (`popup*` in `offers.py`), typed in **Sales → Offers**, capped server-side, and every one of them optional — an offer created before the fields existed still pops up, headed by its own `label` and `summary`. ⚠ The bullet box is a `GrowTextarea` (**E1**) and is split on the newline **only on the way out** (**E8**). | PAKKA |
| **E16** | Reaching for the gold gradient | ⚠ **Gold means THE ACTION on that screen — one per screen, and never on navigation.** Explore's first workflow tile shipped as a gold slab, copied from the reference; asked to stop being one: *"ye 3 button ka colour ek jaisa rakho, ismai golden hata do."* The rail's order is the owner's own pipeline, so painting whatever happens to be first as the recommended one says something nobody meant — and gold stops meaning "press this" the moment a tile you merely navigate with is wearing it. | PAKKA |
| **E17** | Shortening a name to fit a narrow place | ⚠ **Give EVERY workflow its short name, including the ones switched off — and keep the long one on hover.** The collapsed rail grew a label under each glyph and the names did not fit; the words were chosen one by one (*"Script to Storyboard ka only Storyboard, Image to Animatic Image ko Animatics, Video Editor ka Editor"*) and the same reply asked for the rest: *"jo hide mai kiya hai uska bhi kar hi dena, to mai hide wale ko on karun to ye kaam fir nahi karwana pare."* Three of the six are hidden today and one click from returning. `WORKFLOW_SHORT` in `Sidebar.jsx` names all six; `shortLabel()` falls back to the label's LAST WORD for a workflow an administrator adds later. Both names sit in the DOM at once and CSS picks one — swapping the text on collapse would put the layout in charge of the copy. | PAKKA |
| **E18** | Any label that can be ellipsised | ⚠ **Measure it in a browser, and measure it with a `Range` — `scrollWidth > clientWidth` LIES here.** A shrink-to-fit block inside a centred flex column never gets a scroll extent, so the obvious test reported green on a rail that was visibly printing *"Storyboa…"*. `document.createRange().selectNodeContents(el)` measures the words; the element's own rect is what they must fit inside. ⚠ **And it was proved to FAIL first**, at 70px, where it named all three offenders. `text-overflow: ellipsis` is the one failure mode that looks like a rendering glitch rather than a bug, so nobody reports it. | PAKKA |
| **E20** | Anything a customer READS that a developer wrote | ⚠ **Move it into the admin panel, and leave what it does today as the FALLBACK.** Explore's two billboards were built from the workflow list — heading from a workflow's name, body from its landing-page pitch, artwork from its own glyph — so the one part of the app whose job is to say something could only be changed by a redeploy, and could not carry a picture at all. *"This banner should be change aur hide by the admin, of it text and image."* `server/banners.py` + the **Banners** tab now own the words, the picture, the order and the switch. ⚠ **AN EMPTY STORE IS THE SHIPPED STATE, NOT AN OUTAGE** — with no live banner in a slot the page draws exactly what it drew before, and hiding the last one is the same code path. The panel adds a voice; it never becomes a prerequisite for the page rendering. | PAKKA |
| **E21** | Adding an uploaded picture to anything | ⚠ **Copy `branding.py`: a NEW ID PER UPLOAD, the row repointed BEFORE the old file is deleted, and the id checked against a regex on the way back out.** The id is the cache — a new upload is a new URL, so every browser, proxy and open tab picks it up with nobody reasoning about headers, and overwriting one path is exactly how a picture change fails to appear for the person who most needed to see it. ⚠ Serve only ids the store actually HOLDS, or a well-formed name reads whatever else ever landed in that directory. ⚠ And normalise to **WEBP at 1280px** for a banner (PNG at 512 is the logo's rule and would put a megabyte on the front page), keeping RGBA so a cut-out does not arrive inside a black rectangle. | PAKKA |
| **E22** | A form field that is empty because it has not been touched yet | ⚠ **Do not print the complaint until somebody has typed.** The banner form opened with *"A banner needs a heading"* already in red under an untouched box — an accusation, on a screen somebody came to CREATE something on. A disabled action button is the signal until then; the sentence appears once the form is `touched`. | PAKKA |
| **E23** | A masonry wall of pictures that leaves a hole | ⚠ **The cause is the RATIO SPREAD, not the packing — bound the ratio, and cap the column count by the item count.** *Found, reported twice: "gallery me lamba 9:16 board wala column bagal me khali jagah chhod raha hai."* Measured, seven projects gave columns ending at 483, 483, 323, 323 and **154** — the last two thirds empty. ⚠ **SHORTEST-COLUMN PACKING WOULD NOT HAVE HELPED**: a 9:16 board is 3.2× the height of a 16:9 one in the same column and nothing can fill the gap it leaves. ⚠ **AND FEWER COLUMNS MAKES IT WORSE, WHICH IS THE TRAP** — fewer columns are WIDER columns, and the tall card grows from 466px to 843px. `wallAspect` pulls every card into 4:5..16:9 (a 9:16 board loses ~30% of its height, evenly, top and bottom) and `wallColumns` stops five columns holding one card each. ⚠ **THIS IS THE ONE SCREEN THAT CROPS** and it is a deliberate exception to the libraries' rule: `aspectStyle` protects a THUMBNAIL you identify a project by at 86px; this is a wall with a caption under every card. Pinned by measuring the columns in Chromium, and **proved to fail on the old code first** (32%). | PAKKA |
| **E19** | A new screen that prints the app's NAME or draws its MARK | ⚠ **`useBranding()` for the words, `<Logo/>` for the mark — never a string literal and never a second copy of the SVG.** The name was typed into eight components and the mark drawn in two more, so renaming the app was a code change and the icon could never be the owner's own logo. Both are one save in **Admin → Brand** now, and the save lands live in the open tab because the panel writes straight into the store (`setBrand`). ⚠ **The uploaded logo wins over the drawn one inside `Logo.jsx`**, which is why every caller gets it for free — do not branch on it yourself. ⚠ And the built-in mark in `Logo.jsx` is NOT dead code: it is what a deployment with no upload shows, what **Remove logo** goes back to, and what is drawn before the branding call answers. The grep in `tests/branding_check.py` is what stops the ninth screen going stale. | PAKKA |
| **E20** | Any UPLOADED IMAGE that sits on a themed surface (a logo, a watermark, a supplied icon) | ⚠ **It needs ONE FILE PER THEME, and the swap belongs in CSS off `<html data-theme>` — not in React state.** *Found the day the logo upload shipped: a white wordmark read perfectly on the dark rail and disappeared completely in light mode — "jab mai dark mode mai hun to mera logo dikh raha hai magar jab light mode mai karta hun to mera logo white mai merge ho raha hai."* ⚠ **The drawn marks do NOT have this problem and that is what hid it**: `Logo.jsx`, `Icon.jsx` and `WorkflowIcon.jsx` are painted in `currentColor` and re-colour themselves for free, so an uploaded picture is the FIRST thing in this app that cannot. ⚠ **Either slot must fill in for the other** — one upload is a complete answer, because the commonest mark has its own colours and reads on both; a second file is for when it genuinely cannot. ⚠ **CSS, not JavaScript**, because the landing page, the sign-in card and the public storyboard viewer are drawn OUTSIDE the app shell and never see its theme state — the same three screens that forced the brand store to be a module rather than a Context. `base.css` hides `.brand-mark-light` by default and swaps under `:root[data-theme="light"]`; the unconditional default matters, or a missing `data-theme` would draw BOTH marks side by side. ⚠ **The favicon is the one exception** and does need JS — a `<link rel=icon>` cannot be styled — so `branding.js` watches `data-theme` with a `MutationObserver` rather than asking the four places that flip the theme to each remember. ⚠ **And the admin preview shows BOTH GROUNDS AT ONCE, with the colours hard-coded from `theme.css`** rather than `var(--panel)`: previewing a logo on whatever theme the panel happens to be in is exactly how this shipped white-on-white in the first place. | PAKKA |
| **E24** | Deciding which SIDE of the sign-in a screen lives on | ⚠ **A screen that SELLS is logged-out only; a screen that WORKS is logged-in only. One screen must not try to be both.** *Found: Explore was the signed-in front door — billboards, workflow tiles, and every project the account owned as one picture wall — and it was asked to become the marketing page instead: "the page we created on explore should be used to market ... any logged in user must not see explore which is happening right now ... the explore page is going to be only used for getting users, nothing more than that".* ⚠ **MOVING IT IS THREE CHANGES, AND ALL THREE OR NONE.** (1) Take the row out of `Sidebar.jsx` **and** the branch out of `App.jsx` — a page with no door that the shell can still be navigated to is a blank screen waiting for a stray nav string. (2) Move `LANDING_NAV` (**E12**). (3) Re-source every fetch: a public screen may read ONLY `/public/*`, because it is drawn before a token exists. ⚠ **AND ITS CONTENT HAS TO CHANGE WITH IT.** The wall was the customer's OWN work, which is an empty grid to a stranger on the one screen whose whole job is to make them want to sign up — so it became `server/showcase.py`, admin-curated. ⚠ The old reason for having **no public gallery still stands word for word**: nothing in this app is shared by default, so the work on that wall is only ever work an administrator chose to put there, never a customer's. | PAKKA |
| **E25** | A control on a PUBLIC page that leads into the product | ⚠ **It opens the sign-in, and it CARRIES what the person clicked.** *"if anyone clicks anywhere to use and create any workflow we must give a user first to login and then use."* Out there is nothing to navigate to, so a control wired to `onNavigate` looks perfectly right and does nothing at all — every one of them takes `onSignIn(workflowId)` instead. ⚠ **AND THE SECOND HALF IS THE HALF THAT GETS DROPPED**: `App.jsx` holds the id in `pendingWorkflow` and `onAuthed` opens it. Clicking "Script to Storyboard", typing a password and arriving on a generic dashboard is how an interested visitor is lost in the two screens between the click and the account. ⚠ **SANITISE IT ON THE WAY THROUGH** — `asWorkflow()`. One of those ids is a banner's `cta_target`, which an administrator TYPED and the server only shape-checks; "admin", "explore" and a plain typo all pass that check, and a nav nothing matches is a blank page as somebody's first screen after signing up. ⚠ A discount pop-up on a public page gates too: `POST /billing/coupon` is signed-in only and would 401 in front of a prospect, so the code is readable and copyable and the button asks for an account. | PAKKA |
| **E26** | Putting VIDEO on a page | ⚠ **The card carries a POSTER; only the viewer carries a player.** *"the videos or images should be clickable and be able to use it properly play."* Twenty-four `<video>` elements on the page everybody lands on is twenty-four downloads before anyone has clicked anything — on a phone that is the page not loading. The clip is fetched when it is asked for. ⚠ **KEY THE `<video>` BY ITS ITEM.** React reuses one element when only `src` changes, and a player whose src changes underneath it keeps the old buffer, the old `currentTime` and sometimes the old AUDIO — so stepping from one film to the next leaves you listening to both. A new key is a new player. ⚠ **`controls` + `autoPlay` + `playsInline`, and do NOT mute to force the autoplay through.** A browser refusing to autoplay an unmuted clip is the right outcome: `controls` means the play button is already under the pointer, so a blocked autoplay costs one click — muting it costs a silent film nobody realises has sound. ⚠ **Serve it with `FileResponse` and store it AS IT ARRIVED.** Range requests are what let a browser seek instead of pulling the whole file down first; a re-encode on the request path is the change somebody makes when an upload feels slow, and it is the change that breaks the one screen where watching the film IS the demo. ⚠ **There is no `ffprobe` on an `imageio-ffmpeg` install**, so a poster cannot be grabbed off a clip and its shape cannot be measured — ask for both (a still upload, an aspect dropdown), and measure only what Pillow is already holding. | PAKKA |
| **E27** | Adding a TAB to the admin panel | ⚠ **Add it to the field sweep in `tests/admin_fields_check.py` in the same commit — the sweep is a hand-written list, not a loop.** *Found while adding Showcase: the heading said "the six tabs" and it walked six while the panel had SEVEN. Brand and Banners had never been swept once, and they are the two tabs most full of typed copy — which is the exact thing that file measures.* ⚠ **AND OPEN THE CREATE FORM WHILE YOU ARE THERE.** Every one of these panels keeps its form collapsed behind a ＋ button, so a sweep that does not press it measures an empty screen and passes by having nothing to look at — which is how the offer form's clipped bullet box survived. ⚠ Wait on `.admin-card`, not on a row selector: a fresh install has no rows and the sweep would hang. | PAKKA |

---

| **E28** | A wall, gallery or list that accepts a VIDEO | ⚠ **It makes its own thumbnail. Do not ship a screen that asks for the still as a second upload.** *Reported: "when i upload video from admin panel but when i see explore page so no thumbnail show in my upload video"* — with a screenshot of a bare grey card. Nothing was broken: the poster field, the route and the "Add still" button all existed and worked, and **not one thing on the page said a second upload was expected**, so the feature was invisible and the card looked like a bug. ⚠ **THE REASON IT WAS BUILT THAT WAY WAS A WRONG FACT, AND IT IS WORTH KNOWING WHY**: the code said a frame could not be grabbed because *"there is no `ffprobe` on an `imageio-ffmpeg` install"*. True, and irrelevant — **`ffprobe` INSPECTS, `ffmpeg` EXTRACTS**, and `imageio-ffmpeg` ships ffmpeg, which `animatic.ffmpeg_exe()` had been locating for the exporter all along. A capability the repo already had was written off in a comment and then designed around. ⚠ **BUT THE OLD REASONING WAS RIGHT ABOUT BLACK**: films open on black, so probe a little way IN (1.0s, 2.5s, 0.5s, 0.0s) and **refuse a frame that comes back black** rather than shipping a wall of black rectangles — no poster falls back to the glyph, which is honest. ⚠ **THE GRAB FILLS AN EMPTY SLOT ONLY.** A still a person chose outranks frame one and must survive a re-upload of the clip. ⚠ **AND IT RUNS OFF THE EVENT LOOP AND FAILS SOFT** — `run_in_threadpool`, because the handler is `async def` and the file may be 96MB; and a thumbnail is never worth failing an upload over. Pinned by `tests/showcase_check.py` §11 with **real clips built by ffmpeg** (colour, portrait, all-black, and bytes that do not decode) — a fake MP4 could not have asked any of those questions. | PAKKA |
| **E29** | A page whose whole job is to be SHOWN to people | ⚠ **Give it an address.** *Asked as "where is ,my marketing explore page"* — after a VS Code task named MARKETING PAGE opened on the sales page instead, because Explore was internal state (`authView`), not a URL. A shop window you cannot send anybody a link to is a shop window facing a wall: it could only be reached by landing on `/`, reading the sales page and finding "See the work". ⚠ **COPY `?admin`, DO NOT INVENT A ROUTER** — one `syncUrlFlag` helper writes both, `replaceState` not `pushState` (see the note on `ADMIN_PARAM`; there is nothing to answer a Back button). ⚠ **AND A PUBLIC ADDRESS MUST KNOW WHO IT IS NOT FOR**: `?explore` is honoured for a VISITOR and dropped the moment somebody signs in, because `LANDING_NAV` is the standing decision that a signed-in customer never sees Explore again. The effect watches `authed` AS WELL AS the view — signing in does not touch the view, so without it the address would still read `?explore` while the customer is looking at their own Home. | PAKKA |

| **E30** | Two controls side by side where one changes the THING and the other only its COVER | ⚠ **Say which is which on the control itself — the type chip beside them is not enough.** *Reported with a screenshot: "maine admin panel se image daala hai — magar jab explore pe kholta hun to video khul raha hai, image nahi. Aisa kyun?"* The row said **VIDEO · 16:9** and the wall was serving the right file for the right item — proved end to end, two clips, byte-for-byte. Nothing was broken: **"Replace still" had set the COVER, and the item was still a video**, so clicking it played the video exactly as designed. ⚠ **THE FAULT IS THAT THE TWO BUTTONS LOOK LIKE VARIANTS OF ONE ACTION.** "Replace file" and "Replace still" sit next to each other, same size, same style, one word apart — and only ONE of them changes what a visitor sees when they click a card. Both tooltips now lead with the distinction (**"THE THING ITSELF — what opens when somebody clicks this card"** against **"ONLY THE COVER — it does NOT change what opens"**), per the standing rule that the hint goes on hover and the visible label stays short. ⚠ **AND THIS IS THE SECOND REPORT IN A ROW FROM THE SAME ROW OF BUTTONS** — E28 was the still nobody knew to upload, this is the still that got uploaded instead of the file. When a screen produces two "nothing is broken" reports running, the screen is what is broken. | PAKKA |

| **E31** | Two tabs in the panel that edit ONE screen | ⚠ **Make it one tab with sections inside it — and when a comment has to explain that two tabs are the same page, that is the design telling you.** *Asked for outright: "admin panel banners and showcase both section same work kar raha hai explore page ke liye — so tum ek explore ka hi banao aur uske under banner and showcase rakho."* The old tab strip had already half-admitted it: the note above `showcase` read *"IMMEDIATELY AFTER BANNERS, because they are the SAME PAGE … two tabs apart is two tabs too far"* — **adjacency was the workaround, not the fix.** ⚠ **NEITHER CHILD WAS REWRITTEN**: `AdminExplore` is a segmented strip and a switch, and `AdminBanners` / `AdminShowcase` still own their note box, their card and their create form. Merging their markup would have turned two working editors into one half-tested one, over two stores, two routes and two upload rules that are genuinely different things sharing a page. ⚠ **REUSE THE PANEL'S OWN `admin-segment`**, which Activity, Sales, Features and the user detail already use — a second strip that merely resembled them is the mismatch this repo keeps paying for. ⚠ **AND MOUNT ONE AT A TIME**, not both hidden with CSS: each half fetches on mount and holds an open create form, so a half-typed banner surviving a trip to Showcase and back is state nobody expects and nobody tests. | PAKKA |

| **E32** | A screen needs a rail, a header or a panel the app ALREADY HAS | ⚠ **Give the existing component a MODE. Do not build a second one that looks like it — not even for a page with a different shell.** *Paid for in one round trip*: the public Explore page got a hand-built `xp-rail` copied from `.sidebar`'s measurements, and it came back the same day — *"mai chahta hun ki ye sab waisa hi dikhe jaise user login kar ke dikhta hai — jaise abhi missing hai collapse bar and logo and AI Studio name."* **A copy is never finished being close.** It shipped without the brand mark, the app name and the collapse toggle, and every one of those would have come free from the real component. ⚠ **THE MODE SUBTRACTS, IT DOES NOT RE-DESCRIBE**: `publicMode` on `Sidebar` removes the avatar and the account menu — the two things a visitor has no version of — and swaps the word on the gold button at the foot. Everything else is the same markup and the same stylesheet, so the two can no longer drift. ⚠ **AND THE SUBTRACTION HAS ITS OWN BUGS — LOOK FOR THEM.** Removing the avatar removed the `margin-left: auto` that pushed the whole brand row's buttons right, so the collapse toggle tucked itself against the app's name; `.sidebar-public .sb-collapse` takes the job over. **Whatever a mode hides, check what that element was CARRYING for its neighbours.** ⚠ **A DIFFERENT SHELL IS NOT A REASON TO FORK.** Explore has no `.shell` around it, which is what made a copy tempting; the answer was to give the page `.shell`'s own grid (280px / 92px / one column under 820px) rather than a second rail. | PAKKA |

| **E33** | Putting a rail beside a page that has many rows | ⚠ **Give the page ONE wrapper element and make the grid two cells — do not place the rows individually and span the rail with `grid-row: 1 / -1`.** *Shipped and reported the same hour: "pahla image pura blank hai, magar jab scroll kiya to tab aaya ye content — uper mai blank kyun dikh raha hai."* ⚠ **`1 / -1` DOES NOTHING WITHOUT `grid-template-rows`.** A negative grid line counts back from the end of the **EXPLICIT** grid; an implicit grid has none, so `-1` resolves to line 1, the span collapses to a SINGLE row, and that row is as tall as the rail — 100dvh. Everything sharing it is stretched down a screen-tall cell and the rest of the page starts below the fold. ⚠ **AND THE THING THAT MOVED WAS THE HEIGHT, NOT THE TOP.** The header's box still began at y=0; it was STRETCHED to 900px, and `.landing-nav`'s own `align-items: center` floated its links half way down the blank screen. A check written as `nav.top < 160` passes on the broken page. **Measure the height.** ⚠ **THE ANSWER WAS ALREADY IN THE REPO**: `.shell` + `.shell-main` is exactly this, two cells of one row, and had been since it was written. The objection that a wrapper would orphan eight listed selectors was wrong twice over — they were ONE rule, and re-pointing them was one edit. ⚠ **G7, SHARPENED: A SOURCE CHECK IS NOT A LAYOUT CHECK.** `npm run build` passed, and thirty assertions in `showcase_check.py` about that grid were all TRUE while the page was visibly broken — two of them asserted the very declarations that CAUSED it. Geometry is measured in Chromium by `tests/explore_layout_check.py`, which reproduces the blank screen in-page (`display: contents` on the wrapper plus the old span) and must catch it. | PAKKA |

| **E34** | A rail beside a page — and the tests around a component that gained a MODE | ⚠ **The rail must carry a row for the page it is standing on, marked active.** *Asked for by name: "explore ka button kyun nahi dikh raha hai, ye page kahan se khul raha hai? home ke upar explore button daalo."* The public rail listed Home and the workflows and never named **Explore**, which is the page it was drawn on — so nothing was highlighted, and pressing Home left for the sales page with no way back but a link buried in its nav. A rail that cannot say "you are here" is a rail with a one-way door in it. ⚠ **AND HERE IS THE PART THAT BITES TWICE: ADDING A MODE ROTS EVERY "NOT IN FILE" ASSERTION.** Three checks failed the moment this row landed, all of them written as *"the string X does not appear in this component"* — a fair proxy for a rule that was once absolute (**nobody signed in may see Explore**) and meaningless once the SAME component draws both sides. **Re-assert the rule as "behind the guard", not "absent from the file"**: the row exists exactly once and `{publicMode && (` sits above it — and prove the new check fails with the guard removed, which this one was. ⚠ The other two had grown a third branch and were matching a whole handler as one string; they name the three destinations now, which is stronger than the line they replaced. See also **E32** (give the component a mode) and **G7** (a source check is not a rendered screen). | PAKKA |

| **E35** | Artwork on a PUBLIC page that belongs to a workflow | ⚠ **Draw it from the live workflow list, and let the owner upload the picture — never hard-code the tiles.** *Asked for outright: "mai chahta hun ki ye four icon ke jagh mai image lagun so admin panel mai landing page ka fuction bano so mai image dall sakun har workflow ka … jo live hai uska dikhe image yaha pe aur jo hide hai uska nhi dikhe."* The landing hero's four tiles were four hard-coded lines of JSX holding four hand-drawn SVGs, in pipeline order, with **nothing in the file saying which drawing belonged to which workflow** — so hiding a workflow in the admin panel left its picture on the front page, and the biggest artwork on the page every prospect lands on could not be changed without a developer. ⚠ **THE FALLBACK CHAIN IS THE RULE, NOT THE UPLOAD.** Three rungs, in this order: the uploaded picture, then the drawing (`ART_BY_ID`, keyed by workflow id), then the workflow's glyph — **and the glyph needs the server's emoji passed in as `fallback`, because `WorkflowIcon` renders NOTHING for an id it has no glyph for.** Miss that last rung and a workflow launched after the build gets a blank white tile. ⚠ **AND THE VISIBILITY FILTER GOES ON THE SERVER TOO.** The client already draws only what `/public/workflows` named, which is enough to make the page correct — and leaves a picture of a switched-off workflow sitting in a public JSON payload. `landing.public_payload()` drops it. ⚠ The store is keyed by **workflow id, not by a row id an admin invents** (the difference from `banners.py`), which is what makes a seventh workflow need no plumbing; the tile CAP is a layout number and the panel has to SAY when a picture is stored but not drawn, or an operator stares at a good thumbnail and an unchanged page. Pinned by `tests/landing_art_check.py` — including the hide→un-hide round trip, because a "hidden" that also deletes the file is a different product. See also **E3** (copy the existing panel), **E31** (one tab per screen). | PAKKA |
| **E36** | A rail row that is CENTRED when the rail is collapsed | ⚠ **A `margin-left: auto` written for the wide rail must be scoped `:not(.collapsed)`.** In a COLUMN flex box `margin-left: auto` still eats the free space on the left and pushes the button to the RIGHT edge — it does not switch itself off. *Found on Explore: the collapse-bar toggle sat off-centre under a centred logo, because `.sidebar-public .sb-collapse` gave it `margin-left: auto` to push it right in the WIDE public rail (a visitor has no avatar to do that job) and the same rule kept firing once the rail became a column.* Now `.sidebar-public:not(.collapsed) .sb-collapse` — collapsed, `.sb-brand`'s own `align-items: center` is left to do it. | PAKKA |
| **E37** | The SAME component drawing a signed-in screen and a public one | ⚠ **A row that means two different things on the two sides gets a `publicMode` guard — never a deletion — and the tests must assert its ABSENCE on one side and its PRESENCE on the other.** *Found on Explore: the visitor's rail carried **Home** directly under Explore AND the page's own nav carried a second **Home** link four rows away, both meaning "leave for the sales page" — "not need to show home buttun in explore page", reported with a picture of each.* Signed in, Home is the front door (`LANDING_NAV`); signed out, Explore IS the page. So Home is behind `{!publicMode && (` and Explore behind `{publicMode && (`, a mirrored pair in `Sidebar.jsx`. ⚠ **One-sided checks are how this breaks:** `'onNavigate("home")' not in sidebar` would pass with the app's first row deleted, so `showcase_check.py` pins the row's count AND its guard, and `explore_layout_check.py` asserts no Home on the public rail while `explore_mount_check.py` asserts Home is row one inside the app. ⚠ **Remove BOTH copies of a word or the fix reads as half-done.** ⚠ Explore now offers no door backwards at all (`syncExploreUrl` uses `replaceState`, so browser Back does not go there either) — deliberate: it is a shop window whose exits are forwards. | PAKKA |
| **E38** | A page laid out for a laptop, opened on a 27" monitor | ⚠ **A fixed `max-width` is only half a responsive page — give the cap a RANGE, and cap the CARDS separately from the page.** *Reported with a screenshot of a 2560px display: "make it screen responsive either 27 inch screen and what so ever".* Explore's rows were pinned at a flat `1560px`, so a 27" screen showed the same narrow column with ~700px of dead ground either side — and the picture wall INSIDE that column stretched its two cards to ~1000px each, because the wall's column count is capped by the ITEM count (`wallColumns`), and multi-column stretches whatever columns it has to fill its box. So the page looked small and the cards looked enormous at the same time. ⚠ **THE OLD NUMBER BECOMES THE FLOOR, NOT THE ANSWER**: `--xp-max: clamp(1560px, 88vw, 2200px)` — every screen up to ~1770px lays out byte-identically, so nothing already measured moves, and only the big screen changes. ⚠ **AND THERE IS STILL A CEILING** — `none` gives a 2400px line of body text and a nine-column wall. ⚠ **CAP THE WALL, NOT THE CARD**: a `max-width` on a card leaves a hole beside it in a column it no longer fills; narrow the multi-column box instead (`.xp-page > .xp-gallery`), with a 72rem floor so laptops are untouched. ⚠ **AND A CAPPED ROW MUST SPELL ITS LEFT EDGE OUT** — its neighbours are CENTRED in the page column, so `margin-inline: auto` centres the narrower box on its own width and it sits inboard of the heading above it. ⚠ **Responsive rule 4 cuts both ways**: growing the page without re-cutting the billboards turns the top of the screen into a letterbox strip, so there is a `min-width: 1800px` stage for their height. Measured in Chromium at 2560, 1920, 1440, 1100, 760 and 390; `tests/explore_layout_check.py` takes the 2560 measurements now and is GREEN on the real page (row 2200px, wall 1152px, card 497px), with the flat cap put back as the proof it can fail (row → 1560px, card → 701px). ⚠ **AND A CHECK THAT SKIPS ITSELF WHEN ITS FIXTURE IS MISSING IS A CHECK THAT DOES NOT RUN.** The card assertions were written behind `if wall and card:` with a friendly `else: print(...)`, and this file boots EMPTY stores — so the wall was never on screen and the ballooning card was never measured, while the section printed green. **Seed the fixture and assert its presence**; never let a missing fixture read as a pass. | PAKKA |
| **E39** | Handing this app's cut to ANOTHER editor (Premiere, Resolve, Avid, FCP) | ⚠ **A project file is a RECIPE, not the food — so the media travels with it or the user gets a timeline of red "Media Offline" rectangles.** `GET /animatics/{id}/interchange` answers with a **ZIP**: the FCP7 XML plus a `media/` folder holding every picture, clip and sound the XML names, and `media=false` is the deliberate exception for somebody who already holds the footage. ⚠ **And the `<pathurl>`s are RELATIVE unless the user says where they will unzip** — only an absolute `file://localhost/…` re-links with nothing asked, and at export time nobody knows where the zip will land; relative costs ONE "Locate" in Premiere's dialog, and because every file is in one folder, locating one finds them all. The optional "Unzip folder" field writes the absolute form for the user who does know. | PAKKA |
| **E40** | An export that cannot carry everything on screen | ⚠ **Say what will be LOST, by name and by count, BEFORE the download — not after.** A project-exchange file holds the CUT (which clip, where, how long, on which track, the source window, the audio and its level); no format on earth holds this app's WebGL grades, LUTs, masks, blend modes, fourteen transition shapes, text clips or shape clips. `GET /interchange/preview` costs nothing and touches no media, and `ProjectFileModal` prints its list — *"3 colour grades · 2 masks · 1 text clip"* — because "some effects won't transfer" is a sentence people skip and a counted list is a decision they can make. ⚠ **A clip whose file has gone is NAMED too**, in red, not silently dropped. | PAKKA |
| **E41** | Converting this editor's milliseconds into an NLE's frames | ⚠ **ONE conversion, in ONE function, rounding half AWAY FROM ZERO.** `interchange.ms_to_frames` — and **not Python's `round`**, which rounds half to EVEN and would send one cut down and the next up; the drift is invisible for ten clips and is a lip-sync bug for a hundred. ⚠ **And the spans are computed from the FULL clip list before anything is filtered** (`animatic_render.frame_spans`): a clip with no `start_ms` sits after the last clip *on its own track*, so dropping a hidden or file-less clip first slides every later clip on that row and exports a different film. ⚠ `speed` is deliberately NOT exported — the source window is forced to the timeline length and the speed change is reported as a loss, because a clip of the wrong footage is worse than a clip somebody has to re-speed. | PAKKA |
| **E42** | Exporting the same cut in more than one format | ⚠ **ONE model, one writer per format — never a second reading of the timeline.** `interchange.build_sequence` folds the project once; `FORMATS` maps a name to a writer and `format_losses` to that format's own ceiling. ⚠ **AND THE CEILING IS ADDED TO THE BASE LOSSES, NEVER SUBSTITUTED FOR THEM** — an EDL loses the grades *and* every upper video row, so the preview endpoint takes the format and the dialog re-asks when the dropdown changes. A dropdown that changes the download but not the warning is worse than no dropdown: the user reads the Premiere caveat and receives the EDL. ⚠ **One file per format on disk** (`<title>-edl.zip`), the same rule `_video_path` follows for mp4/gif/png — a single `<title>.zip` means fetching the EDL destroys the XML fetched a minute earlier. ⚠ **The `media/` folder is IDENTICAL in all three zips**; only the document at the top changes. | PAKKA |
| **E43** | Writing a file another APPLICATION will parse (not a person) | ⚠ **Match the target's real constraints, not the ones that look reasonable.** Three paid for here, each of which produces a file that looks fine and fails silently: **(a) ExtendScript is ES3** — `{in: 0}` is a *syntax error*, so an After Effects script with a reserved word as a key does not load at all (no comp, no message); the data block says `srcIn`, and the generated file is **pure ASCII** because ExtendScript decides a file's encoding from a BOM and the host locale — the project title reaches AE through `json.dumps`, which escapes it. **(b) An EDL reel name is EIGHT characters**, and older systems truncate rather than complain, so two files differing after character eight conform as one tape; the real filename goes in `* FROM CLIP NAME:`. **(c) EDL events sort by record time with VIDEO first on a tie** — sorting the channel as a *string* puts `A` before `V`, so a film whose voiceover starts at 00:00:00:00 opens with an audio event, which is legal and wrong to every human reading it. | PAKKA |
| **E44** | Bringing ANOTHER editor's cut into this one | ⚠ **It ADDS onto new rows; it never replaces the open timeline** — an import that overwrote what was on screen would be the most destructive button in the editor, and rows are cheap. ⚠ **The server produces the material, the CLIENT decides the timeline** (`import_storyboard`'s contract): `track` and `layer_id` come back RELATIVE (`0,1,2…` / `_import_0`) because only the browser knows which rows are free, and every imported audio lane gets a real row — `layer_id: ""` is the DEFAULT lane and would pile an import into whatever is already there. ⚠ **ONE `flush` = ONE undo step**, which is the actual safety net: the frames, the rows, the sounds, the transitions and the library cards go up in a single write, so Ctrl+Z takes the whole import back out. ⚠ **The patch key is `audioTracks`, not `audio_tracks`** — the API answers in snake_case and the DOCUMENT is camelCase, so the copy-paste spelling is an unknown key: the import looks perfect and the sound is gone on the next reload. Pinned, and *proved to read False when broken*, in `tests/interchange_check.py` §12. | PAKKA |
| **E45** | Media that a project file names but the user did not bring | ⚠ **A labelled COLOUR CARD in the right place, never a silent omission.** A project file names files by a path on the machine that wrote it; a browser cannot read that path, so the footage has to be attached (or the user brings back a `.zip` exported from here, which already holds both). Leaving the unmatched clips out hands back a timeline with holes and no way to see what is missing or where; a card carrying the clip's own name keeps the CUT whole, makes every gap visible, and is fixed by dropping the real file on that row. Every one is named in `placeholders` and printed in the dialog **before** anything is added. ⚠ **Sound gets NO placeholder** — a silent picture card is readable, a silent audio clip is a lie you cannot see. ⚠ **Matching is by FILENAME, case-insensitively, and by the stem as well**, because Windows and macOS disagree about case and a transcode leaves `shot_03.mov` in the XML with `shot_03.mp4` on disk. | PAKKA |
| **E46** | A round trip out to a project file and back | ⚠ **It is FRAME-exact, not millisecond-exact, and expecting the millisecond back is the bug.** This editor holds time in ms; every NLE holds it in frames. At 25 fps a clip placed at 500ms is at frame 12.5, which does not exist — it exports as frame 13 and returns as 520ms, and exports as frame 13 again. Nothing drifts; it snaps once. A test asserting the original millisecond fails on correct code, so assert **whole frames, and within ONE frame of where it started**. ⚠ **Reading in goes through MILLISECONDS on purpose** (`frames_to_ms` at the document's OWN rate), which is what lets a 25fps XML open correctly in a 24fps project — converting frames straight to frames would nail somebody else's rate onto this film. | PAKKA |
| **E47** | Reading a file format whose owner has never published it (`.prproj`, `.aep`) | ⚠ **The refusal stays the DEFAULT answer, and the guess is the SECOND one — offered only after the refusal has been read.** A `.prproj` is Premiere's own save file: no specification, and the structure moved again in Premiere 2026. So `read_document` refuses it with a sentence naming a route that always works (*File › Export › Final Cut Pro XML*), and `_read_prproj` runs only on `experimental=True`, which the dialog sends only after showing that sentence. ⚠ **An experimental reader offered as a checkbox up front is one most people tick without ever seeing the reliable door beside it** — that is why it is a button under the error and not an option above the file picker. ⚠ **But the door must still LOOK like a door.** It shipped as a `.btn ghost`, which draws neither border nor background, so the only thing on that panel there was to press read as a line of text — caught from a screenshot during the first live test. A bordered `.btn`; never `primary`, which would put the guess level with the reliable route and undo the ordering this rule is about. ⚠ **It fails LOUDLY and PARTIALLY, never quietly and completely**: it walks `ObjectRef` references rather than a fixed XPath (a `.prproj` is a flat pile of objects, so a clip's position is four hops from the clip), and when the track layout is unrecognisable it falls back to one row per media type and SAYS the rows are wrong rather than refusing. ⚠ **Every import it produces carries “best-effort, check it against Premiere” as `warnings[0]`, and the dialog badges the report `best guess` for as long as it is on screen** — a caveat only in a list somebody scrolls past is not a caveat at the moment they press Add. ⚠ **And the tests cannot prove it works.** The fixture in `tests/interchange_check.py` §8c is a hand-built imitation and there is no writer to round-trip against; green there means the reader behaves as it claims on the shape it claims, NOT that it opens a real Premiere project. Say that in the release note. | PAKKA (the policy) / OPEN (never run against a real `.prproj`) |
| **E48** | Accepting a COMPRESSED file through an upload limit | ⚠ **The upload cap counts the compressed bytes, so the only place that can stop a decompression bomb is the reader.** `MAX_UPLOAD_BYTES` passed a few megabytes of gzip that unpacks to gigabytes; `interchange._gunzip_capped` decompresses with an explicit ceiling (`PRPROJ_MAX_XML_BYTES`) and refuses past it, because XML of this shape packs at better than 1000:1. ⚠ **A cap added at the route would be the wrong place** — it would have to guess the ratio for every format; the reader knows what it is unpacking. Same rule for any zip, gzip or archive this app is handed. | PAKKA |
| **E49** | Reading a SETTING (frame rate, size) out of somebody else's project file | ⚠ **Take it from the part of the file you are actually importing, never from “anywhere in the document”.** Found by review, not by a run: `_read_prproj` picked the right sequence and then read the rate off the FIRST one in the file, because Premiere keeps the rate on a track-group object several hops from the sequence and the first version only looked one hop. A project holding a 30fps sequence above the 24fps one being imported read every clip **25% adrift** — and a wrong rate does not look like an error, it looks like a film. `_prproj_rate_near` now walks that sequence's own subgraph and never crosses into another `Sequence`; both fallbacks (first rate in the file, then the project's own fps) are **warned about**. ⚠ **And when a best-effort reader gives up, say THAT, not “the file was empty”** — clips found whose positions could not be read now raise a refusal naming the real reason, instead of falling through to the route's “There was nothing on that timeline to bring in”. Pinned in `tests/interchange_check.py` §8c with a two-sequence fixture whose rates differ, so a document-wide search cannot pass. | PAKKA |
| **E50** | Handing imported clips to a Pydantic response model | ⚠ **Clamp to the schema's bounds inside `to_project` and COUNT what you changed — a value outside them is not a bad clip, it is a 500 with no message.** The import route builds `AnimaticFrame` / `AnimaticAudio` straight out of what `to_project` returns, and those cap a clip at TEN MINUTES (`le=600_000`) and forbid a negative start (`ge=0`). This app makes animatics; somebody else's timeline does not know that. ⚠ **The files that tripped it were ORDINARY** — a fifteen-minute interview take, a music bed across a whole reel, a record-in before the sequence starts — and every one reached the user as *Internal Server Error*, whole import lost, nothing naming the clip. ⚠ **It was NOT a `.prproj` bug**: the clamp is in `to_project`, so it had been true of the FCP7 XML and EDL readers since Phase 3 and nothing had caught it. Clamped and reported now (*“2 clips were longer than… and were shortened”*), and ⚠ **the length is taken from the ORIGINAL start before the start is clamped**, or a clip that began before the sequence gets STRETCHED instead of moved. `tests/interchange_check.py` §8d validates every clip `to_project` returns against the real models, **and proves the models would have rejected the unclamped values** — without that last line the section would pass just as happily on a build with no bounds at all. | PAKKA |
| **E51** | Putting two buttons side by side, ANYWHERE in the app | ⚠ **They are one pair and they must be one size — same height, same top edge, and a shared width floor so the short label is not a stub beside the long one.** Reported from a screenshot, and it is a REPEAT complaint: *“jab do buttun saath mai hai to size barabar hona chaiye”*. ⚠ **The cause is the same every time and it is not in the dialog** — `.btn.primary` in `base.css` carries a global `margin-top: 1.1rem` because it is normally the last control in a FORM. Dropped into a ROW it pushes the gold button DOWN, the ghost beside it stretches to the taller line, the two end up BOTTOM-aligned, and the ghost reads as a bigger control. Measured on the import dialog: identical 37.2px heights, tops 8.8px apart. ⚠ **`margin-top: 0` alone is not the fix** — a gold primary carries its own glow and heavier weight, so beside a ghost it still reads larger. Copy the `.export-modal-actions` rule whole: `inline-flex` + centred + `gap` + a fixed `height` + zeroed vertical padding + `margin-top: 0` + `box-sizing: border-box`, and a `min-width` floor. ⚠ **A width FLOOR, never `flex: 1`** — a primary's label grows (*“Add 12 clips to the timeline”*) and an equal share would blow the Cancel button up to match it. ⚠ **This has now been written nine times in CSS and never once here**, which is why it keeps coming back: check `.btn.primary` whenever you add a button row, and pin it — `tests/interchange_check.py` §12 guards this one, and the guard is proved to fail without it. | PAKKA |
| **E52** | Writing a reader for a format with no published spec | ⚠ **A HAND-WRITTEN FIXTURE PROVES ONLY THAT YOU CAN READ YOUR OWN FIXTURE. Get one real file before you believe a single green test.** The `.prproj` reader shipped with 43 passing checks and then found 167 clips in the user's first real Premiere project and could not place ONE of them. Three things were wrong, each fatal on its own, and all three were invisible to a fixture built from the same guess as the code: **(a) Premiere uses TWO kinds of reference** — `ObjectRef` (numeric `ObjectID`) and `ObjectURef` (GUID `ObjectUID`) — and the link from a track group to its TRACKS is the GUID kind, so following only the numeric one finds no tracks at all and drops to the flat route; **(b) the times are NESTED, not referenced** — `<VideoClipTrackItem><ClipTrackItem><TrackItem><End>` is one element with no refs in between, so a reader looking for direct children or following refs reads nothing; **(c) a `<Start>` of zero is simply not written** — 17 of the 167 had none, and requiring Start AND End together throws away every clip at the head of a track. Also learned: **there is no `<Sequence>` object at all** (a sequence IS a `VideoTrackGroup` + `AudioTrackGroup`), the sequence rate is `<FrameRate>` on the track group in ticks-per-frame, and **the same `<FrameRate>` tag carries 44100 and 48000 on audio streams**, so a rate must be sanity-checked before it is believed. ⚠ **The honest move when you cannot get a real file is to say the tests prove behaviour and not compatibility** — which §8c did say, and it was right to. Now BOTH shapes are pinned: the real one, and the ref-shaped one another Premiere version might write. ⚠ **Two more the real file taught, both cosmetic-looking and both real:** a Premiere TITLE carries a `<FilePath>` holding an internal NUMBER (`1196574294`), which became a “missing media” line naming a file nobody can find — a value with no separator and no extension is an id, not a path; and sound that is found but whose files did not arrive gets **no placeholder by design** (E45), so the summary reads “0 sounds on 0 rows” and looks like a file with no audio — it now says how many were found and could not be brought in. | PAKKA |
| **E53** | A picker that collects files the user will need ALL of | ⚠ **It must ADD, not replace — a browser file picker can only ever see inside ONE folder at a time.** Reported live: *“maine ek image select kiya to sirf wahi image aaya… kya ek ek kar ke choose karna hai?”* One Premiere project's media sat in `Images/`, `Videos/` and `Audio/`, and `setMedia(Array.from(e.target.files))` threw the previous folder away on every pick — silently, so it read as the import losing pictures. Now it merges, **de-duplicated by NAME** (which is what the server matches on, so the same file from two folders is one file), shows a running count, and offers **`webkitdirectory`** so a whole folder tree is one click. ⚠ **Filter to real media on the CLIENT** — a folder pick otherwise uploads the project file, every Premiere auto-save and any stray document, megabytes the user waits for so the server can refuse them one by one. ⚠ **And give a way back** (Clear): a list that only grows is a trap. | PAKKA |
| **E54** | Telling “the server said no” apart from “the server never answered” | ⚠ **Mark the transport failure with a FLAG on the error, not a sentence to match on.** Seen live: uvicorn runs with `--reload`, a source edit restarted it, and the `.prproj` experimental read died mid-flight. The dialog counted that as the offer having been taken and hid the button — leaving a network error and no way back to the control just pressed. A backend that never answered said NOTHING about the file, so the offer must survive it. `api.js`'s `request()` now sets `err.offline = true` and the caller reads the flag; matching on the message would break silently the first time somebody rewords a sentence written for a human. | PAKKA |
| **E55** | A browser upload that fails instantly, with ZERO bytes sent | ⚠ **READ THE NETWORK TAB BEFORE BLAMING THE SERVER: `0 B transferred` in 1–3ms means the request never left the machine, so it is not a size, a timeout, or a backend fault.** Two different causes were found behind the same *“Can't reach the server”* message, and the message itself sent the investigation the wrong way twice. **(1)** uvicorn runs `--reload`; editing a `.py` file restarted it mid-request and the retry only covered 1.4s against a 3–5s restart (fixed: doubling backoff, ~10s). **(2)** With the backend healthy the whole time, ONE attached file was blocked before a byte was sent — `net::ERR_ALPN_NEGOTIATION_FAILED`, 1ms, 0 bytes — by security software scanning the upload. ⚠ **ONE bad file kills the WHOLE multipart request**, so 26 files failed and 25 of them were innocent; renaming it changed nothing (content, not name) and it was not even referenced by the project. ⚠ **Bisect the attachment to find it** — one file at a time proved it in 26 requests — and tell the user to narrow the folder down, because *“check the backend”* is advice that cannot work here. ⚠ **And reproduce it in a REAL browser** (Playwright + `set_input_files` + `fetch`): `requests` and `TestClient` both sailed through the same 35MB — only Chrome sees the block. | PAKKA |
| **E56** | Reading a resolved clip back out of the project list — ANYWHERE | ⚠ **Look it up by `id`. `picture.index` is a position in the FILTERED array `sceneAt` was given, and every consumer holds the UNFILTERED one.** `AnimaticEditor` hands `sceneAt` its `shown` document — hidden picture rows removed — so the instant a row is switched off `shown.frames` is shorter, every index after it shifts down, and `frames[picture.index]` names a different clip. ⚠ **THIS WAS FIXED TWICE AND MISSED ONCE, because the third site fails SILENTLY.** `ProgramCanvas` (`framesById`) and `currentIndex` were both corrected when the monitor drew the wrong picture; `useMonitorVideo` was not, and there a wrong clip means the cue carries some other clip's id, `videoElsRef` has no element under it, the loop skips, and the <video> that IS on screen is **never told to play**. Nothing throws, nothing logs, the element is the right one and the texture is re-uploaded every frame — it just holds frame 0 forever. Reported after a Premiere import as *“video ka sirf ek thumbnail jaisa dikhta hai… pura clip mein ek image jaisa”*, with the images and the audio on the same timeline playing perfectly; the project had **six of its eight picture rows hidden** (`hidden_lanes`), which is saved state, so it survived every reload. ⚠ **A resolved picture always carries its clip's `id`** (`resolve()` spreads the clip first) and an id survives being filtered out of an array in a way an index cannot — so build a `framesById` map and never index. ⚠ **Pinned in `tests/monitor_video_check.py`, which drives a REAL H.264 file in Chromium and runs the same scene twice — once plain, once with a switched-off row ahead of the clip.** The plain case passed throughout the bug; only the hidden one catches it. ⚠ **And assert the movement as a DELTA** (`currentTime` ran on by more than a second), never as `currentTime > 1`: an uncued element keeps whatever time the previous case left it at, which reads as “playing” to any absolute test. | PAKKA |

| **E57** | A placeholder for media that did not arrive, on a row ABOVE the bottom one | ⚠ **It goes on the timeline, named — and it draws NOTHING. An opaque card on an upper row hides the whole film behind it.** E45 is still right where it was written (the BOTTOM row, where the frame would otherwise be empty and the gap invisible); one row up the clip is an OVERLAY, and blanking it black is not a marker, it is a lid. ⚠ **This is not an edge case, it is what EVERY Premiere import does.** A Title, a Graphic and an Adjustment Layer have no media file to attach and never will, so every one of them arrives unmatched — a real project brought in 44, three of them at FULL LENGTH over the cut, and the result previewed and exported as 68 seconds of black. ⚠ **And it did not read as “the picture is black”, it read as “the TEXT is broken”** (*“audio, image and video show but text not show”*): where the lettering had been there was now a black rectangle, so the user went looking for a text bug. Two separate faults wearing one symptom. ⚠ **INVISIBLE IS NOT OMITTED** — the clip keeps its place, its own name and its row, stays in `placeholders`, and `opacity` is an ordinary editable field, so anyone who wants to see the gap drags it back up. ⚠ **“Bottom” is the lowest row that HAS CLIPS, not row 0** — a sequence whose V1 is empty still lists V1, and counting lanes instead of clips puts the black screen straight back. ⚠ **And SAY IT**: the warning names titles and Graphics explicitly and points at the Text tool, because “attach the missing files” sends someone hunting for files that do not exist. ⚠ **THE LINE THAT SAID “NO IMPORT CARRIES LETTERING” WAS WRONG AND IS NOW E59.** It read: *not `.prproj`, and not the Final Cut Pro XML this app recommends instead… never tell a user their text will come across.* It was believed because only the clip's `<Name>` was ever looked at, and every Premiere title is named “Graphic”. **A `.prproj` carries the words, the font, the size and the position** — see E59. What this row still governs is a clip with genuinely nothing in it. ⚠ **And an Adjustment Layer is no longer imported at all**: it is an empty holder for colour effects no format carries, so the only thing it contributed was a full-length invisible clip the user had to find and delete. Pinned in `tests/interchange_check.py` §8e, which holds both halves at once — a fix that DROPPED the clips would pass “it is not black” and fail that section. | PAKKA |

| **E58** | Reading a clip's SOURCE WINDOW (its in point) out of a `.prproj` | ⚠ **`<InPoint>` IS NESTED, exactly like `<Start>`/`<End>` — and reading it as a direct child is a fault you CANNOT SEE on a timeline of whole takes.** Premiere writes `<AudioClip><Clip Version="18"><InPoint>`, a GRANDCHILD of the clip object; `_prproj_int` reads a direct child, so every clip answered None, `to_project` turned that into 0, and **every clip played its file from the beginning**. ⚠ **E52 CAUGHT THIS SHAPE FOR THE TIMES AND THE FIX WAS NOT CARRIED ACROSS TO THE IN POINT** — same file, same nesting, same function, one line apart. ⚠ **AND IT LOOKED PERFECT ON THE FIRST REAL IMPORT**: four video clips, four separate files, every in point genuinely 0 — the data confirmed “in_ms is 0” and that was read as evidence the reader was RIGHT. It is only visible when ONE FILE IS USED MORE THAN ONCE: the same project's voiceover was one mp3 razored into 23 pieces with the silences cut out, and all 23 restarted the recording (*“har clip audio ka starting hi play ho raha hai… pura audio sunai nahi de raha”*). ⚠ **SO TEST IT WITH A FILE USED TWICE.** A single clip proves nothing — its in point is 0 whether the reader found it or invented it. `tests/interchange_check.py` §8f uses three clips off one file at RISING in points with gaps between them, and there is no value a reader that found nothing could return that would pass. ⚠ **`MasterClip` carries no `<InPoint>`** (verified against a real project: only `VideoClip` and `AudioClip` do, both at the same depth), so searching descendants cannot pick up the bin's in point by mistake. ⚠ **And pin BOTH shapes** — §8c's flat `<Clip><InPoint>` still has to read, or fixing the real file breaks the one another Premiere version might write. | PAKKA |


| **E59** | Reading the LETTERING out of a `.prproj` — a Premiere title is not an empty clip | ⚠ **THE WORDS ARE IN THE FILE. THIS APP TOLD USERS FOR MONTHS THEY WERE NOT, AND A 40-CAPTION SCRIPT WAS RETYPED ON THAT ADVICE.** Every title, caption and lower third in a Premiere project is a clip **named “Graphic”** with no media file — so a reader that looks only at `<Name>` sees forty identical empty clips, makes forty invisible placeholders, and prints *“any LETTERING they held has to be typed again with the Text tool.”* ⚠ **LOOK ONE LEVEL IN: `<VideoFilterComponent><MatchName>AE.ADBE Text`**, reached from the track item through `ClipTrackItem → ComponentOwner → Components → VideoComponentChain`. The text is there TWICE and **which one you trust matters**: `<InstanceName>` is Premiere naming the layer after its own text (free to read, and STALE the moment somebody renames the layer), while the `Source Text` param's base64 `<StartKeyframeValue>` is the string itself. **Read the blob first, `<InstanceName>` only as a fallback**, and build the fixture with the two DISAGREEING or the test cannot tell which one you used. ⚠ **READ THE BLOB FOR ITS STRINGS AND NOTHING ELSE.** It is a FlatBuffer: every field is addressed through a vtable, so field N is at a different byte offset in every record — 83 real records were measured and no two agreed. What never moves is `<uint32 length><bytes><NUL>`; two of those appear in order, **[0] the font's PostScript name, [1] the text**. ⚠ **AND THE SCAN MUST STEP OVER A STRING IT ACCEPTS**, not advance one byte, or a caption's own bytes get re-read as another string's length. ⚠ **ONE GRAPHIC CAN HOLD SEVERAL TEXT LAYERS** — that is what an `AE.ADBE Graphic Group` is (a title with a subtitle). The first draft returned the FIRST text component and stopped; a real clip's group began with an EMPTY layer, so that clip imported as no text at all while its words sat in the very next component. **Read every layer at the shallowest level, and treat an empty one as “nothing here”, never as an answer.** ⚠ **STOP AT THAT LEVEL — DO NOT KEEP DESCENDING.** The MASTER clip behind a graphic carries a chain of its own holding the text it was first made with, and in the reference project **all 82 captions share one master clip**, so a deeper walk returns the same wrong sentence 82 times — a timeline that is full, plausible and completely wrong. ⚠ **THE FILL COLOUR IS NOT IN A `.prproj`.** Looked for in all three places it could be: no colour `<Param>` on the component (its eighteen are Source Text, Transform, Position, Scale, Horizontal Scale, Rotation, Opacity, Anchor Point, start, end, Parent W/H/Rotation); no float in 0..1, no `00 00 80 3F` and no `FF FF FF FF` in any of the 83 blobs; and the `<PremiereFilterPrivateData>` that would carry a serialised appearance is **EMPTY — a `BinaryHash` attribute and no body** (184 of 206 self-closing), because Premiere keeps that payload outside the project XML. **Use this app's default and SAY SO in the report** rather than inventing a white that might have been yellow — and point at the Final Cut Pro XML export, which DOES carry it (E61/E62). Same wall for a shape's `Appearance`, so a shape comes in at the right time and zero opacity. ⚠ **PREMIERE STORES A TEXT LAYER'S LEFT EDGE; THIS APP WANTS THE CENTRE.** The size comes from the plain-XML `Scale` (50% → 45px at 1080p, `PRPROJ_TEXT_SIZE_PER_SCALE = 0.9`) and the constant is **fitted, not guessed**: for a row of centred captions `left + half the line's width` must return to 0.5, and solving that over 78 real captions of 10–46 characters gives 0.4956 ± 0.013, where 0.85 and 0.95 give 0.4866 and 0.5046. Pinned in `tests/interchange_check.py` §8g. | PAKKA |

| **E60** | What an imported Premiere ROW becomes, and what the row is CALLED | ⚠ **A PREMIERE ROW IS NOT A PICTURE ROW.** In a real 8-row project only three held film: two held captions, two a drawn bar, one an Adjustment Layer. Turning every lane into a picture row is where *“audio, image and video show but text not show”* came from — and it also hands the client “this import needs 8 picture rows”, which draws empty rows above the cut, because **picture clips are addressed by NUMBER (`AnimaticFrame.track`) while everything else is addressed by `layer_id`**. ⚠ **SORT EVERY CLIP FIRST, ONCE, THEN RENUMBER**: text → an `AnimaticTextClip` on a text row, shape → an `AnimaticShape` on a shapes row, Adjustment Layer → nothing, picture → a frame; and only the lanes that still hold a picture get a track number, compacted from zero. ⚠ **“Lowest row” for the E57 opacity rule then means the lowest row that holds a PICTURE**, or a caption row under the film claims it and every missing file above goes invisible. ⚠ **AN IMPORT MAY NEVER WRITE TO `CAPTION_LAYER_ID`.** That row belongs to ✨ Auto captions, which **replaces everything on it on every run** — an imported title parked there is destroyed by a transcription the user paid for, silently. Mint `_import_text_N` and lock it on both sides (server never emits it; the client filters it again). ⚠ **AND A CLIP THAT RESOLVED TO A FILE IS ALWAYS A PICTURE**, whatever it is called — somebody's footage really can be named `Adjustment Layer.mov`, and a filename is not permission to delete a shot. ⚠ **NAME THE ROW BY WHAT IS ON IT, NOT BY THE FILE.** Imported rows were named after the sequence — eleven rows all reading “8_MCP_Model Context Prot…”, which distinguishes nothing and repeats the project title; worse, “…audio” appeared on rows of pictures. Use the same words and the same numbering a hand-added row gets (`Video`, `Images`, `Text`, `Text 2`, `Shapes`, `Audio`), skipping names already on the timeline, and let the server say which picture rows are stills (`video_lane_kinds`). ⚠ **AND DO NOT ASSERT A COUNT WHERE A SET WILL DO**: the source check for “every new row is seated” read `count(...) == 2` and went red the day text and shape rows arrived, for a reason that had nothing to do with what it tests. Pinned in `tests/interchange_check.py` §8h and §12. | PAKKA |

| **E61** | A title's COLOUR, OUTLINE and SHADOW — which format carries what, and how a colour is packed | ⚠ **PREMIERE WRITES A COLOUR AS A 64-BIT INTEGER ON A PLAIN `<StartKeyframe>`, AND THAT IS THE KEY TO EVERY COLOUR A `.prproj` DOES GIVE UP.** `18374686479671623680` is big-endian `ff 00 00 00 00 00 00 00` — four **16-bit** channels in **A,R,G,B** order, each holding its 8-bit value in the **HIGH** byte, so white is `0xff00` per channel and **not** `0xffff`. Every component that OWNS a colour writes it that way: `Shadow Color`, `Key Color`, Tint's `Map Black To` / `Map White To`. ⚠ **THE TEXT FILL IS OWNED BY NO COMPONENT, WHICH IS EXACTLY WHY IT IS MISSING.** Before concluding that again, know what was already searched, because it was expensive: **5104 text records across 127 real projects and two Premiere versions**; the `AE.ADBE Text` component has the same **22 parameters** in all of them and not one is a colour; the `Source Text` FlatBuffer's document table carries a fonts vector, a runs vector and a handful of sizes, and its run table has exactly **two** fields (the string and one number); and `<PremiereFilterPrivateData>`, which would hold a serialised appearance, is **empty — a `BinaryHash` attribute and no body** (184 of 206 self-closing), because Premiere keeps that payload outside the project XML. **Do not go looking again; extend this row if you find it.** ⚠ **BUT THE DROP SHADOW IS RIGHT THERE, AND IT IS NOT A RARE EXTRA** — in two real projects **every one of 194 text clips carried an `AE.ADBE Drop Shadow` in its own component chain**. Read `Shadow Color`, `Opacity`, `Direction`, `Distance`. ⚠ **THREE CONVERSIONS, EACH OF WHICH LOOKS LIKE A RENDER BUG IF YOU GET IT WRONG:** a drop shadow's `Opacity` runs **0…255** (a clip's runs 0…100 — using 100 gives 2.45 and the model 422s the whole import); `Direction` is degrees clockwise from **STRAIGHT UP** while `shadow_angle` is clockwise from the **RIGHT**, so subtract 90 or every shadow is tilted a quarter turn; and `Distance` is **PIXELS** while `shadow` is a fraction of the **FONT SIZE**, so the conversion needs each caption's own size. ⚠ **`Shadow Only` MEANS NO LETTERS ARE DRAWN** — this app has no such mode, so drop the whole shadow and keep the title, rather than importing a smear. ⚠ **AND THE ANSWER TO "WHY IS MY TEXT WHITE?" IS A DIFFERENT EXPORT.** An `xmeml` writes `fontcolor` as a plain `<alpha>/<red>/<green>/<blue>` block and an outline as `linewidth` / `linecolor` — so **Final Cut Pro XML carries what a `.prproj` cannot**, and the import's own warning says so instead of leaving the user at a dead end. ⚠ **Channels may be 0…255 or 0…65535** depending on the exporter: SCALE, never clamp, or every 16-bit colour becomes pure white. Pinned in `tests/interchange_check.py` §8i and §8j. | PAKKA |

| **E62** | Reading the lettering out of a Final Cut Pro XML (`xmeml`) | ⚠ **A TITLE IS A `<generatoritem>` — A SIBLING OF `<clipitem>`, NOT A KIND OF IT.** A reader that walks `track/clipitem` never sees one, which is why every title in an `xmeml` went missing without a single warning. ⚠ **AND IT HAS NO `<file>`**, so even once it is reached, a `read_clip` that refuses items with nothing to resolve throws it away a second time. Both had to change: look for the lettering BEFORE the file key decides, and keep a file-less item when it turns out to be a title. ⚠ **CHECK `<effecttype>generator</effecttype>` FIRST.** An ordinary filter carries `<parameter>` elements too, and mining any effect for a string turns a colour-correction preset into somebody's subtitle. ⚠ **AND A GENERATOR WITH NO WORDS IS STILL DROPPED** — a colour matte and bars-and-tone are generators, and keeping them puts a nameless empty clip on the timeline for every one this app cannot draw. ⚠ **`<origin>`'s `vert` COUNTS UPWARDS AND `y` COUNTS DOWN**; read straight across, every lower third lands at the top of the frame, which reads as the import having IGNORED the position rather than inverted it. ⚠ **`fontsize` IS PIXELS OF THAT SEQUENCE**, and `size_px` here always means pixels at 1080p — a 36px title in a 720p sequence is not a 36px title here. ⚠ Emit the same shape the `.prproj` reader emits (`{"kind": "text", "texts": [...]}`), so `to_project` cannot tell the two formats apart and E60's routing is written once. Pinned in `tests/interchange_check.py` §8j. | PAKKA |

| **E63** | A file the import could not find must say WHICH FOLDER it lived in | ⚠ **NAMING THE MISSING FILE IS NOT SOMETHING A USER CAN ACT ON.** A real import lost exactly three files out of twenty-eight and the report gave no way to fix it: the VOICEOVER sat inside the project folder and arrived on all 23 of its clips, while the music bed and a logo lived inside **another project's folder** — which is ordinary, because a logo and a music bed are reused across a whole series. The user attached the only folder there was any reason to attach, read *“tech_oasis-….mp3 did not arrive”*, and could only conclude the app cannot take music. ⚠ **BOTH READERS ALREADY KNOW THE ANSWER**: `files[key]["pathurl"]` is the full path the editor itself wrote — a Windows path from a `.prproj`, a percent-encoded `file://` URL from an `xmeml`, so **unquote it or the user goes looking for a folder called `Shared%20Art`**. `report["missing"]` is one row per FILE with `{name, folder, kind, clips}`, and `placeholders` stays one entry per CLIP because that is what the gaps on the timeline are counted from. ⚠ **ONE ROW PER FILE, NOT PER CLIP** — a logo used on two clips printed its own name twice and read as two different broken files rather than one folder nobody attached. ⚠ **AND MARK THE SOUNDS.** A missing picture becomes a labelled card on the timeline; a missing sound becomes nothing at all (E45), so the two are not the same loss and one list must not print them as if they were. ⚠ **“NO SOUND WAS BROUGHT IN” WAS A LIE WHENEVER SOME OF IT WAS** — that sentence fired off the count of FAILED clips, so it printed while 23 clips of voiceover sat on the timeline, which makes every other line of the report suspect too; count what actually landed. ⚠ The dialog groups the list by folder and both pickers ADD, so the fix is one more click per folder. Pinned in `tests/interchange_check.py` §8k, plus a break-guard on the dialog. | PAKKA |

| **E64** | The import dialog must SURVIVE being acted on — attaching footage is not a reset | ⚠ **THE REPORT WAS THROWN AWAY AT THE EXACT MOMENT IT WAS BEING ACTED ON.** E63 made the report name the FOLDER a missing file lived in; `addMedia` then called `setRead(null)`, so pressing “…or a whole folder” to go and fetch that folder **wiped the only place the folder was written down**. On screen it reads as being sent back to the start — *“mai wapas aa gaya, ye nhi ki wahi se chalu hua”*. ⚠ **AND THE WAY BACK WAS A REFUSAL, WHICH IS WHERE THE IMPORT WAS ABANDONED.** With the report gone the footer flips to “Read the file” — `readFile()`, the STRICT read, which for a `.prproj` is the 415 this app refuses with on purpose — and “Try to read it anyway” is hidden once it has been taken (`guessed`), so there was **no second offer and no exit but closing the dialog**: *“fir kuch nhi hua to mai bas usko chhor kar import kiya”*. **The re-read must carry the flag the first read used.** ⚠ **KEEP THE REPORT AND MARK IT OUT OF DATE** (`stale`): dimmed, banner saying it is from the last read, and the gold button becomes **“Read the file again”** — which also stops a report read WITHOUT the new footage from being added over it. ⚠ **AND TAKE ONLY WHAT WAS ASKED FOR.** The folder E63 sends somebody to is another film's project folder, full of other films' media — *“us folder mai aur bhi music tha”*. Once a report names what is missing, the picker keeps only those files; **falling back to everything when nothing matches**, or a first pick of ordinary footage would silently upload none of it. ⚠ **THIS WAS ONLY EVER VISIBLE AS A SEQUENCE** — every piece of state was right when it was set; what was wrong was what the third press did to the second press's result. Pinned in `tests/editor_project_import_check.py` (Playwright, no backend), with cheap source guards in `tests/interchange_check.py`. | PAKKA |

| **E65** | ANY dialog in this app — and the one you are about to write | ⚠ **A CLICK THAT MISSES THE CARD MUST NOT THROW THE WORK AWAY. NO `.modal-overlay` MAY CARRY AN `onClick` — ALL 31 OF THEM DID.** Each dialog was written by copying the one beside it, so one convenience spread everywhere, and a dialog's contents are saved NOWHERE: a read that took minutes, a half-typed name, the folder list E63 prints, the `guessed` flag that is the only route back for a `.prproj`. *“galti se mera mouse pop up se bahar screen ke click hua mera popup cut gaya … mera mehnat bekar ho gaya”* — a dozen steps into a real import. ✕ and Cancel are the way out (every dialog here has a ✕, and `dialog_frame_check.py` asserts that too — removing the backdrop from a dialog with no ✕ would be a trap, not a fix). ⚠ **Escape stays as it was EXCEPT on the import dialog**, where it is one keystroke from the typing and cost the same minutes; elsewhere it is a deliberate press and `Timeline`, `TitleInput` and the lightbox need theirs. ⚠ **AND A DIALOG THAT CANNOT BE DISMISSED MUST BE MOVEABLE**, or it sits on top of the very thing it is being checked against. **`client/src/dialog_move.js`, installed once from `App.jsx`, is the ONE implementation — do not write a second one inside a dialog.** It finds dialogs by `.modal-overlay`, so a dialog written next year gets it for free; the handle is the **heading** (a card-wide grab eats text selection) and never a button or a field; it drags by `transform` with **pointer capture**, or a fast drag off the card leaves the dialog stuck to the cursor; the offset lives **on the DOM node**, which is what makes “it opens in the middle again” free — a module-level position comes back where it was last shoved and reads as broken; and it is **clamped so a strip always stays on screen**, because a card dragged past an edge is unreachable, ✕ included. ⚠ **NOTHING IN A DIALOG'S OWN JSX SAYS ANY OF THIS** — the warning lives beside `.modal-overlay` in `soon-upgrade.css`, which is the block anyone changing a dialog's surface opens. ⚠ **AND A DIALOG THAT SCROLLS MUST KEEP ITS ✕ AND ITS HANDLE ON SCREEN**: `.modal-close` is positioned against the card, so on the import dialog it scrolled away with the report and left a reader with no exit at all — hence `.an-xchg-bar`, a sticky title bar carrying both. ⚠ **AND THE ✕ SITS IN THE CORNER THE WAY A WINDOW'S CLOSE BUTTON DOES — IN EVERY DIALOG, NOT JUST THIS ONE.** Asked for twice, the second time with a picture of a browser title bar: *“x close buttun ko aur upar kar sakte hai kya … dekho chrome mai kaise hai corner mai”*, then *“mai chahta hun upar upar kona mai rahe … aur sare popup mai kar dena”*. At `top: 0.8rem` it lined up with the heading and read as a control belonging to the TITLE; `.modal-close` is now `0.35rem / 0.45rem`, which is ONE rule for all 31 dialogs. ⚠ **Keep the padding when you move it**: the glyph is a single character, and since this rule made ✕ the way out it is the one control that must stay easy to hit. On the import dialog the same offsets are re-applied against `.an-xchg-bar`, which is sticky and therefore positioned — that is what keeps the ✕ in the corner AND on screen while the report scrolls. ⚠ **AND A STICKY BOX IS PINNED BY ITS MARGIN BOX, WHICH IS HOW THE CORNER FIX LOOKED LIKE IT HAD NOT LANDED.** The bar is pulled to the top of the card by a NEGATIVE top margin, and `top: 0` cancelled exactly that, parking bar and ✕ a whole card-padding lower with every grep about them still green — *“kaha hua niche hi to dikh raha hai”*. The sticky offset must MATCH the negative margin (`top: -1.35rem`). Measured in pixels by `tests/editor_project_import_check.py` (corner, hit size, and still there after scrolling), and **proved to fail on `top: 0` first**. Pinned in `tests/dialog_frame_check.py` (source, 31 dialogs, detector proved on both shapes the handler was written in) and `tests/dialog_move_browser_check.py` (Chromium: the backdrop click, the drag, the clamp, the ✕, and re-centring on reopen) — **a source check cannot see a listener fire**. | PAKKA |
| **E66** | Any control the user is meant to FIND — buttons, and the ghost button most of all | ⚠ **A BUTTON MUST READ AS A BUTTON BEFORE THE POINTER IS ANYWHERE NEAR IT.** `.btn.ghost` carried `border-color: transparent`, so “Add the footage”, “…or a whole folder” and **every Cancel in the app** were words sitting on a panel: *“buttun merge ho ja raha hai bg mai … thoda aur stroke/highlight karo … agar aur kahi hai to usko v kar do”*. It now takes `--btn-border` like every other button. ⚠ **WHAT MAKES A GHOST A GHOST IS THE MISSING FILL, NOT THE MISSING OUTLINE** — `background: transparent` stays, so it is still visibly the quieter of two buttons side by side. ⚠ **AND `--btn-border` IS ITS OWN VALUE, NOT `var(--border)`.** `--border` is the edge of a PANEL, a line whose job is to be barely there; on the dark page a button wearing it has no edge at all, which is how these went invisible while every rule about them looked right. Dark is `#3c4360` now; light was already firm (`#c7cedd`). ⚠ **THE HOVER WAS NEVER THE PROBLEM AND MUST SURVIVE** — *“jab mai jata hun tab highlight ho hota hai ye badhiya hai isko rakhne do”*: `.btn:hover` still lifts the edge to `--primary` and drops `--panel-2` in behind it. ⚠ This matters most inside a dialog, because since **E65** a button is the only way out of one — a Cancel nobody can see is a hidden exit. Pinned in `tests/dialog_frame_check.py` §5. | PAKKA |

| **E66** | WHERE an imported Premiere caption sits — it is on `Motion`, not on the text component | ⚠ **`AE.ADBE Text`'s OWN `Position` IS WHERE THE WORDS SIT INSIDE THE GRAPHIC, NOT ON THE SCREEN.** For a caption built from Premiere's own template that is ~0.52 — the middle — so a reader that trusts it puts every caption in the centre of the frame however they were laid out. A real 40-caption import did exactly that and it read as *“the position was ignored”*. What actually places the clip is **`AE.ADBE Motion`**, the Position/Anchor Point every Premiere clip carries: **78 of 82 captions in the reference project sit at `0.5:0.9211`** and the other four are title cards at `0.5:0.5`. ⚠ **AND THERE ARE TWO TRANSFORMS, BOTH OF WHICH APPLY.** `AE.ADBE Geometry2` is the **Transform effect** added by hand (the user who reported this had keyframed one onto every caption) and it STACKS on Motion rather than replacing it — in that project it is identity, so reading only Motion looks right on this film and is wrong on the next. **Sum them.** ⚠ **EACH CONTRIBUTES `Position - Anchor Point`, AN OFFSET** — zero for an untouched clip, which is what makes it safe to add to every caption — and neither may set the walk's `found` depth, because a Motion component is on EVERY clip and would stop the search before it reached the words. ⚠ **AND THE VERTICAL IS AN EDGE, EXACTLY LIKE THE HORIZONTAL IN E59**: Premiere stores the BASELINE and this app draws from the block's CENTRE (`draw_texts`: `top = cy - height / 2`). **Measured against a real export**, not guessed: episode 7's stored numbers compose to y = 0.9430 and the rendered .mp4's lettering has its band bottom at 0.944 and its visual centre at 0.928 ± 0.003 over five frames — at `size_px` 45 in a 1080-high frame that gap is **0.36 em**, which is half a cap height and therefore typography rather than a fudge. ⚠ **CLAMP TO -1..2**, what `AnimaticTextClip` accepts: a caption Premiere parked off-screen must not 422 the whole timeline. ⚠ Still NOT read: Motion's own `Scale` (the size still comes from the text component), and only the FIRST keyframe of anything. Pinned in `tests/interchange_check.py` §8l. | PAKKA |

| **E67** | A PNG with a transparent background must not be stored, or drawn, on BLACK | ⚠ **`convert("RGB")` PAINTS EVERY TRANSPARENT PIXEL BLACK, AND THIS APP DID IT IN THREE PLACES.** Both upload paths (`upload_images` and `_store_import_media`) flattened on the way IN, so the alpha was destroyed on disk; `_picture_layer` flattened again on the way OUT; and — found only when the user tested the first two fixes and reported the SAME fault — **the monitor did it a third time**. `ProgramCanvas.jsx` drew every picture with `useAlpha: false`, quoting `_picture_layer`'s `convert("RGB")` as its reason long after `_picture_layer` had stopped doing it. ⚠ **A STALE COMMENT IS HOW A FIX SHIPS HALF-DONE**: the first two changes were real, the exported MP4 was correct, and the user still saw a black band — because the only picture they were ever looking at was the preview. ⚠ **THE PREVIEW AND THE EXPORT ARE ONE PICTURE OR THEY ARE A BUG REPORT.** Measured on the letterhead that reported it: 487,876 of its 593,280 pixels are fully transparent and the RGB under every one of them is `0,0,0`, so forcing it opaque draws a solid black rectangle. The monitor half is asserted on the SOURCE in `tests/effects_check.py`, because `effects_parity_check.py` cannot run without headless-gl and a fault nobody can catch locally is the one that ships. It was invisible for a photograph and ruinous for the one thing anybody uploads a PNG for: *“mera logo ka background transparent tha but yaha pe black aaya”* — a cut-out logo arrives as a black card with a logo printed on it, and **on a row above the film that black card is a lid over the whole picture**, which is E57's fault wearing a different hat. ⚠ **EITHER HALF ALONE FIXES NOTHING** — honouring alpha in the renderer buys nothing when the stored file no longer has any, and keeping it at upload buys nothing when the renderer throws it away. Both, or neither. ⚠ **THE OLD COMMENT'S CAUTION WAS RIGHT AND ITS PREMISE IS GONE.** It read *“honouring it now would silently change every animatic that has ever used a cut-out still”* — but every picture stored before this was flattened to RGB, so it has no alpha left to honour and nothing existing can change. ⚠ **ASK THE SOURCE, DO NOT ASSUME.** A JPEG has no alpha and an RGBA file with solid alpha is a photograph in a bigger box; four channels for either is a third more disk on every frame of every project. And a **palette PNG hides its transparency in `info`, not in its mode** — `im.mode == "P" and "transparency" in im.info`. Pinned in `tests/effects_check.py` (the render half) and a source guard in `tests/interchange_check.py` (the upload half, which cannot be imported — see G13). | PAKKA |

| **E68** | The files ALREADY in the project count as arrived | ⚠ **AN IMPORT SAW ONLY WHAT WAS ATTACHED TO ITS OWN REQUEST.** Nothing looked at the project's own Media pane, so a project already holding every one of its 27 files still turned every clip into a placeholder unless all 27 were picked again — and the obvious repair, **dragging the one missing file into Media and importing again, could not work**: the card sat on screen while the report said the file had not arrived. It also meant a second import stored a **second copy of everything** (a real job on this machine holds 52 files for 27 names). `interchange.media_library(assets)` builds the fallback; the route tries the freshly attached files FIRST. ⚠ **FRESH MUST WIN** — the library is a fallback, never an override, or re-attaching a shot somebody deliberately re-rendered silently keeps the stale cut. ⚠ **A COLOUR CARD IS NOT MEDIA**: an asset with no `upload_id` has no file behind it and must never answer for a filename, or a clip resolves to a rectangle the timeline then treats as footage. Same for an asset whose upload never finished. ⚠ **KEYED BY NAME AND BY STEM**, exactly as the attached files are — an app that transcodes on the way out leaves `shot_03.mov` in the project file and `shot_03.mp4` in the library. ⚠ **AND SAY IT**: a clip that resolved to a file the user did not attach this time is a good outcome and a surprising one, and silence is how somebody ends up unsure which copy of a file their cut is using. ⚠ **THE PURE HALF LIVES IN `interchange.py` ON PURPOSE** — `server/animatics.py` cannot be imported by a test without dragging in config (G13), so logic that needs testing does not belong in the route. Pinned in `tests/interchange_check.py` §8m. | PAKKA |
| **E69** | A missing file whose PATH the report is already printing | ⚠ **IF WE KNOW WHERE IT IS, GO AND GET IT — DO NOT MAKE THE USER RETYPE OUR OWN ANSWER INTO A FILE DIALOG.** E67 made "missing" actionable by naming the folder; that folder then sat on screen while the user walked to it by hand. *"tum khud usko pickup kyun nhi kar rahe ho jab tumne location mil raha hai to user se kyun karwa rahe ho."* When this server runs on the same computer as the editor that wrote the project, the recorded path is real and the import opens it itself. `interchange.local_media_paths(incoming, exists=…)` is the pure half — it unquotes `%20`, strips `file://localhost/` / `file:///` / `file://`, and **puts the leading slash back** for a Mac path (or the path goes relative and resolves against the server's own directory). ⚠ **MEDIA EXTENSIONS ONLY, AND THAT IS THE SECURITY BOUNDARY, NOT TIDINESS.** A `<pathurl>` is text inside an UPLOADED document, so a hand-written project file can name any path on the disk; `media_kind` is the whitelist and is the only reason this is safe to run. ⚠ **A RELATIVE PATH IS REFUSED**, never resolved — it is relative to a machine we do not have. ⚠ **LOOPBACK ONLY** (`_may_read_local_media`, `API_IMPORT_LOCAL_MEDIA=auto`): on a hosted server that string is somebody else's path and reading it would answer one user's import with another user's files. ⚠ **LAST IN THE QUEUE** — attached files, then the project's own Media (E68), then the disk; disk first would store a fresh copy on every import, which is the duplicate storage E68 exists to stop. ⚠ **AND SAY IT**: a file that arrived without being asked for means the app opened something on the user's own disk, and they are entitled to read which. Anything still not found stays in the report with its path, for the user to attach by hand. Pinned in `tests/interchange_check.py` §8n, with the whitelist, the relative-path rule, the unquote and the leading slash each **proved to fail** when removed. | PAKKA |
| **E70** | A refusal the user has no way to act on | ⚠ **DO NOT MAKE SOMEBODY PRESS THROUGH A "NO" WHOSE ONLY ANSWER IS THE BUTTON UNDERNEATH IT.** A `.prproj` used to mean a red panel ("export a Final Cut Pro XML instead"), then a second button ("Try to read it anyway"), then a **second upload of the same 27 files** before anything appeared. *"ye red text dikhne ka zaroori nahi hai user ko."* The advice was sound and the user could not take it — they are here **because** they cannot open Premiere. The dialog knows the extension, so it asks for the best-effort read on the FIRST press: one upload, no red screen, straight to the report. ⚠ **THE ROUTE'S REFUSAL IS UNTOUCHED** — an unflagged `.prproj` is still a 415, because that is the API's answer to a caller that has not said it wants a guess. What changed is that the CLIENT knows its own file. ⚠ **AND THE HONESTY HAD TO GO SOMEWHERE, NOT AWAY**: the result is badged **BEST GUESS** for as long as it is on screen and the first line of `warnings` still names File › Export › Final Cut Pro XML — the warning moved to where the user is actually looking instead of standing between them and it. ⚠ A dialog that drops the badge turns this into a guess presented as a read: `editor_project_import_check.py` asserts one request, no `.error` on screen, and the badge visible; the stub still refuses an unflagged read, so a regression to the two-step **fails** instead of quietly costing an extra upload. | PAKKA |
| **E71** | The same bytes arriving a second time | ⚠ **STORE ONCE PER PROJECT, NOT ONCE PER IMPORT.** Every imported file used to get a fresh `upload_id` and a fresh copy on disk, which leaked twice over: WITHIN one import (the pickers add by name across several folders — E64 — so a file picked from two of them was written twice and only one copy was ever referenced) and ACROSS imports (re-reading the same cut, which is the ordinary thing to do after fetching a missing folder, stored a second copy of all 27 files). A real job on this machine holds **52 files for 27 names**, and three fixes in a row have had to work around that pile. `_store_import_media` is content-addressed now: `sha256` of the incoming bytes against a per-project ledger. ⚠ **HASHED ON THE WAY IN, NEVER OFF THE DISK** — an imported picture is re-encoded to PNG, so the stored bytes are not the bytes that arrived and a walk of the media folder could never match a second import of the same JPEG. ⚠ **NAME IS NOT IDENTITY**: deduping by filename would hand a clip the WRONG picture, which is worse than the duplicate. ⚠ **AND THE LEDGER IS NOT A PROMISE ABOUT THE DISK** — it outlives the files it names, so the file's existence is checked every time; handing back an id whose file was deleted resolves a clip to nothing. ⚠ **PER PROJECT**, because an `upload_id` only resolves inside the animatic that owns the folder. ⚠ Written temp-file-then-`os.replace`: a truncated ledger reads as empty and every dedupe the project had learned is gone. A lost or corrupt ledger costs a duplicate, never an import. Said out loud in the report, or a 27-file upload that stores nothing reads as the upload having failed. Pinned in `tests/import_dedupe_check.py`, with four guards proved to fail. | PAKKA |
| **E72** | An animation in somebody else's project file | ⚠ **`<StartKeyframe>` IS THE RESTING VALUE, NOT THE FIRST KEYFRAME — AND EVERYTHING AFTER IT WAS BEING THROWN AWAY.** The `.prproj` reader read one value per parameter, so a shot that pushed in over four seconds imported frozen and `Scale` was not read anywhere at all: 21 keyframed shots and 78 fading captions in one real project, most of what a viewer would call its motion. The real keys are in `<Keyframes>` as `ticks,value,…;…`. ⚠ **`<StartKeyframe>` MUST NOT BE EMITTED AS A KEY**: it is stamped a hundred hours before the film, so adding it puts a snap on the head of every clip (100 → 80 in thirteen milliseconds, in the file this was read from). ⚠ **THE TICKS ARE IN THE CLIP'S SOURCE CLOCK — `<InPoint>` IS THE ZERO.** Proved: four clips at 0.0s, 3.6s, 7.7s and 9.5s on the timeline carry identical keyframe ticks and share an `<InPoint>`; rebasing against the TIMELINE would put every key an hour past the end of its own clip. ⚠ **A WRONG BASE DOES NOT FAIL — IT PARKS EVERY KEY AN HOUR AWAY**, where the value holds and the clip merely looks un-animated, so a track that lands nowhere near its clip is REFUSED and named. ⚠ **WITH UNIFORM SCALE ON, PREMIERE WRITES `Scale Height` AND PARKS `Scale Width` AT 100** — reading Width as a second axis reports every such clip as squashed. ⚠ **SCALE TRAVELS AS A RELATIVE PUSH WHEN — AND ONLY WHEN — THE REAL SIZE CANNOT BE WORKED OUT.** Premiere measures Scale against the FILE's own pixels while this app fits every picture to the frame, so its number cannot be used as-is. Where the pixel size is available the size IS carried across; that is **E73**, and this fallback (the track divided by its own value at the clip's start) is what remains for everything E73's sum cannot reach. ⚠ **THE CLIP'S OPACITY IS ITS OWN COMPONENT** (`AE.ADBE Opacity`), not a parameter of Motion — a reader that opens only Motion and Geometry2 cannot see a single fade. ⚠ **AND IT MUST NEVER TOUCH A PLACEHOLDER CARD**: a card above the bottom row is parked at `opacity: 0` on purpose, and an imported fade writing 1.0 over it is 68 seconds of black (E45/E5x). ⚠ Captions take only `opacity` and `scale` — a position track measured on the graphic alone would drop the lettering's own offset inside it and jump every caption on its first frame. Every curve is read as a straight line and that is said out loud. Pinned in `tests/interchange_check.py` §8o, with six guards proved to fail. | PAKKA |
| **E73** | A clip that is not the size the frame is | ⚠ **PREMIERE'S `Scale` MEANS SOMETHING HERE, AND THE MISSING TERM WAS THE FILE'S OWN PIXEL COUNT — WHICH IS IN THE PROJECT FILE.** The reader used to say a fixed Scale could not be carried across at all, so every picture arrived FITTED TO THE FRAME. On a real import that shipped a 1920×309 letterhead — sitting at `Scale 24` in Premiere, a small mark in the top-left corner — as a band **four times too wide with its left half off the screen**, over the whole 68-second film: *"logo sahi se set nahi hua aur transparent bhi nahi ho kar aaya"* (the transparency was fine — E57/E71 had already fixed that; what looked like a black smear across the top was the logo's own lettering at 4×). ⚠ **THE SUM IS ONE LINE AND BOTH HALVES ARE `<FrameRect>`:** the `VideoClipTrackItem`'s own rect is the SEQUENCE frame, a `VideoStream`'s is the FILE — so `source_w × scale / frame_w` (Premiere's share of the frame) set equal to `our_scale × min(1, source_aspect/frame_aspect)` (this app's, after "contain") gives the number. ⚠ **THEY ARE TOLD APART BY TAG, NEVER BY DEPTH** — both are `<FrameRect>`, so "shallowest wins" answers the sequence size for both, the sum divides a number by itself, and every clip reports as untouched. ⚠ **AN UNTOUCHED `Scale` OF EXACTLY 100 IS LEFT ALONE, AND THAT IS THE GUARD.** Premiere has two ways to make a small file fill a frame: *Set to Frame Size* writes the fitting number (114.77 for a 1672-wide slide), *Scale to Frame Size* resamples the media and leaves Scale at 100 — and nothing in the file tells that clip apart from one nobody ever touched. So 100 keeps this app's fit, which is what BOTH of those clips look like, and only a Scale somebody moved is converted. ⚠ **THE KEYFRAMES REPLACE ONE COMPONENT'S RESTING VALUE, NOT THE PRODUCT.** Motion's 114.77 and a hand-added Transform's 100 multiply; the track overwrites the Transform's, so the owner's resting value is divided back out before the track is multiplied in. Get it wrong and the reference project's slides zoom FROM full frame instead of TO it. ⚠ **AND THE STATIC IS TAKEN AT THE CLIP'S START, NOT AT REST** — a keyframed clip rests at a value a hundred hours before the film (E72), which is the size it never plays at. Measured on the reference project: the logo lands on 0.24, 720p footage at `Scale 150` and a 1672-wide slide at `Scale 114.77` both land on **1.0** — nothing that already looked right moves. Both counts are said out loud in `warnings`. Pinned in `tests/interchange_check.py` §8p, with three guards proved to fail. | PAKKA |
| **E74** | Duplicate media a closed leak already wrote | ⚠ **CLOSING A LEAK DOES NOT REMOVE WHAT IT SPILLED, AND THE ONLY WAY TO REMOVE IT IS TO DELETE SOMEBODY'S FOOTAGE.** E71 stopped fresh copies being written; the copies already on disk stay until something sweeps them. `cleanup_media.py` is that sweep, and every rule in it is about what it REFUSES to touch. ⚠ **DRY RUN IS THE DEFAULT; `--apply` IS THE ONLY WAY TO DELETE ANYTHING.** ⚠ **NO PROJECT RECORD, NO SWEEP** — the biggest hazard in the whole script: an empty reference set is legitimate, so a media folder whose job cannot be loaded would read as "an empty project" and make every file an orphan. Those folders are skipped whole. ⚠ **IDENTICAL BYTES, NEVER AN IDENTICAL NAME** (E71's rule, from the other end) — two files called `logo.png` can be two pictures, and handing a clip the wrong one is worse than the duplicate. Hashed off the DISK here, unlike the import ledger, because that is the question being asked. ⚠ **A REFERENCED FILE IS NEVER DELETED**, even as one of several copies — and the id sweep is RECURSIVE on the key, because upload ids sit on frames, assets, audio tracks, Veo clips and overlays and that set grows with the app; a place it did not know about is a file it would delete. ⚠ **THE LAST COPY IS NEVER DELETED**, so a picture sitting unused in the Media pane cannot vanish. The import ledger is pruned to match, temp-file-then-`os.replace` (E71). Pinned in `tests/media_cleanup_check.py`. | PAKKA |
| **E75** | Anything positioned by its own x/y in the monitor | ⚠ **AN INLINE `transform` BEATS A STYLESHEET ONE — AND THAT IS HOW A CAPTION LOSES ITS CENTRE.** `.an-text-free` makes x/y the CENTRE of a free-placed caption with `transform: translate(-50%, -50%)`; `captionStyle` writes an inline `transform: scale(…)` for a zoomed one, which DELETED that translate and dropped the caption's top-left corner onto x/y instead. Every imported caption jumped right and down — *"text perfect placment nhi hai, primeir mai niche center mai tha par yaha pe side dikha raha hai"* — and it was permanent rather than intermittent because a Premiere caption carries a 1.0 → 1.1 pop, so `scale != 1` at essentially every frame. The fix is to COMPOSE: `translate(-50%, -50%) scale(s)`, which is also the order that lands the scaled block's centre on x/y (a percentage translate resolves against the un-scaled border box). ⚠ **THE EXPORT WAS RIGHT THE WHOLE TIME.** `draw_texts` places a free block at `cx - box_w / 2` and knows nothing about CSS, so this was the MONITOR disagreeing with the film — the third such fault in two days, after the forced-opaque frame (E67). **THE PREVIEW AND THE EXPORT ARE ONE PICTURE OR THEY ARE A BUG REPORT.** ⚠ **A ZONE CAPTION MUST KEEP ITS BARE `scale()`** — those are laid out by flex, and a translate there shifts every ordinary caption in the app. Pinned in `tests/effects_check.py`, on the source, because the pixel test for it needs a browser that suite does not run. | PAKKA |
| **E76** | A drawn shape arriving from another editor | ⚠ **ITS SIZE AND ITS FILL ARE IN THE `.prproj` AFTER ALL, AND SO IS THE ROW IT BELONGS ON.** A Premiere shape used to arrive as an invisible placeholder, so the white card somebody put *behind* their pictures — the whole reason a letterboxed slide reads as full-screen — vanished and the film played on black. Three findings, each measured: **(1) the payload is in the file** — Premiere writes each blob ONCE inline and every later param carries only its `BinaryHash`, so a reader looking at a repeat sees an empty element and concludes the bytes are elsewhere; `_prproj_binaries` collects them per document. **(2) `Path` needs no vtable** — `<int32 kind><int32 points>` then 28 bytes a point; 242 paths across 129 real projects all parse at that stride and 230 are axis-aligned rectangles. **(3) `Appearance` needs one and the layout does not move** — all 217 shape blobs are exactly 404 bytes and 216 carry identical constant runs each side of the fill; the fill was pinned by DIFFERENCE (two shapes in one sequence differ in seventeen bytes, of which three read `ff ff ff` and `00 00 00`). ⚠ **EVERY GUARD FAILS TO THE PLACEHOLDER**: wrong length, wrong signature, wrong pad byte, not a rectangle, wrong point count — because a shape drawn from a misread blob is a full-frame rectangle in a colour nobody chose, over somebody's film. ⚠ **AND READING IT IS ONLY HALF — THE ROW ORDER IS THE OTHER HALF, AND WITHOUT IT THE FIX IS WORSE THAN THE BUG.** Imported rows fall into this app's DERIVED order (pictures → shapes → overlays → text), which puts every imported shape ABOVE every imported picture whatever the sequence said: a background card with its real fill becomes a **lid over the whole film**, E45 in a new colour. `to_project` emits `lane_order` as relative tokens TOP FIRST — the sequence is read bottom-up, so the walk runs backwards — and the client re-bases them exactly as it re-bases `track` and `layer_id`. ⚠ **THE ROWS ALREADY IN THE PROJECT MUST BE APPENDED TO THAT LIST**, or they go unlisted, and `laneRank` ranks an unlisted row ABOVE a listed one — so the import slides underneath the film it was meant to land on. Pinned in `tests/interchange_check.py` §8q (16 checks, offsets written as literals so the test cannot follow the code), with three more guards proved to fail. | PAKKA |
| **E77** | A shape or a card whose Opacity is KEYFRAMED in the file | ⚠ **A CLIP'S FADE IS PART OF ITS COLOUR, AND A SHAPE WAS BEING IMPORTED WITHOUT ONE.** `_import_shape_clip` took only the STATIC opacity, so a card Premiere holds at zero was drawn here fully opaque — *“jo logo ke niche tha uska colour black tha”*. Measured on the reported project: the two shapes on Premiere's bottom track resolve to **one** `Appearance` blob (`BinaryHash` 478a7a16…), so the fill was never the difference — the second one carries Opacity keys `1.0 → 0.0` whose LAST key lands **984 ms before its own first frame**, so Premiere holds it at 0 and its own export is (0, 0, 0) at 56.3 s while this app painted a full-frame white end card over the logo. ⚠ **THE RESTING VALUE COULD NEVER HAVE CAUGHT IT** — `Opacity` on that component rests at **100**; the whole of the fade is in `<Keyframes>`, which is E72 one layer further in. ⚠ **THE SHAPE'S OWN OPACITY MULTIPLIES THE TRACK, NOT JUST THE RESTING VALUE** (a 50% card that fades out runs 0.5 → 0), and ⚠ **`x`/`y` ARE STILL NOT TAKEN** — the move is already inside the box via `_prproj_placement`, so carrying it twice jumps the shape on frame one. ⚠ **AND “READ” IS NOT “VISIBLE”**: `_shape_ever_visible` samples the track only INSIDE the clip's window, because that track carries a 1.0 that is never drawn — answering off any-key-above-zero reports an invisible end card as one that “came across with its real colour”. The report now says both: how many shapes were read, and how many of those are invisible *because they are invisible in Premiere too*. Pinned in `tests/interchange_check.py` §8r (18 checks, four guards proved to fail). | PAKKA |
| **E78** | Concluding that a value is NOT in a binary file | ⚠ **A BYTE SEARCH CANNOT DISPROVE THE PRESENCE OF A VALUE IN A SCHEMA'D BINARY — THIS RULE WAS PAID FOR BY GETTING IT WRONG TWICE ON THE SAME FIELD.** A caption's fill was written off as unreadable in E59, then RE-CONFIRMED as unreadable after E76 by decoding **every** payload in a real project (345 of them, 73 kB) and searching each one for the known colour as bytes AND as a float triple. It was in the file the whole time, four bytes from the end of a blob that search had already decoded. **`#FFBC0F` is stored as `bc 0f`** — the red channel equals its default of 255 and a FlatBuffer does not write a field equal to its default, so `ff bc 0f` appears nowhere and never could. ⚠ **THE CORPUS EVIDENCE WAS WRONG THE SAME WAY**: “90 records all read `00 00 00 00`, a field that never varies is not a colour” was measured at a guessed offset in a variable-length structure. ⚠ **WHAT SETTLED IT WAS GROUND TRUTH AND A WALK** — the user photographed Premiere's own Properties panel (Fill #FFBC0F, Stroke black, Width 5.0), and the format was then walked field by field instead of grepped. Before writing “not in the file” anywhere: get one value you KNOW, and walk the structure. Absence of a byte pattern is not absence of the field. See E80. | PAKKA |
| **E79** | What a row in the timeline gutter is CALLED | ⚠ **A ROW IS NAMED FOR WHAT IT IS, NOT FOR WHAT IS LYING ON IT.** Video, Images, Text and Shapes rows are named by their layer; an audio row with a layer record was named by whichever FILE it held, so one column said two different kinds of thing — *“audio layer pe name aa raha hai audio clip ka magar aana chahiye audio layer only jaise baki sab”*. It shows worst straight after an import, which mints one row per Premiere audio track: three rows all reading `6_AA_AI_v.mp3`. ⚠ **THE FILENAME IS NOT LOST, IT IS WHERE A FILENAME BELONGS** — every audio clip already carries `title={filename — length}` in `Timeline.jsx`, so it is one hover away on the clip that actually holds it, and a row holding two files can no longer claim the name of one of them. ⚠ **THE FILE-GROUPED ROWS KEEP THEIRS**: those have no layer record and therefore no name of their own. Pinned on the source in `tests/effects_check.py`. | PAKKA |
| **E80** | A caption's FILL and OUTLINE out of a `.prproj` | ⚠ **THEY ARE IN THE `Source Text` BLOB, WHICH IS A FLATBUFFER.** Layout, walked against a caption whose Fill, Stroke and Stroke Width were all known from Premiere's own panel: `[12-byte header: uint32 length, 4 spare, magic 44 33 22 11]` → root table field 0 → document → field 0 is a VECTOR OF RUNS → run field 0 is the text and field 1 the STYLE table → style field 2 = fill colour table, field 4 = stroke colour table, field 6 = float stroke width in px → a colour table's fields 0, 1, 2 are r, g, b as bytes. ⚠ **EACH CHANNEL DEFAULTS TO 255, NOT 0** — defaulting a missing channel to zero (the FlatBuffers default for a plain byte, and the obvious guess) turns that caption's amber into `#00bc0f`, a green nobody chose. ⚠ **A TABLE OFFSET IS SIGNED**: Premiere writes objects both before and after the field naming them, and reading one unsigned turns a small backward reference into a two-gigabyte jump off the end of the blob. ⚠ **ALL THE RUNS OR NONE** — one text layer can be painted a word at a time (29 in the corpus are) and `AnimaticTextClip` holds one colour, so runs that disagree give up the paint entirely rather than repaint the other words. ⚠ **A STROKE NEEDS BOTH ITS COLOUR AND A WIDTH** (517 records carry a width alone, which by the default rule is a white outline — plausible, unproven, invisible on white lettering; 17 carry a colour alone). ⚠ **AND A BLOB THAT DOES NOT WALK GIVES UP THE PAINT RATHER THAN THROWING** — it comes from an uploaded file, so every offset is bounds-checked and the fallback is `IMPORT_TEXT_COLOR`, which is what every import did before this. Measured over **6,287 style records in 120 real projects**: fills come out as a designer's palette (`#ffbc0f`, `#e14e21`, `#3b92d9`, `#4700a8`, `#99d6a6`, `#28324e`…), strokes are overwhelmingly `#000000`, widths land on round numbers. ⚠ **STILL MISSING: the BACKGROUND BAR** — not located in the style table, so a caption still arrives with no bar and the report says so. Pinned in `tests/interchange_check.py` §8s (22 checks, five guards proved to fail), with the blob built by hand so the fixture cannot follow the code. | PAKKA |
| **E81** | A caption's BACKGROUND BAR out of a `.prproj` | ⚠ **IT IS ON THE DOCUMENT TABLE, NOT ON A RUN — WHICH IS WHY THE FIRST LOOK MISSED IT.** The fill and the stroke are per RUN (Premiere lets you paint one word); a bar is drawn once around the whole block, so it hangs off the document table beside the runs vector. Fields, read off a photographed Properties panel (Background ✓, opacity 75, size 20.0, corner 10): **f18** bool, written only when the background is ON; **f19** float, opacity as a PERCENTAGE; **f20** float, padding in px; **f34** float, corner radius in px. Across 6,513 documents f18 travels with f20 and f34 (3,301 of them). ⚠ **THE DEFAULT OPACITY IS 75, AND `f19 = 100.0` BEING WRITTEN 71 TIMES IS WHAT PROVES IT** — a field is only written when it differs from the default, so 100 cannot be the default while 100 is being written. ⚠ **NO FLAG MEANS NO BAR**: 51 documents carry a size and a radius with the background switched OFF, and a bar behind each of those captions is a change to somebody's film they did not make. ⚠ **THE TWO APPS DO NOT PAD THE SAME BOX, AND COPYING THE NUMBER ACROSS MAKES THE BAR HALF AGAIN TOO TALL** — Premiere pads the INK, `draw_texts` pads the LINE BOX, which already carries the leading. Both measured on one caption (45 px type, size 20): Premiere's bar is 72 px at 1080, this app's bar at `backdrop_pad: 0` is already 71. The conversion equates the two heights and comes out at ≈0 there — the rendered bar lands at 76 px — while a background four times as wide still gets four times the padding. The RADIUS needs no such correction: both spend it as `radius × font size`. ⚠ **THE BAR'S COLOUR IS NOT IN THE CAPTION, AND IT IS #3F3F3F — KNOWN FROM THREE DIRECTIONS.** (1) Premiere's own Color Picker on that Background swatch, photographed: **#3F3F3F**. (2) The caption whose `f19 = 100` draws its bar at FULL opacity, so its export shows the stored colour with nothing composited over it: a flat #3e3e3e over 116,915 pixels — one level, which is h.264. (3) The same value is WRITTEN in field 5 of **679 of the corpus's 974** shape `Appearance` blobs. So it is **Premiere's default swatch grey**, which is precisely why no caption carries it: a FlatBuffer does not write a field equal to its default. That is also why nothing could be differenced against it — every caption here is at the default. The report still says which part was matched rather than read. ⚠ **AND PREMIERE COMPOSITES IN LINEAR LIGHT** — second sighting, after the 10% black card that read 244 where sRGB says 229 — so an imported bar over a bright shot comes out slightly darker here. Whole-app difference, named rather than fudged. Pinned in `tests/interchange_check.py` §8s, five more guards proved to fail. | PAKKA |
| **E82** | A shape's `Appearance` blob (and any Premiere Arb blob) | ⚠ **WALK THE FLATBUFFER; DO NOT COUNT BYTES INTO IT. THE FIXED-OFFSET READER WAS REFUSING 55% OF REAL SHAPES.** E76 pinned a shape's fill by DIFFERENCE at absolute offsets and guarded it with a length check of exactly 404 bytes — correct, and it worked on the **441** blobs that are that length. **533 of the 974 in the corpus are not**, and every one of those was refused and arrived as an invisible placeholder. `Appearance` carries the same `44 33 22 11` header as a caption's `Source Text` (E80): `root → field 0` is the appearance table, **field 0 of that is the FILL** and field 5 is its outline. ⚠ **READING FIELD 5 BY MISTAKE PAINTS MOST SHAPES GREY** — 679 of the 974 have `#3f3f3f` there. ⚠ **THE UPGRADE IS ZERO-REGRESSION BY MEASUREMENT, NOT BY ARGUMENT**: both readers were run over all 974 — on the 441 the old one accepted they agree on every single one, and the 533 it refused are exactly where they differ. The new fills are a designer's palette (`#78adcb`, `#7a51d1`, `#2d81ec`, `#a955d1`, `#fe8d7e`…). ⚠ **A TABLE'S OWN DECLARED LENGTH MUST BE BOUNDS-CHECKED, NOT JUST ITS VTABLE** — a blob cut off inside a colour table still has a valid vtable, and because an absent channel is 255 a red truncated after one byte comes back WHITE rather than refused. ⚠ And `''` is still the answer to any doubt: a shape drawn from a misread blob is a full-frame rectangle in a colour nobody chose, over somebody's film. Pinned in `tests/interchange_check.py` §8q, whose fixture is now a real FlatBuffer built by hand rather than bytes laid at 0x170. | PAKKA |
| **E83** | Anything the editor REMEMBERS about how a project looks (row heights, and any per-row view state added later) | ⚠ **KEY IT BY THE PROJECT, NOT BY THE ROW — A LANE KEY IS NOT UNIQUE TO A PROJECT.** Timeline row heights were stored as a flat `{ laneKey: rem }` in `localStorage` on the theory that "I like my Text row tall" is a reading preference, not a property of one edit. It is wrong, because the structural rows are `text:`, `shape:`, `image:`, `frames:0`, `audio:` in **every** project, and an imported project is worse — `interchange.py` names its lanes `_import_text_0`, `_import_shape_0`, … deterministically, so every import produces the same ids as the last one. One accidental nudge of a row seam (a 9px strip under every gutter row, exactly where a row-restack drag starts) therefore made **every project opened afterwards, and every fresh import, come up with some rows tall and some short — with no drag of the user's behind it**. *Reported as: "jab mai editor open karta hun to layer chota bara kyun rahta hai jab user layer ka size badla hi nahi… first time mai ek look rahna chahiye".* Now `{ [projectId]: { at, rows } }` under `cas_animatic_rows2`, the flat record is deleted on sight, and the store is capped at 24 projects. A project nobody has dragged a row in has **no** record, so every row is one height. Pinned by `tests/editor_row_height_check.py`. | PAKKA |
| **E84** | Any treatment the RULES planner places on a run of cuts (transitions today; anything with a pattern tomorrow) | ⚠ **ONE HOUSE GESTURE CARRIES MOST OF IT, AND THE OTHERS COME ROUND ON A COUNTER — NEITHER “ALWAYS THE SAME” NOR “A TOUR OF THE LIBRARY”.** `house_style.js` deliberately placed only `dissolve` and `dip`, on the argument that “a treatment nobody chose is the first thing the user deletes”. That is right about variety for its own sake and wrong about what a cut looks like: on a 27-shot film all eleven treated cuts came out as the same dissolve. *Reported as “mai dekha hi transition bas dissolve hi lagaya gaya, transition alag alag use hona chahiye video editor ke hisab se soch kar”.* The fix is the shape the MOVES already had (`stillMove`'s weighted pattern, which itself replaced a four-way rotation for the same reason): dissolve is the default, a **slide every 4th** treated cut, a **wipe every 6th** with the second one an angled edge travelling the other way, and the **dip still outranks the pattern** on a hold long enough to end a scene — a decision about the FILM beats a pattern about the cuts. ⚠ The counter indexes the TREATED cuts in film order, not the raw cut number. ⚠ And every kind is checked against `caps` first, so a build that renders fewer falls back rather than proposing a step `validatePlan` would drop. Pinned by `tests/director_guardrails_check.py`. | PAKKA |
| **E85** | A tick box in the 🎬 Make Video panel that only the AI pass can satisfy | ⚠ **IF THE FREE DOOR CANNOT DO IT, THE BOX IS A SWITCH THAT DOES NOTHING — GIVE IT A HOUSE ANSWER OR SAY WHY THERE ISN'T ONE.** Sound effects and Background music both came only from the analyse call. The call answered **403 PERMISSION_DENIED**, the run fell back to “Just the rhythm”, both boxes were ticked, and nothing arrived on the timeline. *Reported as “Sound and bg music nhi aaya timeline pe”.* ⚠ **THE LINE IS STORY vs RHYTHM, AND IT IS THE SAME LINE THE REST OF THE FILE DRAWS.** Which moment is a door closing is a story question and arithmetic still must not answer it — but *how fast this film cuts* is a music bed (`houseMusicCue`, three bands off the median hold, always **instrumental** so it does not fight a voiceover), and *which cuts this plan just treated* is a whoosh under each transition (`houseSfxCues`, **one** recording, landing half the transition early because a transition is boundary-local). A whoosh on an ORDINARY cut is still refused — that would be an opinion about the story. ⚠ Both are FALLBACKS: a reading that cued its own always wins. ⚠ And the effects are read off the PLAN, so un-ticking Transitions takes them away with it. Pinned by `tests/director_sound_check.py`. | PAKKA |
| **E86** | An AI panel that is unsure what the user meant | ⭐ **ASK, WITH OPTIONS, AND ALWAYS LEAVE THE DOOR OPEN.** Asked for outright: *"if it unsure about anything give us the options and ask if not these then what tell me"*. Researched first — **Descript Underlord, Premiere’s AI Assistant, CapCut, VEED, ChatCut and Runway all GUESS and act**; not one of them asks. So `ask` is a first-class reply kind (`chat_turn.js`) rather than a paragraph ending in a question mark: 2–4 real options, each with a half-line saying what it would MEAN for this film, and `allow_other` **hard-coded true** so "none of these" is always available. ⚠ **THREE TRIGGERS AND ONLY THREE** — an unclear target, a spend, a deletion — and **the fourth rule is when NOT to ask**: a bot that opens a question box on "put a dissolve on every cut" is a bot people stop opening. The triggers live in the prompt AND in `ASK_REASONS`, and `tests/editor_chat_check.py` §4 asserts the two agree. | PAKKA (shape) + GUZARISH (the asking itself) |
| **E87** | Adding a second AI surface that edits the timeline | ⚠ **REUSE THE VERB REGISTRY, DO NOT WRITE A SECOND ONE.** The chat runs `ACTIONS` through the same `directorApi` bag and the same `readDirectorCtx` the 🎬 popup uses, so the one-transition-per-cut rule, the effects cap and the shot numbering are obeyed for free and cannot drift. ⚠ **WHAT IS NOT REUSED IS `useDirectorRun` ITSELF** — it is a four-phase machine built around a popup opened once and answered once, and a conversation has no phases: any turn may be an answer, a question or a plan, and a plan may sit unapplied while three more messages go by. ⚠ **AND ONE STEP PER TICK, 90ms, THE SAME NUMBER** — two features applying the same registry at different speeds would be two different answers to "has the commit landed yet". | PAKKA |
| **E88** | Where a per-tier allowance for a new feature lives | ⚠ **IN THE TIER’S OWN `limits`, NEVER IN THE FEATURE’S SETTINGS STORE.** The AI Editor’s admin tab shows three things on one screen and OWNS one: on/off is `features.py` (`cap.editor-chat`), the monthly message count is that tier’s `limits["chat_turns"]` written through `billing.save_tier`, and only the behaviour is `chat_settings.py`. A `turn_limits` map in the chat’s own store would have been fewer lines and a number the pricing page did not know about — and the first customer to hit it would have been reading a limit nobody advertised. `server/usage.py` states the rule; the counter name and the limit key are the same string. | PAKKA |
| **E89** | A hard-coded count in a test that guards a growing catalogue | ⚠ **DERIVE IT FROM THE SOURCE.** Adding a seventh capability broke `capability_check` (“all six are there”, twice) and `billing_check` (“everything is in the free tier”, `12`) — three assertions that had to be edited by hand for a change that was correct. A test people must edit every time the thing it guards grows is a test people learn to edit without reading. Both now count `features._catalog()` / `_CAPABILITIES`, which asserts the property that was actually meant: the resolver returns ALL of them, whatever there happens to be. | PAKKA (convention) |
| **E90** | A panel a browser suite cannot reach — one that renders `null` while it is shut | ⚠ **BUNDLE IT AND `renderToStaticMarkup` IT.** G7 says a green `npm run build` is not evidence a screen renders, and it is right — but `EditorChat` returns `null` unless it is open, and every existing browser suite leaves it shut, so **its entire markup was code no test had ever run.** The cheap middle step is esbuild + `react-dom/server` against a stub shaped like the hook’s return (`tests/editor_chat_render_check.py`, ~3s, no browser). ⚠ **IT PAID FOR ITSELF ON THE FIRST RUN**: the composer promised *"nothing changes until you press Apply"* on **every** turn, including an empty chat with no Apply button anywhere — a reassurance pointing at a control that was not on screen. ⚠ **AND THE HARNESS MUST UNESCAPE**: JSX turns every apostrophe into `&#x27;`, so `"couldn’t use" in markup` is an assertion that can never pass — three checks reported the component broken when the harness was. A harness that lies about the code is worse than no harness. ⚠ **STILL NOT A BROWSER**: no layout, no CSS, no click. | PAKKA (convention) |
| **E91** | A verb that DELETES or SPLITS a shot | ⚠ **RESOLVE THE SHOT NUMBER TO A FRAME ID IN `validate`, NEVER AT RUN TIME.** Every other verb resolves `shot: 5` against the live read-model at the top of its own step, which is right while the film keeps its shape. It is wrong the moment a step can renumber: “delete shot 3, then title shot 5” means the shot the PERSON called 5 when they read the preview, and after step one that shot is number 4 — so the title lands on the wrong picture and **reports success**. `validate` runs once, against the document the user was shown; a deletion changes no other frame’s id. ⚠ **AND `run` MUST LOOK THE ID UP AGAIN AND THROW WHEN IT IS GONE** — between preview and Apply the user can edit by hand. One failed step beats deleting whatever now sits at that index. | PAKKA |
| **E92** | A relative amount (“trim a second off”) in a verb | ⚠ **READ IT WITH `int`, NOT `ms`.** `ms()` is “a time” and floors at zero, because every other verb takes a position or a length. `trim_shot` takes a DIFFERENCE, where the sign IS the instruction — and reading it through `ms()` silently turned every “hold it a second longer” into “no amount to trim by”. ⚠ **AND THE RELATIVE VERB HAS TO EXIST BESIDE THE ABSOLUTE ONE**: “take a second off shot 3” and “make shot 3 one second” are different films, and a model given only `set_shot_duration` answers the second when it was asked the first. On a six-second shot that is a five-second mistake. | PAKKA |
| **E93** | Anything the AI does that needs a NETWORK call (sound, and whatever comes next) | ⚠ **IT IS NOT A VERB AND CANNOT BE ONE.** Every entry in `ACTIONS` is synchronous — it calls one editor function and returns — so a stock-library search has no shape to be in. It rides BESIDE the steps and runs AFTER them, which is also the only correct order: a cue lands on a MOMENT and `set_shot_duration` moves every moment after it. Same placement, same reason, as the Director’s phases D and E. ⚠ **AND SOUND-ALONE IS STILL A PLAN** — “put some music under it” writes no steps at all; reading that as an `answer` drew a chat bubble where an Apply button belonged, so the user was told what would happen and given no way to make it happen. *Found by a test, not by a user.* | PAKKA |
| **E94** | Deciding what kind of reply an AI turn is | ⚠ **THE CONTENT DECIDES, NOT THE LABEL — IN BOTH DIRECTIONS.** `chat_turn.js` had said this from the start about plans whose every step was dropped, and then broke it the other way: the plan branch was gated on `kind === "plan"`, so a model that sent a music cue under `kind: "answer"` — which is exactly what one does when it thinks of “add music” as answering rather than editing — produced prose and **no Apply button at all**. A label is what the model INTENDED; the content is what it managed, and only the second is drawable. | PAKKA |
| **E95** | Measuring something about an audio file for the AI | ⚠ **THE BROWSER HAS ALREADY DECODED IT — READ THAT, DO NOT ASK THE SERVER.** `beats.js`/`useAudioAnalysis` decode every upload once for the waveforms and the beat markers, so “where is the dead air” is a question the client can answer for **no upload, no ffmpeg, no model call and no quota**. A route would have re-decoded a file to compute a number the client held. ⚠ **THE ALGORITHM IS THEN A TWIN OF `captions.spans_from_envelope`, AND A TWIN NOBODY CHECKS IS A TWIN THAT DRIFTS** — all seven shared constants are read out of BOTH files by `editor_chat_check.py` §10. ⚠ **AND `MIN_SILENCE_MS` IS THE WRONG LINE FOR AN EDIT**: it separates a pause from a consonant, which is right for laying captions on a wave and far too fine for a cut — every sentence has 300ms of air after it, and offering to remove forty of those ruins the reading. `DEAD_AIR_MS` (900) is the line a person calls dead air, and it deliberately has no twin. | PAKKA |
| **E96** | An AI offering to “remove the filler” from a voiceover | ⚠ **SAY WHICH ONE YOU ARE REMOVING.** This app’s captions carry LINE timings, not word timings, so a filler word can be taken off the SCREEN and cannot be taken out of the SOUND. A user who asked to “remove the umms” and got captions deleted while the audio still stumbles has been quietly handed the wrong thing, and nothing on screen would tell them. ⚠ **AND THE WORD LIST IS SHORT ON PURPOSE**: only words that carry no meaning in ANY sentence. “like”, “so”, “right” and “well” are filler half the time and load-bearing the other half — a false positive here is a word deleted from somebody’s film. Hinglish fillers are in the list because this app’s users speak Hinglish (A8). ⚠ **AND THE BAR IS “MOSTLY”, NOT “CONTAINS”**: “Umm, I think we should go” is a real line with a stumble on it. | PAKKA (shape) + GUZARISH (the offer) |
| **E97** | A measurement the AI would act on | ⚠ **REPORT “I CANNOT TELL” RATHER THAN ACTING ON A BAD READING.** Below `MIN_SOUND_SHARE` the envelope is not describing speech — a noise floor above the threshold, a format decoded oddly — and the worst outcome available to this whole feature is proposing to cut four fifths of somebody’s voiceover off a failed measurement. `deadAir` returns `trusted: false` and the digest says so out loud; omitting it would leave the model to assume there is no dead air, which is a different and worse answer. ⚠ A related limit is PINNED rather than fixed: an energy envelope cannot tell constant hiss from constant speech, because the threshold is derived from the track. It errs towards “this is all speech”, so nothing gets cut. | PAKKA |
| **E98** | A `tests/*_check.py` that writes a JavaScript harness | ⚠ **`<<TOKEN>>` SUBSTITUTION, NEVER `%`-FORMATTING AND NEVER `str.format`.** JavaScript is full of `%` (a modulo, a percentage in a comment) and full of `{}`. Under `%`-formatting every one of those is a format specifier, so adding an ordinary line of JS makes the TEMPLATE fail to render — with a `TypeError` pointing at the file-writing line that says nothing whatsoever about JavaScript. Cost three debugging rounds in one sitting before the template was converted. `director._fill` exists for exactly this reason; copy it. | PAKKA (convention) |
| **E99** | Adding assertions to an existing `*_check.py` | ⚠ **CHECK WHICH FUNCTION YOU LANDED IN.** These suites split into `client_checks(data)` / `prompt_checks()` / `server_checks()`, and only the first is handed the node results. Anchoring an insert on “the next `print(“\n5 · …”)`” put two separate blocks into `prompt_checks`, where `data` does not exist — a `NameError` at the END of a green-looking run, twice. ⚠ **AND ANCHOR ON THE LAST ASSERTION OF A SECTION, NOT ON THE NEXT HEADING**: sections here have sub-headings of their own, so “the next print” lands in the middle of one. | PAKKA (convention) |
| **E100** | Writing an assertion about something an AI planner PRODUCED | ⚠ **ASSERT THE PROPERTY IN YOUR CHECK'S OWN NAME, NOT THE VALUE THAT HAPPENS TO CARRY IT.** `editor_director_check.py` asked *“a transition arrives INTO every other shot”* and then matched on `“dissolve” in the cell `or` `“dip” in the cell` — an exact description of the planner on the day it was written. The day `treatmentFor` learned to spend a `slide` every 4th treated cut, a flat eight-shot board read `[2, 4, 6]` and the suite went red **about the rhythm, because of a change to the kind**. The cell is `“—”` when empty; `r[2] != “—”` is the property. ⚠ **AND THE KIND WAS ALREADY TESTED, IN THE RIGHT PLACE** — `director_guardrails_check.py` pins the whole pattern against the planner directly, which is where a house-style rule belongs. A browser suite duplicating it got the duplicate wrong and the original stayed green. ⚠ **THE COST IS NOT THE RED LINE, IT IS THE HALF HOUR SPENT PROVING IT WAS NOT YOURS** — diagnosed here by running the suite in a clean `git worktree` at HEAD, which is the cheap way to answer “did I break this” without touching the working tree (G5). | PAKKA (convention) |
| **E101** | A HEADER that already carries a control at its right edge, inside a `fixed`/`absolute` panel that is NOT a dialog | ⚠ **THE SHARED ✕ IS `position: absolute`, SO IT RESOLVES AGAINST THE PANEL AND LEAVES THE HEADER ROW ENTIRELY — AND WHERE THE ROW ALREADY ENDS IN A CONTROL, THE ✕ MUST COME BACK INTO THE ROW.** The AI Editor's header ends in the dock switcher (“beside the rail” / “right-hand side”) and `.ec-panel` is `fixed`, so the ✕ landed ON the two icons: *“chat bot panel mai x overlap ho raha hai ye right left icon se”*. ⚠ **RESERVING THE CORNER WITH A `padding-right` IS NOT THE FIX, AND THIS RULE WAS PAID FOR BY TRYING IT FIRST** — it cured the overlap and left a STEP, the ✕ a few pixels above the icons beside it: *“icon aur x close buttun upper niche dikh raha hia ek jaisa rakho please align ye achha nhi lag raha hai”*. `.ec-head .modal-close` is `position: static` now. ⚠ **THIS DOES NOT REOPEN E65 AND MUST NOT BE READ AS DOING SO.** E65 is about the 31 DIALOGS, every one of which has an EMPTY corner for the ✕; the corner there is still not negotiable. A panel whose title bar has its own controls is the exception, and it is the exception BECAUSE the corner is already occupied. ⚠ **AND SIDE BY SIDE MEANS ONE BOX, NOT TWO NEARLY-EQUAL ONES** — `--ec-ctl: 1.75rem` on `.ec-head` sets the height of BOTH the switcher pill and the ✕ (which also takes it as `min-width`, since the glyph is one character and E65 made the ✕ the way out). Hand-tuned padding per control is how they drifted apart in the first place; anything added to this row takes `--ec-ctl` too. ⚠ **NOT PROVED IN A BROWSER** — `dialog_frame_check.py` (31 dialogs) and `editor_chat_render_check.py` are green, but neither measures a pixel; a screenshot in BOTH docks is the evidence. | GUZARISH |
| **E102** | Two buttons standing side by side, anywhere in this app | ⚠ **THEY ARE ONE SIZE, ON ONE LINE — AND THE REASON THIS KEEPS COMING BACK IS THAT THE CAUSE IS A GLOBAL RULE, NOT A SCREEN.** *“dekho admin panel mai save name and reset name buttun uper niche v hai aur chhota bara … mai bahut baar bola hun aisa nhi hona chaiye magar aisa mil hi jata hai”*, with a screenshot. `.btn.primary` in `base.css` carries `margin-top: 1.1rem` — a FORM's spacing living on a COLOUR variant — so **every** primary in the app is born 1.1rem lower than the button beside it, and whoever noticed patched their own container: **39 rules across 21 stylesheets exist for no other reason than to undo it.** That is one bug found thirty-nine times. ⚠ **THE MARGIN IS STILL THERE, AND DELETING IT IS NOT THE FIX TO REACH FOR.** 68 buttons in 34 files wear `btn primary`, and the ones outside those 39 rules are form submits relying on it; removing it is one line and 68 gaps to re-check IN A BROWSER, which no test here can see. The ROW opts out instead — **`btn-row`** in `base.css`, which zeroes the margin on the buttons inside it. ⚠ **IT WINS ON ORDER, NOT SPECIFICITY.** `.btn-row > .btn` and `.btn.primary` are both 0-2-0; move the row rule above the variant in `base.css` and the step comes back with every test green. ⚠ **AND SIZE IS THE OTHER HALF, WHICH NO CSS CAN FIX.** `btn primary` beside `btn small` is a big button next to a little one wherever its top edge is, and correcting it in CSS would override a size somebody chose on purpose. So it is read out of the JSX instead: **`tests/button_row_check.py`** finds buttons standing next to each other and fails when their sizes disagree or a `primary` sits in a row that resets nothing. It skips square icon buttons (a shape, not a slab) and buttons in different conditional branches (never on screen together), and **it is proved on the reported markup first**. Four real rows were wrong when it was written: `.admin-brand-acts`, `.pf-foot`, `.danger-actions`, `.dir-actions`. ⚠ **THE READER MEASURES FROM THE END OF THE PREVIOUS BUTTON'S OPENING TAG** — nearly every button carries `onClick={() => f()}`, whose `)}` reads as a branch close; measuring from the start cut 70 real rows to 27 and stayed green. | PAKKA |
| **E103** | The app's COLOURS — adding one, changing one, or writing a rule that spends the accent | ⚠ **THE ACCENT AND THE GROUND ARE AN ADMIN FIELD NOW, AND EVERY OTHER COLOUR IS DERIVED FROM THEM.** *“theme colour style banao brand ke under hi … only dark blue colour hai isliye mujhe change kar ke dekhna hai kismai thik lagega … jab kare color change to sab jagah achhe se ho jaye aur test v dekhe achhe se”*. Brand → **Colours**: eight presets or two colours of your own; `client/src/palette.js` turns them into panels, borders, text, buttons, glows and the timeline's selection tints, **for both light and dark**. ⚠ **THE BUILT-IN LOOK INJECTS NOTHING AT ALL.** `theme.css` is hand-tuned over a long series of live tests and no derivation reproduces it to the byte, so `cssFor(default)` returns `""` and a deployment that never opens the screen renders through exactly the CSS it always did. That is the difference between adding a feature and silently restyling everybody's app. ⚠ **BOTH BLOCKS, AND THE LIGHT ONE NEEDS THE ATTRIBUTE SELECTOR.** A plain `:root` cannot beat `theme.css`'s `:root[data-theme="light"]` — a specificity loss, not an ordering one — and the failure looks like “dark mode works, light mode ignored me”. ⚠ **`rgba(229, 193, 88, 0.1)` MAY NEVER BE TYPED AGAIN.** 69 rules in 20 stylesheets did, and every one would have stayed gold on a deployment that chose green while nothing went red. They are `rgba(var(--accent-rgb), …)` now; `--accent-rgb`, `--accent-deep-rgb` and `--accent-on-white` are defined ONCE in `theme.css` (not per theme, so the rewrite was byte-for-byte invisible). `palette_check.py` fails on the 70th. ⚠ **THE CONTENT COLOURS ARE NOT DERIVED, ON PURPOSE** — video orange, image pink, Veo purple, caption green, shape violet say WHAT A CLIP HOLDS (asked for by name; see `theme.css`). Only the SELECTION tints follow the accent. ⚠ **THE MATHS LIVES IN ONE PLACE AND THE TEST RUNS IT.** The server stores two hex strings and a slug and knows nothing about colour; `tests/palette_check.py` bundles `palette.js` with esbuild, runs it under node and measures WCAG contrast on the real output — a Python reimplementation would be the copy nobody updated. ⚠ **DERIVED TEXT IS CORRECTED, NOT TRUSTED**: `readable()` walks lightness toward black or white — **both directions**, and picking one by a luminance threshold was a real bug that returned 2.6:1 on a `#a0a0a0` ground. All 256 greys, both themes, are asserted ≥ 4.5:1. ⚠ **THE ONE THING NOTHING CAN CORRECT IS AN ACCENT THE SAME COLOUR AS THE GROUND** — the buttons vanish into the panel, which is **E66** by a new door. The panel measures it (“Button on panel”, 3:1) and says so; it does not refuse, because a deployment's own brand colour is not this screen's call. ⚠ **AND CHOOSING IS A LIVE PREVIEW OF THE WHOLE APP, WITH A PUT-BACK ON THE WAY OUT** — leaving the tab restores what the server holds, or an administrator is left hunting a setting that does not exist. Proved end to end in Chromium by `brand_landing_check.py` §6 (repaint → reload → read `--panel` and `--gold-fill` off `:root`, both themes, then back to built-in and assert the override is GONE) — a cascade only exists in a browser. | PAKKA |
| **E104** | Any admin switch whose SETTING can silently cancel its STATUS — the feature rollout today, anything with a gate tomorrow | ⚠ **A WORKFLOW WAS SET *LIVE* AND NO CUSTOMER COULD SEE IT, AND THIS SCREEN SAID EVERYTHING WAS FINE.** *“mai jab text to turnaround image workflow live kiya hun na hi wo user workflow mai na landing page mai dikh raha hai kyun?”* — with screenshots of a green `Live` pill beside a rail that did not carry it and a landing page still saying “Three workflows”. `workflow.text-to-image` was `status: live` with `rollout.mode: "allowlist"` and **nobody on the list**, so `_rollout_passes` refused every account. ⚠ **THE RESOLVER WAS RIGHT; THE PANEL WAS LYING.** The row printed *“Everyone in the rollout can use it.”* — true of the STATUS, false of the situation — with `· Named people (0)` tacked on the end as if it were a detail. Given both, a reader believes the calm one. ⚠ **AND IT IS INVISIBLE TO THE ONLY PERSON WHO EVER CHECKS.** Admins pass every rollout gate on purpose (you cannot stage a feature you are locked out of), so the administrator who threw the switch sees the workflow in their OWN rail and concludes it launched. That asymmetry is the bug; the empty list is just how it got in. ⚠ **THE FIX IS A WARNING, NOT A CORRECTION — DO NOT MAKE THE APP REWRITE THE SETTING.** An empty allowlist is legitimate (you make the list before you fill it). `reachesNobody()` in `AdminFeatures.jsx` names the state, the row turns amber (`.admin-feature.unreachable`), the reassuring sentence is **removed rather than moved**, and a **“Show it to everyone”** button sits beside the warning. Same rule for a 0% rollout. “Admins only” and “Hidden” are NOT flagged — they already describe themselves, and a warning on an accurate setting is noise. ⚠ **AND THE SAME QUESTION ASKED OF EVERY SWITCH, NOT JUST THE ONE REPORTED**: `tests/workflow_reach_check.py` walks all six workflows through all four rollout modes for a customer, an admin and the logged-out landing page; checks each one **leads somewhere** (a page in `App.jsx`, a rail fallback entry whose label matches the server's, a short name, a drawn glyph) and that no glyph or short name belongs to a workflow that no longer exists; then renders the real `FeatureRow` in each state. ⚠ **AND AN ASSERTION ABOUT A SENTENCE MUST READ THE SENTENCE, NOT THE PAGE** — the `Live` button's own `title` is that same string, correctly, so “is it gone?” had to be scoped to the summary paragraph or it could never pass. | PAKKA |
| **E105** | Any screen that draws a SERVER-OWNED list from a built-in fallback while it waits — the landing page's workflows today | ⚠ **A HIDDEN WORKFLOW WAS ADVERTISED TO EVERY VISITOR FOR AS LONG AS THE REQUEST TOOK.** *“jab refresh kiye to one sec ke liye dikha fir nhi”*. `useLiveWorkflows` started at `null`, returned `FALLBACK` — the built-in list of ALL SIX — and swapped to the server's answer a moment later. That is precisely the fault `/public/workflows` was added to fix (*“hiding one left it advertised to every stranger who visited”*) coming back through the front door, and **no count-the-cards test can see it**: the count is taken after `networkidle`, by which time the page is already correct. ⚠ **THE SIDEBAR HAD ALREADY LEARNED THIS AND THE LANDING PAGE HAD NOT** — `Sidebar.jsx` carries `workflowsKnown` and skeleton rows for exactly this reason. A rule fixed on one surface is not fixed; check the others the same day. ⚠ **THE ANSWER IS REMEMBERED, NOT SKELETONED, AND THAT IS A DELIBERATE DIFFERENCE.** A rail is chrome, so a skeleton is fine; a hero that empties itself for half a second on every visit is a worse first impression than the flash. `cas_public_workflows` in `localStorage`, same shape and same reasoning as the remembered brand in `branding.js` — a returning visitor is right in the FIRST paint with no request having answered. ⚠ **AND `known` COVERS THE ONE VISIT MEMORY CANNOT** — the first ever. Nothing is remembered, so the page draws no card grid, no hero tiles and **no count** until the answer lands, rather than claiming six and settling on three. `known` flips true on a FAILED call too, or a visitor with no network would get a page that never draws its list. ⚠ **PROVED BY MAKING THE ANSWER SLOW ON PURPOSE** (`brand_landing_check.py` §4b): `page.route` delays `/public/workflows` by 1.6s, then asserts zero cards and no number on a first visit, the right cards once it lands, and the right cards IN THE FIRST PAINT on the next reload. A test that waits for the network can never catch this. | PAKKA |
| **E106** | ANY house cap, share or per-minute rate the AI plans against — `HOUSE_CAPS` above all | ⚠ **A CAP IS A DEFAULT FOR WHEN NOBODY SAID, NEVER AN ANSWER TO SOMEBODY WHO DID.** *Paid for live:* the user typed *"add transition image layer 10 every each clip"* **twice**, then *"i want add alll clip not 9"*, and the panel answered *"Our system has a limit that only allows transitions on up to 35% of the cuts in a video to keep it clean. For your 27 shots, that means we can only use a maximum of 9 transitions."* Followed by *"mai aisa chahta hun user jo kahe wo hona chahiye"*. ⚠ **THE CAPS WERE RIGHT AND WERE BEING APPLIED IN THE WRONG PLACE.** `TRANSITION_CUT_SHARE: 0.35` and friends were written for the FREE door, where the app chooses on its own and "a dissolve means something because most cuts are straight" is true. Quoted at a person who asked, the same number is a refusal by a default. A plan may now carry `asked_for_all`, set by the planner ONLY when the words are there ("all", "every", "each", "sab par"), and `applyGuardrails` lifts every share, every per-minute rate and the alternate-cuts rule for that plan. ⚠ **WHAT LIFTS IS TASTE; WHAT NEVER LIFTS IS STRUCTURE.** `MIN_CLIP_MS`, `TEXT_OVERHANG_MS`, "the two clips must touch" and "they must be on the same row" stay — a lifted cap that produced a record which renders as nothing would be worse than the refusal, because it reports work it did not do. `EFFECTS_PER_CLIP` goes to `MAX_EFFECTS` (6) and no further: that is a shader uniform array, so the seventh effect is not "a lot of grading", it is an effect that does not exist. ⚠ **AND THE PROMPT IS HALF THE FIX** — the fence cannot lift a cap the planner never proposed against, so the manifest field is called `house_defaults_when_not_asked` now and the restraint rule reads "WHEN THE CHOICE IS YOURS". Pinned by `tests/chat_layers_check.py`. | PAKKA |
| **E107** | Anything that reads `ctx.frames` as an ordered film — `boardFrom`, any cut/transition/neighbour rule | ⚠ **`frames` IS EVERY PICTURE CLIP ON EVERY PICTURE ROW, IN ONE LIST. IT IS NOT A SEQUENCE.** *Paid for live, in the same report:* on a project with 3 clips on **Video** (layer 9) and 24 on **Images** (layer 10) playing UNDERNEATH it, `boardFrom` flattened the lot into a 27-shot film laid end to end — so the chat proposed a transition "after shot 24" and the validator answered *"there is a 28.0s gap after shot 24"* and *"shot 21 overlaps the shot after it"*. **There is no gap and no overlap.** Those two shots are on different rows at the same moment, and every unexplainable message in that report is this one bug. ⚠ **A CUT ONLY EXISTS BETWEEN TWO CLIPS ON THE SAME ROW**, and `cutAfter` now refuses a cross-row one BEFORE the touching test — because two clips on two rows fail the gap test as well, and being told about a gap sends the reader looking for one on a timeline that has not got one. ⚠ **AND THE PLANNER IS TOLD, NOT JUST CORRECTED.** A better error message is not the fix: the model has to know the film is stacked before it proposes anything, so every shot carries its row and `boardFrom` lists the picture rows with their clip counts. ⚠ **THE ROW NUMBER IS `laneIndex + 1` FROM THE GUTTER AND IS DERIVED, NEVER RECOMPUTED** — "lagao layer 10 ke clips par" is a sentence about the number the person can SEE (`tl-layer-num` in `Timeline.jsx`), and a second numbering would answer about a different row. It is a LABEL; `track` stays the identity. ⚠ **NO ROW STACK MEANS ONE ROW, NOT UNKNOWN** — every maths-only test hands a bare `{frames, starts}` and that is exactly what they mean, so the fields simply do not appear and a one-row film is never told about rows. Pinned by `tests/chat_layers_check.py`. | PAKKA |
| **E108** | The ✨ chat and paid work — `allow_paid_passes` | ⚠ **"MAY OFFER" AND "MAY START" ARE DIFFERENT SENTENCES, AND ONLY THE FIRST ONE IS TRUE.** The flag shipped OFF, and off it made `rails_text` tell the model *"this deployment does NOT let the chat start paid work at all — do not offer to start one"*. The model obeyed: asked for a voiceover, the chat changed the subject — so it went quiet about the three most valuable things the app does. Asked for outright: *"jo free hai wo free mai hoga aur jismai paisa lagta hai usmai paisa lagega — uske liye subscription/plan lega agar nahi liya hua hai to"*. It ships ON now and the rails say it MAY offer paid work, MAY NOT start it, and must name the button (🎬 Make Video, Voiceover, 🖼 Animatic images) rather than quote a price or guess a tier. ⚠ **NOTHING IN THIS FEATURE CAN SPEND, EVER** — `POST /editor-chat/{id}/turn` spends text quota and returns a proposal; that is unchanged and is the load-bearing rule. ⚠ **AND THE OLD DOCSTRING DESCRIBED A FEATURE THAT DOES NOT EXIST**: it claimed that switched on the chat "may OPEN the priced confirm", but no client code reads this flag at all (only `AdminChat.jsx`, which sets it). A priced confirm inside the chat is still unbuilt — do not read the comment as a description of one. | PAKKA |
| **E109** | The editor's status strip at the foot of the screen | ⚠ **THE STRIP IS PERMANENT FURNITURE, NOT A POP-UP.** It used to render only when it had a notice, an error or a progress bar — so opening a project, new or recent, showed **nothing** at the foot of the editor, and the first anybody saw of it was it *appearing* under their first click, moving the layout while they were reading something else. Reported outright: *"jab mai koi bhi new ya recent project open karta hun to ye us samay nhi dikhta, tabhi dikhta hai jab user kuchh click kare — mai chahta hun ki ye hamesha dikhta rahe."* The `<div className="an-statusbar">` is now unconditional and holds a **resting hint** (`restingHint`) whenever there is nothing to report, so the foot of the editor is one fixed height for the whole session. ⚠ **THE HINT ALWAYS LOSES.** An error wins, a notice wins, and every progress row renders beside it regardless — `{!error && !notice && …}` is what makes it safe to leave on permanently, because a tip can never push a real message off the one line they all share. ⚠ **AND IT IS THREE OR FOUR WORDS, WITH THE SENTENCE IN THE `title`** (E-rule for hints everywhere: helper prose goes on hover). The strip is `nowrap` and elides — a paragraph here is simply cut off mid-word. It is muted (`.an-status-hint`), not the notice's gold, or a permanent line in message colours teaches you to stop reading the strip. ⚠ **IT IS CONTEXTUAL, IN THIS ORDER**: a non-Selection tool first (a razor you cannot see is the oldest trap in an editor) → an empty project → a multi-selection → one selection → playing → the shortcut line. ⚠ **`.an-status-note` IS UNTOUCHED**, which is deliberate: eight browser suites read a notice through that exact class and would have gone green against the hint if the two shared one. | PAKKA |
| **E110** | The ✨ chat offering PAID work — `passes`, and anything that ever wants to print a price beside it | ⚠ **THE CHAT NAMES A DOOR. IT NEVER OPENS ONE, AND IT NEVER SAYS A NUMBER.** A turn may carry up to two of `voiceover` / `captions` / `veo` / `images`, and the panel draws a real button per offer that opens the confirm dialog ✨ Animate, 🎙 Voiceover and 🖼 Animatic images already use — which is what asks the server for the cost and what refuses an account whose plan does not cover it. ⚠ **DO NOT ADD A PRICE TO THE CHAT, EVER.** A figure computed on the client, off the board the browser happens to be holding, sitting next to a door that asks the server for the real one, is two answers about somebody's money; the first time they disagree the user is right to distrust both. The rule is written into `chat_turn.js`, `EditorChat.jsx` and `editor_chat_agent.py`, and `tests/editor_chat_doors_check.py` asserts all three comments still say it. ⚠ **AN OFFER IS NOT AN EDIT AND MUST NOT DRAW AN APPLY BUTTON** — there is nothing on the timeline to apply, so a turn carrying only `passes` is an `answer` with a button on it. ⚠ **AND AN OFFER SURVIVES A REFRESH WHILE A PLAN DOES NOT**: an Apply would run stale steps against a timeline that has since moved (E-series `stale`), while a door button reads the film as it is *now* and prices it there. ⚠ **ONLY `veo` MAY CARRY A SHOT NUMBER** — the other two doors are whole-film, and a stray `shot` would offer to render through a button that renders nothing. ⚠ **`captions` IS ITS OWN DOOR AND IS NOT THE VOICEOVER**, though the voiceover writes captions too: this one reads audio the person RECORDED THEMSELVES and adds no voice, which is the whole request when somebody imports their own video — offering the voiceover there proposes replacing their own narration. It needs an audio track, and with none the door says so rather than opening a panel whose confirm is silently disabled. ⚠ **AND A DOOR MUST CALL THE SAME FUNCTION THE HAND-DRIVEN BUTTON CALLS, NEVER THE STATE SETTER.** `openPaidDoor` shipped with `setSpeechFor("voiceover")`, which opens the panel with none of `openVoiceover()`'s setup done — no flush, no dialogue sheet fetched, last run's confirm and error still sitting there — so the user would have read an empty script under a stale price. Caught by writing the check, not by seeing it. Mirrored `PAID_DOORS` on both sides, the same arrangement `ASK_REASONS` has. | PAKKA |
| **E111** | The ✨ chat and the PICTURES — `look`, and anything that wants to send images on every turn | ⚠ **THE CHAT IS BLIND BY DEFAULT, AND SEEING IS A REQUEST IT HAS TO MAKE.** *"Chat tasveer dekh nahi sakti"* was true: it gets labels, durations and dialogue, so *"mera video analyse karo — konsa part bekar hai"* could only ever be half answered (silence it finds off the waveform, for free; a dull shot it had never seen). A turn may now come back as `kind: "look"` naming the fewest shots that could settle it, and the browser re-posts the SAME message with those pictures attached, proxied to 512px through the same `?w=` the monitor already asks for. ⚠ **DO NOT MAKE IT AUTOMATIC.** Sending the film with every turn would make the cheapest question in the product ("how long is this?") the most expensive one, so nothing is sent until the model says the answer depends on what is in frame — and the prompt lists the cases where it does not. ⚠ **ONE LOOK PER MESSAGE, ENFORCED IN CODE.** `_read_turn(blind=False)` — the call that already carries pictures — refuses a second look outright, because the prompt can only ask and two models each wanting a slightly different set of stills is a loop that bills on every lap. ⚠ **A LOOK OUTRANKS A PLAN ON THE SAME REPLY**: a model that sent both hedged, and the plan it wrote blind is the one it was unsure enough about to ask for the pictures. ⚠ **AND A PICTURE IS IN `fingerprint()`, BY DIGEST** — it changes the answer as surely as a sentence does, and hashing the bytes into the digest input would hash a megabyte to compare two calls. Pinned by `tests/editor_chat_doors_check.py`. | PAKKA |
| **E112** | Attaching images to ANY `llm_json` call — the look pass today, anything else tomorrow | ⚠ **A LONG SYSTEM INSTRUCTION PLUS IMAGE PARTS HANGS THE CALL. MEASURED, NOT THEORISED.** The chat's full ~9.8KB system prompt with five 512px stills attached took **149 seconds and then failed** — 504 `DEADLINE_EXCEEDED`, twice, reproducibly, against `gemini-3.5-flash`. The *same* pictures, the *same* prompt and the *same* response schema under a two-line system instruction answered in **7.5 seconds**. ⚠ **BISECTED RATHER THAN GUESSED**, in this order: five images with a tiny schema = 3–5s; five images with the real 3.5KB response schema = 5s; the real 9.8KB system with no images = 4.7s; the real system **with** images = 149s and dead. One variable. ⚠ **THE FIX MOVES THE RULES, IT DOES NOT TRIM THEM** — on a look the whole system prompt is prepended to the user message under "YOUR WORKING RULES — these are your instructions, not background", and `LOOK_SYSTEM` (two lines) takes its place. A second, shorter rulebook for looking would quietly plan worse than a turn that could not see, and nobody would find out until they read two transcripts side by side. ⚠ **SO BEFORE YOU ADD IMAGES ANYWHERE ELSE IN THIS APP, CHECK THE SYSTEM INSTRUCTION'S SIZE FIRST** — and time the call, because the failure mode is a hang, not an error. ⚠ **THE FIX HOLDS, AND ITS "7.5 SECONDS" DOES NOT — re-measured live 2026-09-04**: the look path builds exactly as designed (the log shows `system=0.3KB, prompt=18.1KB, images=5`, so the rules really did move), and it answered in **34.6s** rather than hanging at 149s. The mechanism is proven; the number was one sample on one day and is now four to five times bigger. **A look is ~10× a text turn (2.6s) and that is the honest figure to plan budgets against** — see F10, where it changed how a retry is decided. | PAKKA |
| **E113** | Any panel a person is allowed to MOVE or RESIZE — the ✨ AI Editor's floating window today | ⚠ **MOVE AND RESIZE ARE TWO DIFFERENT PERMISSIONS, AND MOST PANELS ONLY GET THE SECOND.** Asked for outright: *"chat bot ka popup screen ko move kar sake and chhota aur bara kar sake"*. A dock pinned to a screen edge has **no answer** for "put it somewhere else" — so the two pinned docks resize (width only; they are full height by definition) and a THIRD dock, `float`, is the one that moves. ⚠ **THE WIDTH HANDLE IS `PaneSplitter`, THE WORKSPACE'S OWN SEAM — do not write a second one.** It already carries the drag maths, double-click-to-reset, the arrow keys and the 2px line that only shows on hover; a fresh handle would be a second thing that behaves *almost* like the seams. It borrows `--an-seam`, which `.an-nle` declares — a `fixed` panel is not inside `.an-nle`, so **it must re-declare that variable or the grab strip is zero pixels wide**. The **eight** handles on the floating window are the only new ones, and they are new because no seam in this app drags on both axes or moves a window's position while it resizes — **see E132**. ⚠ **THE WINDOW IS CLAMPED WHOLLY ON SCREEN, NOT "MOSTLY".** The desktop-window-manager rule (keep a title bar's worth visible) is wrong here: there is no taskbar to fetch it back from, so once its Send button is past the right edge the button that would move it is unreachable. `panel_box.js` clamps SIZE FIRST, THEN POSITION — a width clamped after the x it was used to compute leaves a gap that grows on every resize — and the viewport is **state, not a read at draw time**, because nothing else re-renders the panel when the browser window shrinks under it. ⚠ **AND THE INLINE STYLE BEATS THE MEDIA QUERY.** Under 820px the component stops emitting geometry at all; trusting `@media` to override an inline `left` is how a phone gets a 380px window jammed in the corner. *Pinned by `tests/editor_chat_render_check.py` §6a.* ⚠ **AND WHAT THE PANEL HOLDS IS NO LONGER THIS BROWSER'S — SEE E135**: one project keeps many chats, on the server, keyed by (owner, project). | PAKKA |
| ~~**E114**~~ | Making any panel SEE-THROUGH — **STRUCK, see E115** | ⚠ **THE ALPHA GOES ON THE BACKGROUNDS, NEVER ON THE PANEL.** `opacity` on the root fades the conversation, the buttons and the spinner along with the ground they sit on, and a chat you cannot read is not a transparent chat. The admin's percentage arrives as ONE custom property (`--ec-opacity`) and `color-mix` puts it on the three surfaces — the panel, the log and the composer — while the text stays at full strength. ⚠ **AND THE BLUR IS NOT DECORATION.** Underneath is a playing film with captions burnt into it; at 60% those captions read straight through the assistant's own words. `backdrop-filter` is what keeps both legible, and it is behind an `.is-see-through` class so a solid panel does not pay a per-frame GPU cost for nothing. ⚠ **IT IS THE OPERATOR'S FIELD, NOT THE CUSTOMER'S** — *"admin panel mai ai editor se chatbot panel ko transparent kar sake"*. A customer who slides their own chat to 40% and can no longer read it has no way back, which is why the floor is 40 and why the editor has no slider. Same family as **E103**: a colour decision belongs in the admin panel, never hard-coded in CSS. *Pinned by `tests/editor_chat_render_check.py` §6b and `tests/editor_chat_check.py` §9.* ⚠ **STRUCK THE NEXT DAY AND ONLY HALF OF IT WAS RIGHT** — the alpha-on-the-backgrounds half survives into **E115**; the blur and the floor of 40 were both measured in the dark theme only and were both wrong in the light one. | ~~PAKKA~~ struck |
| **E115** | Making any panel SEE-THROUGH — replaces **E114** | ⚠ **THE ALPHA GOES ON THE GROUND, NEVER ON THE PANEL** *(this half of E114 stands)*: `opacity` on the root fades the conversation, the buttons and the spinner along with the ground they sit on, and a chat you cannot read is not a transparent chat. The admin's percentage arrives as ONE custom property (`--ec-opacity`) and `color-mix` puts it on the background. ⚠ **ONE LAYER, NOT THREE — THIS IS WHAT E114 GOT WRONG.** The log and the composer were mixed to the SAME percentage as the panel behind them, and alpha **stacks**: 40% over 40% covers 64%, and the log is most of the panel's surface. The slider said one thing and the screen did another. Inner surfaces go **fully transparent** and keep only their borders, so the number is the whole story at every point on the range. ⚠ **AND NEVER JUDGE A TRANSPARENCY EFFECT IN ONE THEME.** E114 set a floor of 40 and added `backdrop-filter: blur(16px)` — both fine in dark, both meaningless in light, where `--panel: #ffffff` sits over an `#f4f6fa` page: **white frosted onto white is a panel that looks solid at every setting**. Reported as *"dark mai thora ho raha hai white mai to ho hi nhi raha hai"*. The blur is gone (a filter that changes how the number reads, differently per theme, makes the operator's judgement impossible) and the range is **0–100 with no floor** — *"0 to 100 rakho mai admin panel se check kar lunga kitna better hai"*. ⚠ **AND ONCE 0 IS LEGAL, EVERY `or 100` IS A BUG.** `int(row.get("opacity") or 100)` turned the one value at the very end of the slider into its exact opposite; zero is an ANSWER on this field, so it is `is not None`, never `or`, and `??` never `||` on the client. Still the operator's field, not the customer's — someone who fades their own chat until they cannot read it has no way back. Same family as **E103**: a colour decision belongs in the admin panel. *Pinned by `tests/editor_chat_render_check.py` §6b (including the zero case) and `tests/editor_chat_check.py` §9.* | PAKKA |
| **E116** | The EDGES of any panel that floats OVER the work — outer AND inner — the ✨ chat, and anything docked over the editor later | ⚠ **`--border` IS TUNED FOR BLENDING, AND THIS IS THE ONE PLACE THAT IS WRONG.** Every card in this app sits INSIDE a page and wants an edge that disappears into it; a panel laid over the editor is separating two surfaces from the SAME `--panel` family, so the token that makes a card look settled makes a floating panel look like part of the furniture behind it. Reported as *"chatbot panel ka stroke badhao, wo bg editor mai match v ho raha hai — highlight karo panel border ko"*. So `.ec-panel` carries its own `--ec-stroke`, **2px not 1px**, and the floating window adds a **tight 1px ring inside its wide shadow** — the soft shadow says "above the page" and says nothing where the panel happens to sit over something equally dark; the ring is what actually draws the outline. ⚠ **DERIVED, NEVER TYPED (E103).** It is `--border` mixed towards `--text`, so it follows the admin's Brand colours and flips direction by itself — the edge LIFTS in dark and DARKENS in light. A hex would have been right in one theme and wrong in the other, which is the identical mistake **E115** records for the blur. ⚠ **AND IT GOES FURTHER AS THE GROUND GOES AWAY**: under `.is-see-through` the mix moves further towards `--text`, because at 30% fill the border is the only thing telling you where the panel ends and the film begins. ⚠ **NOT GOLD** — **E16** owns that: gold is the action on a screen, and an outline is not an action. ⚠ **AND THE BOXES INSIDE IT NEED A SECOND, WEAKER TOKEN — ONE STROKE IS NOT ENOUGH.** The log and the composer wear `--border` from `storyboard.css`, and once the panel is see-through **they have no fill either** (that is what stops the alpha stacking, E115), so nothing at all marked where the conversation ended and the typing box began: *"under wale panel v thora highlight karo, thora merge ho raha hai"*. `--ec-stroke-in` sits **between `--ec-stroke` and plain `--border`** and the ordering is the rule: three edges of the same weight is a grid, not a window — the outer stroke has to stay the strongest line or it stops reading as the panel's boundary. The head and foot dividers ride the same token, or the chrome disagrees with the boxes. ⚠ **AND ONLY THE RESTING STATE** — `.sc-composer:focus-within` is one specificity step above it, so clicking in still paints the gold exactly as before; that half was already right (*"jab text likhne jata hun tab aata hai, wo thik hai"*), what was missing was finding the box BEFORE focusing it. | PAKKA |
| **E117** | Any LOOK-AND-FEEL number that lands differently in the two themes — blur, glow, shadow strength, tint, contrast | ⚠ **IF YOU CANNOT PICK IT RIGHT FOR BOTH THEMES, IT IS NOT A CONSTANT — IT IS A SETTING.** The chat panel's `backdrop-filter` proved this twice in two days: hard-coded at `blur(16px)` it was right over a busy timeline in DARK and invisible in LIGHT (white frosted onto white, so the panel looked solid at every opacity); removed entirely, the text became unreadable at low opacity. **Both failures were the same mistake — one number chosen for every screen.** Asked for outright: *"text padhne mai mushkil hai isliye blur daalo — **tum nhi**, admin panel pe daal do, mai set kar lunga blur ko v"*. It is `blur` in `chat_settings.LIMITS` now, a slider beside `opacity`, and the operator judges the pair together on their own deployment. ⚠ **THE DEFAULT IS WHAT IS ALREADY ON SCREEN (0), NEVER YOUR BEST GUESS** — a default IS picking the number, which is the thing you were told not to do, and it silently changes every existing deployment. ⚠ **AND A FILTER COSTS THE SAME AT ZERO AS AT SIXTEEN**: `backdrop-filter` at ANY value, `0px` included, promotes the element to its own compositing layer and re-filters everything behind it every frame — over a PLAYING timeline. So it is gated by a CLASS (`.is-blurred`, added only when `blur > 0` **and** the panel is see-through), never by `blur(calc(var(--x) * 1px))` sitting on the rule permanently. *Pinned by `tests/editor_chat_render_check.py` §6b (blur-on-solid and blur-of-zero each asserted to add no class) and `tests/editor_chat_check.py` §9.* | PAKKA |

| **E118** | Anything that creates a LIBRARY ROW before the user has done anything — New Project here, and any "new X" tile added later | ⚠ **A ROW IS CREATED BY THE FIRST REAL ACTION, NEVER BY OPENING A SCREEN.** *Paid for live:* four empty *Untitled Project* rows on the Editor library — *"mai ye project mai kuchh nhi kiya magar ye yaha pe dikhte hai … user kuchh kare to hi project banao"*. New Project used to `POST /animatics` on the way IN and rely on the editor deleting it again on the way OUT (`isEmpty` → `handleBack`). ⚠ **THAT DISCARD ONLY RUNS ON ONE OF THE FOUR EXITS.** The ← button ran it; the sidebar, a refresh and a closed tab did not — so every other way out of the editor left a row **and had already spent a slot of the account's `projects` quota** for a project containing nothing. ⚠ **SO THE EDITOR NOW OPENS ON NOTHING.** `NEW_DRAFT` (`AnimaticLibrary.jsx`) means *a project that does not exist yet*: `animaticId` is `null`, `useAnimaticProject` loads nothing and adopts the empty document as its own baseline, and `ensureProject()` mints the real project at the first thing that needs a server — an upload, a board or project-file import, a Freesound add, a ✨ draw, a chat turn, a Save-as, or the first autosave of any other edit. Do nothing and go back and **nothing was ever made**. ⚠ **AWAIT IT AND USE WHAT IT RETURNS — never the `animaticId` in the closure**, which is still `null` there for ever; a url built from it (`/animatics/null/media/…`) 404s, and the media fetch caches nothing on failure and does not retry. That is the same trap `doBoardImport` paid for once as forty-two black tiles. Async paths read `idRef.current`, which is written synchronously by the create. ⚠ **A DRAFT NEVER RE-READS ITSELF FROM THE SERVER**, even though its id goes `null` → real mid-session: fetching then would replace the frames that just created it with the empty project the server was just handed. Same rule keeps the ✨ chat's conversation. ⚠ **AND THE SERVER HIDES THEM TOO**, because rows made by older builds are already in the database and a stale tab can still make one: `list_animatics` skips a project that is empty AND never named, and sweeps one older than a day off disk (`_is_ghost`). ⚠ **THE EMPTINESS TEST MAY NOT NAME A FIELD `SUMMARY_DROP` REMOVES** — the list route serves slimmed documents and `params.overlays` is dropped, so a project whose only content is a picture on an Images lane reads as empty; `size_bytes == 0` is what keeps it. *Pinned by `tests/e2e_animatic.py` §2b–4b.* | PAKKA |
| **E119** | EVERY library list route — the audit that goes with **E118** | ⚠ **ONE WORKFLOW HAD THE BUG; ALL OF THEM GOT CHECKED, AGAINST THE LIVE COLLECTION.** Asked for straight after the Editor fix: *"mai chahta hun ki mera aur v workflow mai aisa kuchh to nhi hai check karo"*. Read-only sweep of every job kind: **animatic 4 empty rows (the reported ones), storyboard 0, final video 0, plan 0, character 0**. ⚠ **THE ONE THAT LOOKED LIKE A SIXTH WAS NOT ONE** — an *"Untitled plan"* with no messages and no calendar turned out to hold a **written script** and its token usage. A crude emptiness test (messages / plan / channel) called it a ghost; `_is_ghost_plan` asks about `scripts` too and keeps it. **Write the test against what the workflow can HOLD, not against the two or three fields you happen to remember** — the cost of getting it wrong is deleting somebody's work. ⚠ **THE PLAN LIST GOT THE GUARD ANYWAY, AND IT WAITS TEN MINUTES BEFORE HIDING ANYTHING.** `ensureSession` is already lazy there, but a create followed by a failed first message still leaves a record — and unlike the Editor's library, this list is refreshed IN THE SAME ACTION that creates the session, while the first model turn is still in flight, so hiding on sight would take the session the user is sitting in off their own sidebar. ⚠ **AND `Script → Storyboard` IS ALREADY RIGHT FOR A DIFFERENT REASON** — its draft is only written once there are shots (`shots.length` guards the effect), and the script autosave compares against what it loaded, so opening the form and typing nothing writes nothing. `Animatics → Final Video` has no blank-create tile at all and keeps its `isEmpty` guard as a deliberately unreachable one. *Pinned by `tests/ghost_rows_check.py`, which asserts the rules AND re-runs them over every real record — it fails if a row with content would be hidden.* | PAKKA |
| **E120** | Any pane that asks the SERVER a question it could answer LOCALLY — the Properties pane's storyboard rows today | ⚠ **A ROUND TRIP PER SELECTION THAT COULD ONLY EVER FAIL.** `RegeneratePanelInline` and `RelengthShotInline` sat under a comment saying they *"render nothing unless this clip's picture comes from a board panel"* — and they very nearly did: both return `null` once the answer arrives. But each ASKED FIRST, so clicking an upload, a video clip or a colour card fired `GET .../panel` and `GET .../sequence`, which 400'd, were swallowed, and left red failed requests in the console on **every single click**. Found by `tests/e2e_animatic.py` §14, which fails the run on a console error and is the only thing that was ever going to see it. ⚠ **THE CLIENT ALREADY KNOWS**: `isBoardShot` (one definition, in `voice_pass.js`) reads `frame.src.kind`, which is the same test the server's `_board_behind` starts with — so the two panes are not MOUNTED for a clip that names no board, and the server stays the authority on whether that board still exists. ⚠ **AND THE ROUTE WAS WRONG TOO, MEASURED AGAINST ITS OWN SIBLING**: `get_frame_panel` answers **200 with a `reason`** for a clip with no board, *"so the pane never renders a button that is going to 400"*; `get_frame_sequence` raised 400 for the identical case. It carries the reason now, in the same field name, because a caller still has to tell *"no board"* apart from *"a board with nothing drawn yet"*. ⚠ **A SWALLOWED ERROR IS NOT A HANDLED ONE** — `.catch(() => {})` hid this for months while the browser logged it every time. | PAKKA |
| **E121** | ANY hint, tooltip or comment that describes what a control DOES — and every one of them the day a behaviour changes | ⚠ **A HINT THAT CONTRADICTS ITS CONTROL IS WORSE THAN NO HINT: it is read once, believed, and never checked again.** The Properties pane's Duration row promised *"everything after it moves"* — true when the picture was one butt-jointed sequence, where a hold WAS the running total, and false from the day a clip got its own `start_ms` on a numbered track. Typing 5s into the middle of three 2s clips makes the film **0:07, not 0:09**: the clip grows and nothing else budges. ⚠ **THE BEHAVIOUR WAS RIGHT AND THE SENTENCE WAS WRONG, and that decides which one to change.** The box does exactly what the timeline's DEFAULT trim (V) does — *"the only one that never touches a clip you were not pointing at"* — so making the box ripple would have made it the ONE control in the editor that behaves unlike the default tool. The hint names the Ripple tool (B) instead, so somebody who wants the old behaviour knows it is one keypress away. ⚠ **THE SAME STALE CLAIM HAD SPREAD TO TWO MORE PLACES** — `chat_turn.js` and `useEditorChat.js` both justified "sound runs after the steps" with *"`set_shot_duration` moves every moment after it"*, and the chat's verb calls the very same `patchFrame`. The RULE survives (an inserted shot ripples its row; re-timing moves that shot's own end); the REASON was corrected. **When a behaviour changes, grep for the sentence, not just the function.** ⚠ **AND IT IS NOT THE VEO PATH.** Generating a take or inserting a shot still carries the whole film — captions, text, shapes, images, voiceover — along with the pictures (`renderShifts` + `RIPPLED_LISTS`); that road was never touched and is pinned by `tests/timeline_ripple_check.py` and `tests/veo_ripple_check.py`. *The hint itself is now pinned by `tests/e2e_animatic.py` §7, which reads the tooltip a person actually sees.* | PAKKA |
| **E122** | ANY turn the ✨ chat labels a `plan` — and every future verb whose `run` is a no-op | ⚠ **A PLAN THAT WOULD MOVE NOTHING IS NOT A PLAN, AND AN APPLY BUTTON OVER ZERO EDITS IS THE WORST LIE THIS PANEL CAN TELL.** *Paid for live, with a screenshot:* **“add music and sound effects in this storyboard story wise”** on a fourteen-shot board answered *“I've added a cinematic, storytelling music bed and placed sound effects on key shots to bring the storyboard to life”* — over **one `note` step, no `sound`**, and a button reading **“Apply 0 edits · Nothing has changed yet”**. Nothing was added; the Audio row stayed empty. ⚠ **NOTHING WAS MALFORMED AND NOTHING WAS DROPPED** — `note` is a real verb whose `run` is `() => {}` (it exists so a plan can explain itself), so every check in the path passed and the turn was still a do-nothing. ⚠ **THE RULE WAS ALREADY WRITTEN AND THE TEST WAS ONE WORD SHORT**: `chat_turn.js`'s own header says *“a reply labelled `plan` whose every step was dropped is not a plan”*, and the code tested **`steps.length`**, which a note passes — while `EditorChat.jsx` two files away already counted `verb !== "note"` for the button's label. **A rule stated in a docstring is not a rule the code obeys.** Both sides now test for a step that DOES something (`_read_turn` in `editor_chat_agent.py`, `readTurn` in `chat_turn.js`), independently — same split as `_coerce_ask` / `normaliseAsk`. ⚠ **AND THE NOTES GO INTO `dropped`, NOT INTO SILENCE**: what the model wrote is the user's only evidence it misunderstood the job, and a turn that quietly becomes a chat bubble reads as the model *choosing* to answer. ⚠ **THE PROMPT HALF IS A SEPARATE FAULT AND IS ONLY GUZARISH**: the system prompt already forbade the past tense (*“say ‘I'll put a dissolve’, never ‘I've added dissolves’”*) and the model broke it anyway, so more prompt words are the weaker half of this fix. What DID change there is **E106 applied to sound**: *“SPARINGLY, AND NEVER ONE PER SHOT”* was a house default written as a law, and *“story wise”* is exactly the ask it refuses — it now lifts when the words are there, plus **“a note is not a sound and not an edit”**. Pinned by `tests/editor_chat_check.py` (8 checks, proven to FAIL against the old code on both sides). | PAKKA |
| **E123** | ANY JSON schema handed to a model as NATIVE structured output — `reply_schema`, `plan_schema`, and every one added after them | ⚠ **A PROPERTY THAT IS NOT IN `required` IS ONE THE MODEL MAY SIMPLY NOT WRITE — not fill badly, not leave empty: OMIT THE KEY.** *Paid for live, over four runs:* **“add music and sound effects in this storyboard story wise”** on a fourteen-shot board came back with `music` filled, **no `sfx` key at all**, and a `reply` that NAMED the effects it had not sent — *“the lighting of the lamps, the rustle of gifts, and the fireworks”*. The model knew exactly what it wanted and had no slot it was obliged to put it in. ⚠ **AND EVERY WORD-SHAPED FIX FAILED FIRST, IN THIS ORDER**: the house rule lifted in `prompts.yaml` (E106 applied to sound); the field's own `description` rewritten to *“an empty list is a refusal”*; the rule moved into the TURN prompt directly under the verb list; and finally the field path named in the **user's own message** (*“Put one entry in sound.sfx for every shot”*). All four returned `sfx: []`. Then **one line** — `"required": ["sfx"]` on the `sound` object, nothing else changed — returned **fourteen cues, one per shot**, with real stock terms (“temple bell puja”, “sand pouring rustle”, “fireworks crackle”). **Words do not make a model fill a slot the schema says it may skip.** ⚠ **THE SAME BUG WAS ALREADY BITING SOMEWHERE ELSE IN THE SAME SCHEMA**: `plan.steps[]` required only `verb`, so a step arrived as `{"verb": "note"}` with no `args` and the panel showed *“no arguments this verb understands”* over an empty plan. Now `["verb", "args"]` — and note that nothing INSIDE `args` can be required, because it is a flat union of every verb's argument names, so *“write the object”* is the strongest thing sayable there. ⚠ **WHAT IS NOT REQUIRED, AND WHY** — `required` is a claim about what the model MUST answer, not a wish list. The five payloads at the root (`plan`, `sound`, `ask`, `look`, `passes`) stay optional because a turn carries ONE of them; `sound.music` stays optional because it arrived in all four runs unasked, and requiring it would push a bed onto somebody who asked for one door slam. ⚠ **PINNED AS AN EXACT MAP, NOT A “must contain”**: `tests/editor_chat_check.py` walks every object in `reply_schema` and fails on any path whose `required` list drifts, or on a property added without a decision — because a test that only guarded the two known-bad fields would let the third through in silence. | PAKKA |
| **E124** | ANYWHERE a `JsonRequest` is rebuilt from another one — `as_prompt_schema`, `_repair_request`, and the next one somebody adds | ⚠ **A REBUILT REQUEST THAT DROPS `capability` IS A REQUEST FOR A DIFFERENT PROVIDER, ON A DIFFERENT KEY, UNDER A DIFFERENT MODEL.** `resolve_provider(capability="")` falls back to `TEXT_PROVIDER`, so on this repo's own `.env` — ✨ chat on the Gemini Developer API, text on **Vertex** — the repair call went to Vertex, which has no credentials here. *Caught live, 2026-09-05,* running `tests/chat_live_battery.py`: one case came back **`403 PERMISSION_DENIED … aiplatform.googleapis.com … CONSUMER_INVALID`** — **an error about credentials, on a call whose only fault was a missing brace, on a key that was perfectly good.** Both builders were missing the field; both now carry it. ⚠ **AND THE SILENT VERSION IS THE WORSE ONE**: where BOTH backends have credentials nothing fails at all — the mend is simply billed to the other capability's key, which is the exact bug `get_client`'s `provider|key_env` cache key was built to prevent, re-entering by the back door. A wrong answer that works is worse than one that 403s. ⚠ **`images` IS THE SAME QUESTION AND GETS THE OPPOSITE ANSWER, DELIBERATELY.** `as_prompt_schema` MUST carry the pictures — it is the same call with the schema moved into the prompt, and dropping them sends a LOOK out blind to answer about a film it was never shown. `_repair_request` MUST NOT — a mend quotes the model's own broken text back and asks it to close the braces, so re-uploading five stills is real money and thirty seconds for nothing. **Same field, two builders, two correct answers** — which is why neither may be copied from the other without reading why. ⚠ **PINNED BY THE ROUND TRIP, NOT BY THE FIELDS.** `tests/director_timeout_check.py` asserts the two builders directly AND drives a real `complete_json` through a fake transport that breaks its first answer, then reads the capability the adapter was handed on **both** calls — because the adapter is what asks `resolve_provider`, and a test of the fields alone would not have noticed the repair being a second call at all. Proven to fail without the fix: `['chat', '']`. | PAKKA |
| **E125** | ANY object rebuilt FIELD BY FIELD from another one — the ✨ chat's agent row above all, and every `toStore` / projection beside it | ⚠ **NAMING THE FIELDS YOU COPY MEANS THE ONE YOU FORGET FAILS SILENTLY, AND IT TAKES THE WHOLE FEATURE WITH IT.** `normaliseTurn` returns five payloads — `plan`, `sound`, `ask`, `passes`, `look` — and `useEditorChat.send` copied **three** into the row the panel actually draws from. *Paid for live, 2026-09-05, twice over:* · **`sound`** — with the E123 fix in, the model sent fourteen cues and a music bed, and the panel still drew **“0 edits · Apply 0 edits · Nothing has changed yet”**, because the button counts `turn.sound?.sfx`. Worse than the label: `apply()` opens `if ((!steps.length && !turn?.sound)) return`, so on a sound-only turn **the button did literally nothing when pressed** — sound had never once reached a timeline through this panel. · **`passes`** — `EditorChat.jsx` draws the paid-door buttons from `turn.passes`, so ✨ Animate / 🎙 Voiceover / 🖼 Animatic images **never appeared**, however clearly the chat offered them. ⚠ **AND THE TELL WAS ALREADY IN THE FILE**: `toStore` read `t.passes` — the save projection was written for a field the live row never had, so the two halves disagreed and neither failed out loud. **When two projections of the same object disagree, one of them is wrong; find out which before reading either as the truth.** ⚠ **THE FIX IS THE FIELDS; THE RULE IS THE TEST.** `tests/editor_chat_check.py` asks the REAL `normaliseTurn` what keys a turn can carry (handing it steps, sound and an offer at once) and then fails unless the hook's row copies every one — so a sixth payload added tomorrow fails until it is carried. Guarding `sound` and `passes` by name would only have guarded the two already fixed. Proven to fail without the fix. ⚠ **Same family as E124** (a rebuilt `JsonRequest` dropping its capability): different file, different object, identical mistake — which is why this row is about the SHAPE, not about the chat. | PAKKA |
| **E126** | ANY field written into a document the server VALIDATES — above all a number with a `ge=` on it | ⚠ **A 422 REFUSES THE WHOLE DOCUMENT, NOT THE BAD FIELD — SO ONE WRONG NUMBER SILENTLY DELETES A NIGHT'S WORK.** `placeSoundtrack` wrote `trim_ms: 0` on every clip the AI Editor and the 🎬 Director laid down, meaning “no trim”. `AnimaticAudio.trim_ms` is `int | None` with **`ge=100`**: absent means “play the whole file”, and 0 is neither absent nor 100. Every save of that project was refused — frames, captions, durations, title, all of it — and the autosave re-sent the same rejected body on the next edit, forever. *Paid for live, 2026-09-05:* sound and music set on the timeline at night, **project blank the next morning**, with a wall of raw pydantic JSON along the bottom of the editor. ⚠ **THE AUTOSAVE ALSO ONLY FIRED ON A CHANGE**, so once it failed nothing tried again until the user touched the project — and they had gone to bed. It now retries with a backoff (5s, 10s, 15s…, bounded) and the failure is said in words, not as `JSON.stringify(detail)`. ⚠ **THE RULE IS THE CLAMP, NOT THE FIX.** `audioForSave` in `audio_clips.js` joins `frameForSave` / `assetForSave`, and it does one thing more than they do: those exist so a field is not **dropped**, this one also exists so a field cannot be **poisoned** — every number forced into the range the schema accepts on the way out, whatever wrote it. A bad value costs that value, never the project. `tests/audio_save_contract_check.py` validates the real `sfxPlacements` / `musicPlacement` output against the real `AnimaticAudio`, and **proves the raw shape is still refused** so it cannot pass against the broken code. Proven to fail without the fix. | PAKKA |
| **E127** | ANY sound placed automatically on a timeline | ⚠ **“NO TRIM” MEANS “PLAY THE ENTIRE RECORDING”, AND A STOCK RECORDING IS 20–30 SECONDS.** With `trim_ms` unset, ten cues on a 28-second film were ten 30-second files playing on top of each other from the moment each began. *Reported from the screen, 2026-09-05:* *“sound effects sab ek dusre ke clip ke upar tha aur sabka awaz aa raha tha”*, with the clips visibly running past the end of their own shots on the lane. ⚠ **AND THE NUMBER WAS ALREADY THERE AND THROWN AWAY** — `sfxCues` measures `hold_ms` (how long the cued shot holds) for every cue and `sfxPlacements` never read it. **Measuring something and not using it is the same as not measuring it.** Each effect is now its shot **plus a `SFX_TAIL_MS` ring-out**, cut back by whichever comes first: the next cue on the lane, or the end of the recording. The tail is deliberate — a slam ringing over the next cut is how sound works — but a ring that outlives the NEXT CUE is not sound design, it is two sounds. A recording shorter than its shot is left whole. Pinned by `tests/director_sound_check.py` § 4b. | PAKKA |
| **E128** | ANY loop that reads the time off a media element (`<audio>` / `<video>`) | ⚠ **`el.play()` MARKS AN ELEMENT UNPAUSED IMMEDIATELY — UNPAUSED IS NOT PLAYING.** The promise has not settled, nothing is decoded, and `currentTime` is still 0. `useTimelineTransport` picked “the first element that is not paused” as its master clock and read the time off it, so it got the same answer every animation frame: **the playhead pinned at 0 and the pictures frozen.** *Reported from the screen, 2026-09-05:* *“mai play dala raha tha 0 frame pe to play nhi ho raha tha, but jab timeline ka cursor thoda aage se play kiya 2/3 sec se to hone laga”* — at 0 the bed and the first effect both start cold; two seconds in, something is already decoded. ⚠ **IT COULD NOT HAPPEN BEFORE THE SOUND PASSES EXISTED**, which is why a feature that added sound broke playback. The clock candidate must now have `readyState >= HAVE_CURRENT_DATA` **and** a timestamp that has actually moved (`clockRead`, `CLOCK_STALL_MS` in `audio_mix.js`); otherwise the wall clock drives and hands back the moment the element genuinely starts. **Fall back, never wait — the pictures do not need the audio's permission to move.** Pinned by `tests/transport_clock_check.py`. | PAKKA |
| **E129** | ANY ceiling on what an automatic pass may fetch or make | ⚠ **A LIMIT THE SYSTEM CAN ACTUALLY EXCEED IS A FACT; A LIMIT SOMEBODY CHOSE IS AN ARGUMENT THE USER CANNOT WIN.** `MAX_SFX_SOUNDS` was a flat **10**, reasoned from the shared Freesound minute — but that budget is the SERVER's, the server already reports a rate-limited cue in `skipped` with “try again in a minute” and keeps every sound already filed. Meanwhile a fourteen-shot board was told **“one pass fetches at most 10 different sounds”, four times over**, about a project with room for thirty-four more files. *Reported from the screen, 2026-09-05, and the user's ruling was explicit:* **“user jitna maange utna mile”**. The only ceiling left is a real one — how many audio FILES the project can still hold (`soundRoom`, `MAX_AUDIO_FILES`, mirrored from `API_MAX_ANIMATIC_AUDIO_TRACKS`, raised 16 → 48) — and every refusal now names the PROJECT so the user can act on it. ⚠ **AND THE PREVIEW ASKS THE SAME QUESTION AS THE APPLY**: `normaliseTurn` reads the room too, so the count on the button is the count that happens. ⚠ **THE CEILING LIVED IN A FILE THAT COULD NOT SEE IT** — `MAX_AUDIO_TRACKS` was private to `AnimaticEditor.jsx`, so `sound_pass.js` invented a number of its own. **Two files guessing at one limit is two limits.** | PAKKA |
| **E130** | ANY pass that decides FROM WHAT THE FILM IS — sound, music, captions, titles, motion, colour | ⚠ **A PASS THAT DECIDES FROM CONTENT MUST BE GIVEN THE CONTENT — AND TOLD, IN WORDS, WHEN IT HAS NONE.** The ✨ AI Editor and the 🎬 Director both write sound off `board.shots[i].description`. `boardFrom` sends `description: frame.description \|\| frame.prompt` — and **`AnimaticFrame` has NEITHER field**, and `frameForSave` carries neither, so that key has been `""` on every turn since it was written. What the model actually saw for a fourteen-shot film was `Title: (untitled)` and `1. [2.0s] Shot 1 … 14. [2.0s] Shot 14`. *Paid for live, 2026-09-05:* a Diwali puja board — marigolds, diyas, a rangoli, a woman at a decorated puja table — came back scored **“pop bubble”, “camera shutter”, “digital beep”, “mouse click”, “glitch static”** under a bed of **“upbeat energetic corporate pop vlog”**. ⚠ **THE WORDS EXISTED THE WHOLE TIME — ON THE BOARD, NOT ON THE FRAME.** A frame carries `src: {storyboard_id, index}`; the PANEL behind it has the description, the location, the cast, and its board has the title, genre, world and script. `fill_board_words` (`server/common.py`) puts them back on the server, owner-checked exactly as `_voice_lines_of` does, and **one fill serves both features** because both read the same key. ⚠ **AND BLINDNESS IS NOW STATED RATHER THAN INFERRED**: a project made of uploads genuinely has no words, and the digest says so and names the two honest moves (`look`, `ask`). A model that is not told it is blind does not know it is guessing. Pinned by `tests/sound_matches_film_check.py` §§1-4 (12 checks fail without the fill). | PAKKA |
| **E131** | Writing a sound effect, a music bed, or anything else with a house “style” to fall back on | ⚠ **WHOOSH / BEEP / CLICK / GLITCH / “UPBEAT ENERGETIC CORPORATE” IS WHAT A MODEL WRITES WHEN IT DOES NOT KNOW WHAT THE FILM IS.** It is not a taste; it is the average stock video, produced with total confidence. Both sound prompts now make the same three demands, in the same words, because there are two sound writers and they drift: **(1)** name the KIND of film and WHOSE WORLD it is — ad, promo, story, vlog, cooking, fitness, music, logo, corporate, wedding, festival, documentary — and, in the chat, say it in `reply` so the user can correct it in one word; **(2)** cue only what a microphone in that room would have picked up (a kitchen sizzles, a shrine has a bell and a conch, a gym has a barbell and a breath); **(3)** the bed is named by INSTRUMENT or TRADITION (“sitar tabla”, “devotional bhajan”, “oud percussion”, “gospel organ”, “taiko drums”), never by a stock library's biggest folder. ⚠ **CULTURE, FAITH AND PLACE ARE THE USER'S AND ARE NEVER SWAPPED**, and never quietly translated into a Western default because that is what the library has most of. ⚠ **AND A FILM YOU CANNOT DESCRIBE IS NOT SCORED AT ALL** — look, or ask with options; an unscored film the user can score themselves beats a film wearing somebody else's soundtrack. ⚠ **STATUS IS GUZARISH ON PURPOSE** — §5 of `tests/sound_matches_film_check.py` proves the rules are still in both prompts, which is the most a test can honestly claim; only a live run says the model obeyed them. | GUZARISH |
| **E132** | Resizing a FLOATING window from an edge or corner — the ✨ AI Editor's today, any free panel later | ⚠ **THE EDGE THE PERSON IS NOT HOLDING MUST NOT MOVE, AND `{ w: w + dx }` PLUS A CLAMP DOES NOT GIVE YOU THAT.** Asked for outright after E113 shipped one corner: *"abhi ek taraf se hi chota bara karta hun, mai chahta hun charo taraf se"* — so the window now takes all four sides and all four corners. ⚠ **THE BUG THIS ROW EXISTS FOR IS THE WALK.** Dragging the LEFT or TOP edge changes the window's POSITION as well as its size (`x + dx` with `w - dx`), and the obvious handler keeps subtracting from `x` after `w` has already bottomed out at `MIN_W` — so past the minimum the window **stops shrinking and starts sliding across the screen**, dragging its far edge with it. Nothing throws. The fix is the order: **anchor the far edge first, clamp the SIZE, then derive the position from the size.** The far edge is also the real MAXIMUM — a window whose left edge is dragged past the screen can only grow to `right - 8`, not to the viewport width, which would let it grow off the side. ⚠ **THE SUMS LIVE IN `panel_box.js`, NOT IN THE COMPONENT** — same reason E113 gives: eight handlers each re-deriving one clamp is eight places for this to come back. One `resizeBox(box, dx, dy, edge)`, read by compass letter, so a ninth handle needs no new branch. ⚠ **ONE TAB STOP, NOT EIGHT.** The corner is focusable and its arrow keys already resize both axes; seven more stops between the composer and the page would buy the keyboard nothing, so the other seven are `aria-hidden` pointer affordances. ⚠ **AND THE GRAB STRIPS HANG HALF OUTSIDE THE BORDER** — a handle pinned flush inside means the 2px border itself does nothing and every miss lands on the film behind the window; the corners are declared AFTER the edges so the diagonal wins where they meet, and every one carries `touch-action: none` or the browser claims the drag as a page scroll and the pointermoves stop arriving mid-drag. *Pinned by `tests/panel_resize_check.mjs` (39 checks, the walk and the off-screen grow among them) and `tests/editor_chat_render_check.py` §6a, which names all eight handles.* | PAKKA |
| **E133** | ANY table of interchangeable-looking choices the AI must pick ONE of — transitions, effects, text presets, shapes | ⚠ **A LIST OF MECHANISMS WITH NO MEANING ATTACHED HAS EXACTLY ONE SAFE ANSWER, AND THE MODEL WILL GIVE IT EVERY TIME.** The editor renders twelve transition kinds. What the planners were handed was twelve ids, a label and a MECHANISM — `dissolve (Dissolve)`, `wipe — "an edge travels across"`. Nothing anywhere said what a wipe is FOR. *Paid for live, 2026-09-05:* asked to treat a fourteen-shot Ganesh Chaturthi reel *"pura story pe… transition and effects ke saath"*, the plan came back **“Dissolve on the cut after shot 1 … after shot 13” — thirteen identical dissolves.** *“Dissolve on the cut hi use kar raha hai… in do shot ke bich mai konsa badhiyan transition rahega waisa set kare.”* ⚠ **THE MODEL WAS NOT BEING LAZY — it had no grammar to choose with.** ⚠ **SO THE MEANING GOES ON THE RECORD, NOT IN THE PROMPT.** Every entry in `TRANSITIONS` now carries a **`when`** — what it MEANS and where it belongs (`dip`: black ends a chapter, white is a flash; `slide`: a list or a reel that must keep moving; `angular`: time itself) — and it travels through the manifest to BOTH planners. The chat's vocabulary trim keeps it even though that trim cuts every other table down to `id (label)` for tokens, because this is the one table where the prose IS the feature. ⚠ **THAT PLACEMENT IS THE RULE:** a thirteenth kind added without a `when` is a treatment the AI can render and can never knowingly choose — the same silent half-wiring as **E125**. `tests/transition_choice_check.py` fails until it has one. ⚠ **AND `note` WAS NOT TOUCHED**: it is what a person reads on a chip in the pane. This added a field for the AI; it did not reword the UI. | PAKKA |
| **E134** | Deciding how many treatments a film gets vs. WHICH treatment each cut gets | ⚠ **THESE ARE TWO DIFFERENT DECISIONS AND THE RESTRAINT RULE ONLY SETTLES THE FIRST.** “Restraint is the craft” and “prefer the plainest kind” governed WHERE a transition goes; nothing governed WHICH — so the answer to “which” defaulted to “the same one, everywhere”, which is not an edit, it is **a setting applied to a film**. Both prompts now make the choice **per cut, from the two shots either side of it**: ask what CHANGED, and let the answer pick — nothing much → a straight cut (still the commonest right answer); time passed → dissolve; a chapter ends → dip to black; a flash or a memory → dip to white; a deliberate move → wipe/diagonal; a list or a product → slide; a reveal → split/iris/box; time as the subject → clock; a corporate divider → blinds; playful or retro → checker. ⚠ **AND THE FORMAT SETS THE LENGTH**: a reel is 200–400ms and suits slides and wipes (a 1000ms dissolve eats half of a 2-second shot); a story or devotional film can hold 600–1000ms. **Vary the length as well as the kind — one duration repeated is the same tell as one kind repeated.** ⚠ **THIS IS NOT A LICENCE FOR VARIETY**, and both prompts say so out loud: *“a wipe here for a change”* is still the worst reason there is. The test is unchanged — you must be able to say what changed at that cut; if you cannot, leave it straight. ⚠ **AND IT ONLY BECAME POSSIBLE BECAUSE OF E130**: choosing from the PAIR of shots needs the shots to have descriptions, which they did not have until the board's own words were plumbed through. | GUZARISH |
| **E135** | Any conversation a person is meant to KEEP — the ✨ AI Editor's chats today, any assistant with a history later | ⚠ **A TRANSCRIPT IN `localStorage` IS NOT SAVED, IT IS CACHED, AND THE DIFFERENCE ONLY SHOWS UP ON SOMEBODY ELSE'S COMPUTER.** The chat lived in ONE key per project (`aniwala.editorChat.v1.<id>`), which meant one conversation per film, gone on another machine, gone when site data is cleared, and no way to keep *"the sound pass"* apart from *"the titles pass"*. Asked for outright: *"user new chat bana kar alag alag baat kar sake … aur sab chat save hona chahiye, user jo karwaya hai usko us project mai dekh sake fir baad mai — project by project save karna … mujhe production level bana hai"*. Now: a row per chat on the SERVER, keyed by **(owner, project)**, `server/chat_sessions.py` + five routes on the existing `/editor-chat` router. ⚠ **THE LINE BETWEEN THE TWO STORES IS THE RULE, NOT AN OPTIMISATION**: the WORK (titles, transcripts) is the server's because it follows the account; WHICH CHAT WAS OPEN and a mirror of that one chat's turns stay in the browser, because "where was I" is a fact about one screen and the mirror is what makes a reopened panel paint instantly and survive a dead network. The mirror is a CACHE and the server is the truth — and it is **one chat, not all of them**, or a browser ends up holding forty conversations per project in a 5MB quota nobody reads. ⚠ **`None` MEANS "LEAVE IT ALONE" AND `[]` MEANS "IT IS EMPTY", ALL THE WAY DOWN.** A rename posts a title and no turns; an autosave posts turns and no title. Collapsing the two with `or` makes **renaming a chat delete its transcript** — the single most expensive bug this feature could have, invisible until it happens to somebody with a long one. Both directions are pinned. ⚠ **`created_at` IS WRITTEN ONCE** (`$setOnInsert`, never `$set`) or every chat looks new on every keystroke and a list ordered by time shuffles under the pointer. ⚠ **THE CEILING IS A CEILING, NOT A BIN.** Over it, the store sweeps chats **nobody ever typed in** and then **refuses out loud** (409, naming the way to make room). It never deletes a conversation to make space — that is the one thing a store like this must never do. ⚠ **AND THE CEILING IS THE OPERATOR'S NUMBER, NOT A CONSTANT — SEE E138**: it, the turns-kept and the size guard are admin-panel fields (`chat_settings.LIMITS`), `0` means no limit, and the environment variables they shipped as were deleted rather than kept as a hidden maximum over the panel. ⚠ **OWNERSHIP IS IN THE KEY AS WELL AS IN THE ROUTE**, so a `job_id` guessed by hand returns nothing even through a route added later that forgets to check; both locks are tested, the store's without a route at all. ⚠ **THE FILING CABINET IS NOT BEHIND THE FEATURE FLAG OR THE QUOTA.** Switching the chat off must stop new work, not make old conversations unreadable, and reading back what you already paid for is not a second charge. ⚠ **AND THE OLD KEY IS RESCUED, ONCE, BEFORE IT IS DROPPED.** Every existing user has a conversation under v1; shipping without the migration would have looked exactly like the new feature deleted their history — and `forgetLegacy` is a SEPARATE call made only after the turns are actually on the server, because a network is at its most likely to fail between a read and a delete. ⚠ **A DELETED PROJECT TAKES ITS CHATS** (`_purge_animatic(job_id, owner)`), or they sit there for ever with nothing pointing at them. Pinned by `tests/chat_sessions_check.py` (47 checks) and `tests/chat_store_check.mjs` (44). | PAKKA |
| **E136** | Putting a LIST of anything inside a narrow docked panel — chats today, versions or takes tomorrow | ⚠ **A TAB PER ITEM IS THE OBVIOUS ANSWER AND IT IS THE WRONG ONE HERE.** The request said *"tab ka function banao"* and showed an IDE's tab strip; the ✨ panel is **300–760px wide**, so two tabs fill it, the rest scroll out of reach, and **the first thing a tab strip truncates is the NAME** — which is the only thing telling two conversations apart. What shipped is the same IDE's OTHER row: **a name, a clock, and a ＋**, with the list as a popover that shows every row at full length at any width. ⚠ **AND IT IS A SECOND ROW, NOT MORE BUTTONS IN THE TITLE BAR** — on the floating dock that bar is the drag handle (E113), and three more press targets in it is three more ways to fail to pick the window up. ⚠ **THE POPOVER IS OPAQUE EVEN WHEN THE PANEL IS SEE-THROUGH** (E115): `--ec-opacity` fades the ground a conversation sits on, and a list of NAMES over a moving timeline at 40% is not a list anybody can read. ⚠ **`--ec-ctl` MOVED FROM `.ec-head` TO `.ec-panel`** and that is load-bearing, not tidying: the new row is a SIBLING of the title bar, so a token declared on the title bar reached it as nothing at all and its buttons collapsed to their text — same family as E65 ("buttons side by side are one box, one top edge"). ⚠ **THE DESTRUCTIVE ACTION ASKS IN THE ROW, NOT IN A DIALOG.** Deleting a conversation cannot be undone from anywhere in this app, so it is asked — but a native `confirm()` over a floating panel is the one modal this whole feature was built to avoid, and it lands in the middle of the screen the panel deliberately sits beside. ⚠ **AND A CHAT IS NOT CREATED UNTIL SOMEBODY SPEAKS IN IT** — ＋ posts nothing and empties the panel; the autosave files the chat when the first turn lands. Five presses of ＋ therefore leave nothing behind, which is the same rule the editor learned about blank projects (**E118**). Pinned by `tests/editor_chat_render_check.py` §6c. | PAKKA |
| **E137** | Any panel that AUTOSAVES what somebody is typing into a list it also reorders | ⚠ **THE ID IS NOT ENOUGH TO SAY WHICH CONVERSATION THE PANEL IS ON.** A brand-new chat and the one before it are BOTH `""` until the first save comes back, so a create that lands after the person pressed ＋ adopts the id it was handed and **yanks them into the chat they just left, with an empty log on screen**. `useChatSessions` carries an EPOCH counter bumped by ＋, by opening another chat and by deleting one; the create only adopts its id when the epoch is unchanged. The chat is still filed either way — that is the whole point — it just must not be reopened underneath somebody. ⚠ **ONE WRITE IN FLIGHT AT A TIME, AND THE NEWEST WINS.** Two overlapping PUTs can land in either order and the older body arriving second is a chat that silently loses its last message; every write goes through one promise chain. ⚠ **THE SPINNER BELONGS TO THE NEWEST FETCH, BY TOKEN** — not to "is this still the open chat", because pressing ＋ mid-fetch leaves no open chat at all and a spinner keyed on that question is never cleared by anyone. ⚠ **AND THE AUTOSAVE IS KEYED ON CONTENT, NOT ON RENDERS.** The panel re-renders every second while a turn is in flight (the elapsed counter, F9); a save keyed on renders is a PUT a second for the whole of a 35s look. Debounced 800ms, and skipped outright when the serialised body equals what was last written. | PAKKA |
| **E138** | Any limit on how much of a customer's WORK is kept — chats today, versions, takes or uploads tomorrow | ⚠ **A NUMBER ONLY AN SSH SESSION CAN CHANGE IS NOT A SETTING, IT IS A CONSTANT WITH EXTRA STEPS.** E135's three ceilings shipped as `API_MAX_CHAT_SESSIONS_PER_PROJECT`, `API_MAX_CHAT_SESSION_CHARS` and a hard-coded 60 in the browser — and the operator's answer was immediate: *"isme admin panel mai v daalo, mai limit set kar dunga — mai jitna daalun wahi hona chahiye, mai handle kar lunga"*. They are `max_chats_per_project`, `chat_history_keep` and `max_chat_chars` in `chat_settings.LIMITS`, beside `opacity` and `blur`, on the Chat tab under **"What a project keeps"**. ⚠ **THE ENV VARS WERE DELETED, NOT KEPT AS A CEILING OVER THE PANEL.** A hidden maximum that quietly overrules what the operator typed is exactly the fault `opacity`'s floor of 40 was (**E115**): the screen says one thing and the product does another, and nobody can see which. ⚠ **`0` MEANS NO LIMIT, AND IT HAS TO.** An operator who does not want a ceiling must be able to SAY so; "set it to 500 and hope" is not saying so. And `0` read as falsey and replaced by the default is the identical bug `opacity: 0` had on this same router — **zero is an ANSWER on these fields, so it is `> 0`, never `or`.** ⚠ **THE BOUNDS TRAVEL WITH THE VALUES** (`admin_payload().limits` → the number input's `min`/`max`), so the browser cannot accept a figure the store will silently clamp; a second copy of the bounds in JSX is a settings page that lies about what it saved. ⚠ **AND THE CLIENT MUST NOT TRIM FIRST — THIS IS THE ONE THAT FAILS SILENTLY.** The browser caps what it mirrors (`MAX_KEPT` in `chat_sessions.js`); if that cap is BELOW the admin field's maximum, the turns are gone before the server ever sees them and the operator's number stops meaning anything above it, with nothing on screen to say so. The browser's cap therefore equals `LIMITS["chat_history_keep"]["max"]` and the two move together — **pinned by `tests/chat_sessions_check.py` §11, which reads the constant out of the `.js` file rather than trusting a comment.** ⚠ **AND WHAT GOING OVER EACH ONE *DOES* IS PRINTED BESIDE THEM**: two refuse out loud, one drops the oldest messages — an operator typing a number has no way to tell which is which from a number field, and "it silently deleted things" is the report you get if they guess wrong. §11 has 24 checks; every one moves the setting and then proves the ROUTE changed with it, because reading the store back only proves the store can hold a number. | PAKKA |
| **E139** | ANY box that opens over an existing value and selects it — a rename, an inline title, a duration field | ⚠ **`select()` MUST HAPPEN ONCE, ON OPEN. RUN IT AGAIN AND THE FIELD CAN NEVER HOLD MORE THAN ONE CHARACTER.** The ✨ chat's rename box shipped with the select re-running on every change, so each new letter arrived at a fully highlighted field and replaced what was there: type "c", type "a", the box says "a". Reported exactly that way — *"jab mai rename kar rahun to ek letter likha raha hai, dusra letter likhta hun to pahla ko hata de raha hai same jagah pe"*. ⚠ **AND IT IS SILENT — NOTHING THROWS, NOTHING WARNS.** It looks like a broken keyboard, not a bug in a dependency array, which is why it survived a green build and 17 green suites. ⚠ **THE TWO WAYS TO GET IT WRONG ARE THE TWO WAYS TO WRITE IT.** A **ref callback** runs on EVERY render, so it needs `if (el && document.activeElement !== el)` — the guard IS the fix. An **effect** must depend on whether the box is OPEN, never on the value it holds; `useEffect(…, [text])` re-selects on every keystroke and is the same bug wearing different clothes. ⚠ **AND THE APP ALREADY KNEW.** `MediaBin.jsx`'s rename box had been bitten by this and carries the `activeElement` guard with a comment naming the trap; the chat panel was written without reading it and re-derived it wrong. **One idiom, in both places** — two ways to open a rename box in one app is two places to get this identical bug, and the existing one is the one to copy (the standing rule: read how the current workflows build a control and copy it exactly). It is also a ref callback rather than `autoFocus` for MediaBin's other reason: plain autofocus lets the browser scroll the field into view, which in a scrolling pane jumps the list under the pointer. ⚠ **A RENDER TEST CANNOT SEE THIS** — `renderToStaticMarkup` runs no effects and there is no DOM, and jsdom is a dependency this project does not have and does not want. So `tests/editor_chat_render_check.py` §6d reads the mechanism **out of the source file**, and the guard is proved to fail with it removed. | PAKKA |
| **E140** | ANY "new one" button governed by a ceiling — a new chat today, a new project, take or version tomorrow | ⚠ **THE BUTTON MUST ASK BEFORE IT OPENS SOMETHING, NOT AFTER THE PERSON HAS FILLED IT IN.** The ✨ chat's ＋ shipped with the ceiling enforced ONLY on the server, at the autosave. So a project already holding its allowance answered ＋ with a cheerful empty *"New chat"*, and the refusal (409) arrived once a whole message had been typed. Reported from a live deployment the same hour the limit became an admin field, with it set to 1: *"maine admin panel mai ek likha, to yaha pe new chat open hua — kya ye sahi hai?"*. It was not: **the panel opened a thing it already knew it could not save.** ⚠ **THE SERVER GUARD WAS NEVER THE BUG — IT IS STILL THE ONLY REAL ONE** (another tab, a lowered ceiling, a direct POST). What was missing is that the CLIENT had every fact it needed to answer first: the list and the ceiling both come down on `GET /sessions`. A refusal that can be given before the work is a refusal that must be. ⚠ **AND "AS MANY ROWS AS THE CEILING" IS THE WRONG TEST.** The server sweeps chats **nobody ever typed in** before it refuses (`drop_one_unused`), so a project whose rows include an empty one still has room — that empty row is what makes it. A client answering "full" there greys out a button the server would have honoured, which is the mirror-image bug and just as confusing. `isFull(sessions, limit)` in `chat_sessions.js` is the one place that decides, and `limit: 0` is **no ceiling**, never "no room" (E138's rule, and `opacity: 0`'s before it). ⚠ **DISABLED, NOT HIDDEN, WITH THE WAY OUT IN THE TOOLTIP.** A control that vanishes at a limit reads as a feature that broke; one visibly unavailable that says *"delete one to start another"* on hover is a rule somebody can act on. Visible text stays short and the explanation is on hover — the standing rule for hints in this app. ⚠ **AND THE FOOTER STOPS PROMISING.** A ceiling can be lowered in the admin panel while somebody is part-way through an unsaved chat, and *"saved with this project"* is then a promise the panel cannot keep; it says the project is full instead, in `--warn`. Pinned by `tests/editor_chat_render_check.py` §6e (proved to fail with the `disabled` removed) and `tests/chat_store_check.mjs` §1b (9 checks on `isFull`, the empty-row case among them). | PAKKA |
| **E141** | ANY standing sentence on a narrow panel — a caption under the box a person TYPES INTO, a note under a plan, any "why you can't do this" line | ⚠ **A FIXED TWO-ROW BOX WITH A PERMANENT CAPTION UNDER IT SPENDS THE PANEL'S LAST STRIP ON A SENTENCE NOBODY READS TWICE.** Paid for live: a four-line instruction — *"is shorts/reel ke hisaab se sound effects and background music lago pura story pe aur transition and effects ke saath"* — was typed into a `rows={2}` textarea, so the middle of the person's OWN request scrolled out of sight behind an inner scrollbar at the exact moment they were checking it before pressing Send, while *"Enter to send · I'll show you any edit before it happens"* sat below it on every single turn: *"ye text hatao … is text ke jagah ko chat mai use karo, send button hi rahne do"*. ⚠ **THE FIX IS BOTH HALVES OR NEITHER**: the caption moved onto Send's own tooltip (helper text goes on hover in this app — the button is what the promise is about), AND the box auto-grows UPWARD into the panel to **45% of that panel's own height** — a percentage, not a pixel count, because the panel is resizable and dockable and one hard-coded max is wrong on three of its four shapes. `resize` is OFF while autosize is on: a dragged height and a typed one fight, and the corner grip was what people grabbed instead of just typing. ⚠ **WHAT STAYS IN THAT ROW IS ONLY WHAT IS TRUE RIGHT NOW** — *Thinking… 12s* / *Making the edit…* is a STATE and it leaves when the turn lands; a caption is not. Pinned by `tests/editor_chat_render_check.py`, which now asserts an idle composer draws **no** hint row at all while the promise itself is still on the panel. ⚠ **AND THE SAME RULE CAUGHT A SECOND SENTENCE ON THE SAME PANEL** — a restored-from-storage plan explained itself in three lines of *"This plan was from before the page reloaded, so it can't be applied now — the shot numbers may mean something else…"*, bigger on screen than the plan it was refusing: *"thora short mai rakho, information itna bara achha nahi lag raha hai"*. Now **⏳ Old plan — ask again.** with the reason on hover, matching ↩ *Put back — the film is as it was.* beside it. **A note about what you CANNOT do must never be longer than the thing you can**: visible text stays 3–5 words, the explanation goes in `title`. ⚠ **AND A GROWING BOX MEASURES ITSELF THE WAY `GrowTextarea.jsx` ALREADY DOES** — `el.scrollHeight + (el.offsetHeight - el.clientHeight) + 1`, never a bare `scrollHeight`, which is short by the BORDER (`box-sizing: border-box` is global) and short again by the SUB-PIXEL an integer throws away; under the `overflow: hidden` such a box sets while it fits, either one **eats the bottom of the last line**, which is the exact fault the growing was asked for. This app has now paid for that twice before (the shot description, and the offer form's bullet box) and the composer nearly made it three. ⚠ **AND ONE MECHANISM OWNS THE HEIGHT** — no `resize` handle and no `field-sizing: content` beside a JS autosize, for the same reason two owners of anything is a bug: the browser's sizing knows nothing about the cap. Pinned by `tests/editor_chat_render_check.py`. | PAKKA |
| **E142** | ANY CLOCK A CUSTOMER CAN RUN OUT OF — the ✨ AI Editor's per-message budget today, a render or upload timeout tomorrow | ⚠ **A TIMEOUT IS A SETTING, AND A SETTING AN OPERATOR CANNOT REACH IS A CONSTANT WITH A LONGER NAME.** *Paid for live:* a real 504 on a real board — *"is shorts/reel ke hisaab se sound effects and background music lago pura story pe aur transition and effects ke saath"*, **three jobs in one message on a full film**, the most expensive shape this feature has — answered with *"120s is all one call gets (CHAT_BUDGET_SECONDS)"*, at a person with no shell: *"kya tum admin panel mai banaye ho limit set karne wala"*. No. E138's rule, one feature over. It is `chat_settings.turn_seconds` now, **90–180, default 120**. ⚠ **AND THE OPERATOR TYPES ONE NUMBER, NEVER THREE.** Three clocks have to agree — the model call, the server's wait, the browser's wait — and this trio had already been raised **by hand twice** (70/90 → 120/150). The hand-raise IS the bug: move the model's clock alone and the tab starts aborting turns the server is still correctly serving, which **bills the customer for an answer they never see**. The other two are derived (`wire_wait_seconds` = `turn_seconds + 30`) and the tab is TOLD its number on `/editor-chat/config`; `api.js`'s constant survives only as the floor before that response lands. ⚠ **BOUNDS ARE ASSERTED AGAINST MEASUREMENTS, NEVER AGAINST THEMSELVES** — this is what makes them the width of the input rather than a second opinion. The **floor is 90 because 70 is the number that failed live**: a look is 27–35s measured and a malformed answer buys a SECOND paid call inside the same attempt, so 2 × 35 = 70 **exactly**, with nothing left to mend anything with. The **ceiling is 180 because the socket stops there** (`llm_json.DEFAULT_TIMEOUT_SECONDS`) — 240 would have saved in the panel and been quietly ignored by the connection, which is the same bug one layer down. ⚠ **THE NUMBER REACHES THE CALL FROM ABOVE, AND IT BEATS THE `.env`** — `JsonRequest.budget_seconds` carries it, because `llm_json` is imported by the worker and by tests with no database and must never depend on a settings store; a setting that loses to a default is D5's *"I set it and nothing happened"*. ⚠ **AND THE TIMEOUT SENTENCE NAMES A PLACE THEY CAN GO** — an ALL-CAPS source prints as a variable, anything else prints as prose (*"one message gets 120s. Ask for one thing at a time, or raise the limit in the admin panel"*). `CHAT_BUDGET_SECONDS` survives as the **seed for a fresh deploy's default only**, read once, so nobody's raised deployment silently drops back to 120. Pinned by `tests/director_timeout_check.py`, which checks the derived pair **at both ends of the range** — a rule that only holds at the default breaks the day somebody uses the setting — and reads the agent, the route and the hook to prove the wiring exists. | PAKKA |
| **E143** | ANY admin-panel NUMBER box that autosaves — every field on the Chat tab today, any settings number tomorrow | ⚠ **A BOX THAT SAVES ON EVERY KEYSTROKE CANNOT BE TYPED INTO ONCE THE FLOOR HAS TWO DIGITS.** Each keystroke was a real save: the server clamps the half-typed number to the bound, `setRow` writes the clamped value straight back into a CONTROLLED input, and the box changes under the operator's hands. Invisible for months because every floor was 0, 4 or 10 — a first digit already clears them — and unusable the instant E142's *Seconds one message may take* arrived with a floor of **90**: typing `150` saves `1`, is clamped to `90`, and you are now editing "90". **"Characters kept per conversation" (floor 20,000) could never be typed at all** — `4` becomes `20000` before the second digit lands. Caught the moment the operator opened the new field and asked *"mai sahi jagah hun ya nhi"*. ⚠ **THE FIX IS THE MANNERS THE MODEL FIELD IN THE SAME PANEL ALWAYS HAD**: uncontrolled + `defaultValue`, commit on **blur** (Enter blurs), `key`ed on the stored value so the box still snaps to whatever the server really kept — a clamp included. ⚠ **AND AN EMPTY BOX IS NOT A ZERO** — `Number("")` is `0` and `0` is a REAL setting on two of these rows ("no ceiling"); someone who cleared the field to retype and clicked away has asked for nothing, so it is put back rather than saved. ⚠ **THE GENERAL RULE: a bound that CLAMPS must never be enforced against a value the person has not finished typing.** | PAKKA |
| **E144** | ANY ONE MESSAGE THAT IS REALLY N JOBS — the ✨ AI Editor's "sound, music, transitions and effects on the whole film" today; a bulk retouch, a bulk re-render tomorrow | ⚠ **THE PROBLEM IS THE LENGTH OF THE ANSWER, NOT THE DIFFICULTY OF THE JOB, AND NO TIMEOUT FIXES IT.** A text model is slowest at WRITING: four jobs over sixty shots is three hundred lines in one reply, which is what produced E142's live 504. Raising the clock only moves where it breaks — **a single HTTP request cannot be made reliable at ten minutes**, because the socket (180s), the proxy (~100s) and the browser each cut it on their own schedule. ⚠ **SO A BIG MESSAGE BECOMES A JOB, NOT A LONGER REQUEST.** The turn returns a BRIEF (`kind: "work"`, `work.tasks[]`), the route hands it to `server/editor_chat_work.py`, and the id comes back at once; the panel polls, shows a bar, and can Stop. *"Unlimited plan ka matlab unlimited MESSAGES hai, ek message unlimited lamba nahi"* — and the turn is billed **once**, at the turn, never per batch, or the same request would cost more on a long film than a short one. ⚠ **AND THE SPEED COMES FROM SPLITTING, NEVER FROM COMPRESSING THE JUDGEMENT.** The operator caught the wrong version of this before it was built: *"1 niyam likhne do to har clip pe dissolve hi laga dega na — magar mujhe to chahiye ki do shot ke bich ko samajh kar jo jaruri hai wo lage"*. Exactly — one blanket rule would have undone the whole per-cut transition fix. **Nothing is compressed**: each batch still reads real shot descriptions, still runs the same system prompt, and still decides cut by cut. It is asked about TWELVE cuts instead of sixty, and four batches run at once — so the job is about as long as its LONGEST part, not the sum. ⚠ **A SHORTER ANSWER IS ALSO A MORE ACCURATE ONE** — `llm_json` already measures that long answers arrive malformed and buy a second paid repair call. ⚠ **FIVE THINGS THAT ARE NOT OPTIONAL, EACH PAID FOR IN THE TEST BEFORE IT EVER RAN LIVE**: (1) **the window a batch SEES is not the range it WRITES FOR** — a cut is about two shots, so it sees one past each end and must not write there; conflating the two produced 40 steps on a 36-shot film, and the merge now REFUSES an out-of-range step rather than trusting the prompt. (2) **Sound is ONE call, not a dozen** — its answer is three words a shot, and splitting it would give the film five different music beds. (3) **One batch failing is not the job failing** — the good steps still come back and the reply NAMES what was missed. (4) **Stop stops the SPEND, not the wait**, and what was written stays applicable — so the panel says "Stop here", not "Stop waiting". (5) **A record left RUNNING with no runner is `lost`, said in words** — a bar that never moves again is the worst of the three possible answers. ⚠ **AND THE MERGED ANSWER IS AN ORDINARY TURN** — same `normaliseTurn`, `validatePlan`, `applyGuardrails`, preview, Apply and Undo; a big job is not a second kind of edit with a second set of rules. ⚠ `asked_for_all` is **false** on it, or the guardrails would expand steps that are already per-shot. ⚠ **AND THE FIRST LIVE RUN FOUND THE ONE THING NO FAKE MODEL COULD**: *"transition and effects ke saath"* came back with **12 perfect, varied transitions** (dissolve, split, diamond, slide, dip — the per-cut judgement survived the split, which was the whole worry) and **every single effect thrown away** — eight rows of *"add_effect: the step named no effect to add"*. `plan_schema`'s `args` is a **FLAT UNION of every verb's argument names**, so a batch holding `add_transition` AND `add_effect` was offered `kind` and `effect` side by side and reached for the one it had just used a dozen times. **THE PROMPT CANNOT FIX THIS** — E106/E123 are the same lesson twice. `batch_schema` now narrows `args` to the arguments THIS batch's verbs really take, so `kind` is not in an effects batch's schema at all; the batch prompt also names its verbs with their exact arguments (`add_effect(shot, effect, params)`) on their own line, and the turn prompt says transitions and effects are TWO tasks — which is also faster, since two tasks run at the same time and one combined task runs once. ⚠ **AND DUPLICATE SOUND CUES ARE MERGED**, not reported: one cue per shot, because *"shot 14 already has a sound cued"* under a red "couldn't use" heading reads as the app failing rather than as the duplicate it is. ⚠ **AND THE SECOND LIVE RUN FOUND THE ONE FAILURE A FAN-OUT INVENTS.** *"2 parts did not come back, so those shots were left alone"* — on a **fourteen-shot** film, which is not a hard job. `llm_json` already retries a call three times, so a batch that still failed did not fail for its OWN reasons: **it failed because three other calls were in the air at the same moment**. A single request never had that failure mode; splitting the work created it. The answer is the SHAPE of the retry, not more of them — the lost units are tried **once more, serially, after the wave**, when the burst is over; anything still failing is named and left alone. ⚠ **AND THE REASON REACHES THE SCREEN** — "2 parts did not come back" and nothing else tells the person something broke and gives them nothing to do about it; the reason now rides on `dropped`, which the panel already draws, so a rate limit and a dead key stop looking like the same event. ⚠ **THE REPLY ALSO COUNTS THE WAY THE PANEL COUNTS** — it said *"13 edits"* over a button saying *"Apply 27 edits"* because it counted a `note` as an edit, which the preview's chips never have: two numbers for one thing, neither obviously wrong, is worse than either. Pinned by `tests/editor_chat_work_check.py` (79 checks, no model called; its fake adapters overshoot their range, run slow, fail under load only, and lose a batch outright). | PAKKA |
| **E145** | ANY CAPTION IN A LANGUAGE THAT IS NOT WRITTEN IN LATIN LETTERS — Hindi, Marathi, Punjabi, Bengali, Gujarati, Odia, Tamil, Telugu, Kannada, Malayalam, Arabic, Urdu, Hebrew, Thai | ⚠ **THE PREVIEW WAS RIGHT AND THE MP4 WAS WRONG, WHICH IS WHY NOBODY SAW IT.** Most of the world's writing does not go one character to one glyph: `हिन्दी` is six characters and four glyphs, in a different order, and turning one into the other is called SHAPING. The browser has always shaped. The exporter never did — Pillow shapes through **libraqm**, its wheel already carries libraqm and HarfBuzz, and libraqm looks for **FriBiDi** by name while `PIL.ImageFont` is first imported and, not finding it, **switches itself off silently with a working Pillow left behind.** So `draw.text` kept drawing, in typed order, unjoined: `हिन्दी` → `हनि्दी`, `क्षत्रिय` → `क् षत् रयि`. **Every Hindi caption this app ever exported was malformed**, and the Program monitor showed it correctly the whole time. ⚠ **THIS IS THE EXACT BUG `animatic_fonts.py` WAS WRITTEN TO KILL, THROUGH A DOOR IT DID NOT WATCH** — both sides loaded the same .ttf and disagreed anyway, because the same file drawn by two different text engines is two different pictures. Bundling the font was never sufficient; bundling the *engine* is. ⚠ **THE FIX IS AN IMPORT ORDER, AND IT IS ONE LINE WIDE.** `text_shaping.py` pulls FriBiDi into the process with `ctypes` (never PATH — this app spawns ffmpeg and PATH is inherited) and **must be imported before `PIL.ImageFont`**; libraqm asks once, and after that the answer is final for the life of the process. `animatic.py` and `storyboard_pdf.py` import it above their PIL line with a comment saying so. Move it below and every one of these languages silently starts exporting wrong again. ⚠ **WINDOWS HAS NO PACKAGE FOR IT**, so `vendor/fribidi/libfribidi-0.dll` ships with the project (LGPL-2.1+, unmodified, hash and source in `vendor/fribidi/README.md`); **on Linux `apt-get install -y libfribidi0` is not optional** and belongs in any Dockerfile. ⚠ **AND IT FAILS LOUD, TWICE**: `tests/captions_check.py` fails outright if shaping is off (a dev box that cannot shape makes videos nobody should ship), and `build_animatic` **refuses to render** a project containing such a caption rather than finishing it wrongly — an export that stops costs a support ticket, one that finishes costs the client. | PAKKA |
| **E146** | Offering a font for a language — the picker, and anything that CREATES a caption without being asked (subtitles, the ✨ chat, a Premiere import) | ⚠ **EVERY FONT IN THE PICKER DRAWS SOMETHING, SO A WRONG ONE LOOKS LIKE A VALID CHOICE UNTIL THE RENDER.** A Hindi title set in Anton shows ▯▯▯ — in the export, after the customer has paid for it. The list grew from 14 faces to **56** covering 20 writing systems, and the thing that makes that safe is not the count: each entry's `scripts` is **read out of the .ttf's own cmap** by `tools/fonts_sync.py` and re-read by `captions_check.py` in **both directions**, so a face that claims Hindi and cannot draw it fails the suite, and so does one that quietly gained a script nobody declared. ⚠ **A CMAP ENTRY IS NOT PROOF OF INK, AND AN EMPTY GLYPH IS NOT PROOF OF ABSENCE** — both were wrong once. Fonts map characters to blank outlines (renders as a gap, reads as working); and in **Noto Nastaliq Urdu every Arabic letter is an empty stub** that GSUB substitutes before anything is drawn, so an outline-only test concluded the best Urdu font in the world contains no Urdu. The check walks GSUB/GPOS coverage — **recursively**, because Nastaliq wraps its lookups in extension subtables and a one-level walk finds nothing. ⚠ **THE FONT IS RESOLVED FROM THE WORDS, NOT ASKED FOR.** `best_font_for_text` (twinned in `fonts.js`) is called by `caption_clips`, `add_text`/`set_text` and the interchange importer, and it **keeps an explicit choice whenever that choice fits** — it only ever replaces one that would have rendered as boxes. *The worst instance was the quiet one:* `caption_clips` hard-coded `"font": "inter"`, so subtitles transcribed from a **Hindi voiceover** were burnt into the MP4 as empty boxes with nobody having chosen a font at any point. ⚠ **AND "FITS" IS NOT ENOUGH TO CHOOSE BY** — Rubik contains Arabic and sits near the top of the list, so a first-that-fits walk set every Arabic title in a Latin face; and Urdu in naskh reads to an Urdu reader as *wrong*, not as a style. The REQUIRED SCRIPTS are walked (most specific first), not the fonts, which is why Urdu gets nastaliq and Japanese gets the Japanese face rather than the Simplified Chinese one that also covers its kana. ⚠ **ONE HONEST GAP, DOCUMENTED**: Han is classified as a single script because a Chinese character does not say which region's font it wants. Simplified faces were measured to cover traditional characters; **Traditional and Japanese faces do NOT cover the Simplified set** (Noto Sans TC has no 这, 说, 们). The shelf names in the picker are the guide; a Traditional face chosen by hand for Simplified text is the one case the warning misses. | PAKKA |
| **E147** | ANY ARGUMENT NAME TYPED INTO A PROMPT OR A TEST BY HAND — the ✨ AI Editor's verbs today, any generated call to a registry tomorrow | ⚠ **THE SAME SIXTEEN EFFECTS WERE THROWN AWAY TWICE, AND THE SECOND TIME IT WAS THE FIX THAT DID IT.** *"is shorts/reel ke hisaab se sound effects and background music lago pura story pe aur transition and effects ke saath"* came back with **9 transitions, 9 sound cues, and 16 rows of `add_effect: the step named no effect to add`** — the exact screen E144 had already been paid for. E144's repair narrowed the batch schema (right, and it did not help here) and then wrote a sentence into the batch prompt: *"an `add_effect` needs `effect`; `kind` belongs to a transition"*. **That sentence is false.** `add_effect` takes **`kind`** — the very same name `add_transition` takes — so the prompt was ordering the model to fill a field this editor does not have, `fold_steps` dropped it as another verb's argument, and every effect arrived holding a shot number and nothing else. ⚠ **AND THE TEST AGREED WITH THE PROMPT, WHICH IS WHY IT STAYED GREEN.** `tests/editor_chat_work_check.py` built its fixture by hand — `add_effect(shot, effect, params)` — and asserted the app did the wrong thing correctly. A test that invents its own copy of a contract cannot catch the contract being wrong. ⚠ **THE RULE: an argument name has exactly one home, `verbVocab()` in `client/src/animatic/agent/actions.js`, and everything else READS it.** The batch prompt now names none by hand and points at the generated `<<VERBS>>` card; the test reads `actions.js` and fails if the prompt contradicts it. ⚠ **AND THE FLOOR UNDER BOTH IS `director._ARG_ALIASES`, because neither a prompt nor a schema may be the last line of defence — both were, twice, and both failed.** A synonym the named verb cannot use is now RENAMED to the one it can (`effect`, `fx`, `style`, `preset` → `kind`; `clip`/`frame` → `shot`; `duration_ms` → `ms`) instead of being dropped — an alias only ever fires when the verb has no such field of its own, so `preset` on an `apply_text_preset` is untouched, and it never overwrites what the model wrote properly. **A dropped synonym is not a dropped argument, it is a dropped EDIT.** ⚠ Same visit: `params` written as a plain object (`{"amount": "0.4"}`) is now read instead of ignored — refusing it left the effect at its default, which reads to the user as the AI not listening. ⚠ **AND THE TEST'S OWN FILTER WAS FRAGILE IN THE SAME WAY** — it picked out shot batches by sniffing for the words "SHOTS" and "ONLY" anywhere in the prompt, so writing "ONLY" into a correct paragraph turned five checks red; it matches the `YOUR PART OF IT: shots` line now. | PAKKA (the alias fold, the manifest-read test) + GUZARISH (the prompt wording) |

| **E148** | Admin → AI Editor → **Seconds one message may take** | ⚠ **A value of 180 was immediately returning to 120 after the page refreshed.** The browser sent `turn_seconds`, but `ChatSettingsBody` did not declare it; Pydantic ignored the unknown field, the PATCH returned “Nothing to change”, and the panel's error recovery reloaded the old 120. The request model now carries the field, after which `chat_settings.clean()` continues to enforce the server's 90–180 bounds. Regression-pinned in `tests/director_timeout_check.py`. | PAKKA |

| **E149** | ✨ AI Editor · leaving and returning to a project with paid AI work | ⚠ **A transcript without the plan is not saved work.** The chat store now keeps the complete validated plan, sound pass, drops, apply log, created-item refs and long-job receipt. A compact project signature makes an unapplied plan reusable after a refresh when the document is unchanged, while a changed document keeps the plan visible and disables Apply. Apply checkpoints are written as steps land, so a reload can resume only unfinished steps without duplicating created clips; a running batch job keeps its server `work_id` and resumes polling on return. Pinned by `tests/chat_store_check.mjs` and `tests/chat_sessions_check.py`. | PAKKA |

| **E150** | ✨ AI Editor · a batch response containing a giant JSON number or a malformed effect step | ⚠ **A MODEL'S BAD NUMBER MUST NOT KILL THE WHOLE PASS.** A JSON integer with 11,074 digits used to raise Python's process-level digit guard before `llm_json` could invoke its one repair retry, so an otherwise recoverable batch appeared under “things I couldn't use.” JSON integers now go through a bounded parser (the global interpreter limit is never relaxed), and the parse reason is handed to the normal repair path. ⚠ **AND AN UNAMBIGUOUS EFFECT MUST NOT BE LOST FOR COSMETIC SHAPE.** `director.fold_steps()` also accepts a provider that flattened `args` onto the step or wrapped the selected effect as `{id, params}`, while still filtering against the client manifest and never guessing an unnamed effect. Pinned by `tests/director_timeout_check.py` and `tests/editor_chat_work_check.py`. | PAKKA |

| **E151** | ✨ AI Editor → applying a plan that also carries sound, and ANY long two-phase action whose second phase is a network call | ⚠ **AN APPLY IS NOT OVER WHEN THE LAST STEP COMMITS — IT IS OVER WHEN ITS SOUND HAS LANDED.** The guard on a second Apply tested `running`, which is the STEP loop, and that goes false the instant the last verb runs while the sound half is still at the library for up to the whole request clock. So a second plan was applied on top of a film the first apply had not finished: **both cards said "✓ Applied"**, and because `snapshotRef` and `revertable` are single-valued the second apply silently ate the first one's Undo and pointed the snapshot at a half-edited document. Reported live 2026-09-06: *"abhi dono applied ka v aa raha hai — aisa nhi hona chahiye, ek ki ho ye nhi ki dono"*. ⚠ **THE GUARD IS A REF, NOT STATE** (`applyBusyRef`): two clicks in one React batch both read the old state and both start. ⚠ **AND IT IS RELEASED IN `finish`, ON BOTH PATHS** — a guard that jams shut leaves Apply dead for the session, which is worse than the race. ⚠ **TALKING STAYS FREE.** The composer's `busy` is unchanged and `applyBusy` is a second, stricter one: taking the keyboard away for the two minutes a sound search can take would be a worse panel than the bug. | **PAKKA** (`tests/editor_chat_work_check.py`) |

| **E152** | ANY status line drawn from a hook's global flag — the ✨ chat's spinners today, a progress line beside a list row tomorrow | ⚠ **A STATUS LINE MUST NAME THE TURN IT BELONGS TO.** `scoring` was a bare string and `running` a bare boolean, so the panel drew *"⏳ Finding 15 sounds…"* under **every applied plan in the scrollback** and a sound-only card said *"Making the edit…"* about somebody else's apply. One apply was in flight; two cards claimed it, and the person quite reasonably read that as two edits running at once — *"upar wala v kyun chal raha tha"*. The fix is not a nicer sentence: it is `runningTurn` / `scoringTurn` beside the flag, and every line keyed off `=== turn.id`. **A flag with no owner cannot be rendered per row.** | **PAKKA** (`tests/editor_chat_render_check.py`) |

| **E153** | ✨ AI Editor → a big job whose wait fails, is stopped, or times out | ⚠ **A TURN THAT ENDED BADLY MUST NOT STAY MARKED `running`.** The user row is stamped `work_state: "running"` when a job starts and the catch in `send` left it that way — so the RESUME effect, which exists for a page **reload**, found a "running" job on the very next render and started polling it again behind the person's back while they were already asking something else. ⚠ **AND THE RESUME MUST TAKE THE NEWEST PENDING JOB, NOT THE FIRST** (`find` took the oldest — an abandoned question), must skip one an agent reply has **already answered**, and must never run while another request owns `abortRef` (Stop would reach the wrong call). Live 2026-09-06. | **PAKKA** (`tests/editor_chat_work_check.py`) |

| **E154** | ANY verb whose argument is chosen from a list — `add_effect`/`add_transition`/`add_shape`/`add_shot_motion`'s `kind` today | ⚠ **AN ENUM, NOT A SENTENCE. THIS BUG CAME BACK THREE TIMES.** `args` on the wire is a FLAT union of every verb's argument names (no unions in the schema), and `add_effect` and `add_transition` **both** call theirs `kind` — so E147's argument narrowing removes nothing for exactly the pair that needed separating, and the prompt fix before it was one more sentence to ignore. A batch asked for *"transition and effects ke saath"* wrote `kind` on the transitions and left it off **every** effect: 14 rows of *"add_effect: the step named no effect to add"* on a live 14-shot reel, 2026-09-06. ⚠ **THE FIX IS `_pin_kind`**: the field becomes an `enum` of the family's real ids, and `required` whenever every verb in the batch takes one. **A model cannot leave an enumerated field blank and cannot invent a value for it** — which is the one guarantee neither a prompt nor a filter can give. ⚠ **THE FAMILY IS DECLARED ON THE VERB** (`family:` in `actions.js`, emitted by `verbVocab()`) so the ids are read off the manifest the client sent, never off a table on the server — E147's rule, applied to values instead of names. ⚠ **AND A CUT IS NOT A SHOT**: the same run asked for a transition on "cut 14" of a 14-shot film. The batch is told its range in SHOTS, so the verb card now states the cut range outright. | **PAKKA** (`tests/editor_chat_work_check.py`) |

| **E155** | Any "what I couldn't use" list | ⚠ **THE SAME REASON FIFTEEN TIMES IS ONE FACT, NOT FIFTEEN.** The drops panel opened onto fifteen identical copies of one message, which reads as fifteen unrelated breakages and buried the two lines that were genuinely different (a rate-limited pass, a transition past the end of the film). Fold the repeats and show `×N`; **the count in the header stays honest** — fifteen steps really were lost, only the repetition is folded. | **PAKKA** (`EditorChat.jsx`) |

## F · Waiting, progress and drafts

`BreakdownProgress.jsx`, `ScriptToStoryboard.jsx`, `StoryboardLibrary.jsx`

| # | Scenario | The rule | Status |
|---|---|---|---|
| **F1** | Two chained calls, one wait | **ONE ring, mounted once.** Two elements or two `key`s are two instances and the second climbs from zero — reported as "a bar finished and a new one started". A non-final call hands off **where it stands**, with no sprint to a ceiling. | PAKKA |
| **F2** | A long wait | **The fill decelerates and never parks.** Rate is set by the distance still to go, so it is still creeping a minute in; it stops at 96, and **paints 100 for 220ms before handing over** or React never renders it. ⚠ 99% is the worst number to park on — it reads as stuck, not working. | PAKKA |
| **F3** | An unfinished board from an earlier session | **Offered, never taken.** A banner carrying its SHOT COUNT (which is what makes a stale board recognisable before opening it), with Resume and a Discard that asks first. ⚠ A first-mount-wins flag does NOT work — StrictMode mounts twice in dev. | PAKKA |
| **F4** | Anything the user has EDITED BY HAND | **It must survive a refresh.** ⚠ The test is not "can we rebuild it" but **"does rebuilding give back the same thing"** — a concept re-generated from the same brief is a *different film*, so the card was unrecoverable and every hand edit with it. *Found after four rounds of testing in one afternoon: the script box was saved and the concept card was not.* It rides on the SAME draft row as the text it came from — one draft, one row — and `null` clears it on approve and Start over. Compare **by value** before saving; the card is a new object every keystroke. ⚠ **Restoring it must NOT reopen the screen — see F5.** ~~Offer it on the form~~ was tried and `workflow_mount_check.py` proved that unreachable (the only route to the form is "New storyboard", which clears the concept on the way past); ~~reopen it on every fresh page load~~ was the fix for that and was worse. The offer belongs on the **library**, which is the screen a cold start actually reaches. | PAKKA |
| **F5** | Opening a workflow with unfinished work saved | **The front door is the front door. NOTHING auto-opens a deeper screen — unfinished work waits in the LIBRARY as a row, and the user chooses.** *"jab user open kar raha hai script to storyboard to first page khulna chahiye, nhi ki ye page."* The concept card was reopening itself on every fresh page load, so the workflow opened on two different screens depending on how old the tab was. ⚠ **AND CHECK THAT THE SAVED THING IS STILL ALIVE.** Nothing ever cleared a SPENT concept — `api.clearScriptDraft` was written and never called from anywhere — so the card being reopened was usually the plan for a film already generated, exported and forgotten weeks earlier. A draft is now dropped the moment it becomes the next stage (`finishBreakdown` → `setConcept(null)`), or the library grows a second row for the same film. ⚠ A row that offers work is only honest if discarding it clears the **server** slot too, or it is back on the next load. This is the SAME rule as F3, learned twice; F3 was fixed and the concept card kept overriding it. | PAKKA |
| **F6** | A "still loading / still slow" warning | **DISARM THE TIMER WHEN THE REQUEST FINISHES — unsetting the flag is not the same thing.** The library's 10-second banner was cleared only in the effect's CLEANUP (which runs on unmount, not on the request settling); the `finally` merely set `slow` back to false, which does nothing to a timer that has not fired yet. A list that answered in 300ms left a live timer behind, and ten seconds later — on a screen already showing every board — up came *"Still loading your storyboards … usually a database it needs (MongoDB) being unreachable"*, and it stayed. *"mai bar bar ye notice dekh rah hun."* ⚠ **The banner ACCUSED the database and the database was innocent** — measured while chasing it: Mongo 0.01s, the size walk 0.03s. A warning that fires after the thing it warns about has SUCCEEDED teaches the user to ignore the banner on the day it is telling the truth. ⚠ A static read of the file cannot see a timer fire — pinned in Chromium, `workflow_mount_check.py` [8], and the check was **proved to fail on the old code first**. Compare `BreakdownProgress.jsx`, which guards its own slow timer on `doneRef.current` and got this right. | PAKKA |
| **F7** | ✨ AI Editor · a chat turn that is slower than the tab | ⚠ **THE SERVER'S BUDGET MUST BE SMALLER THAN THE BROWSER'S PATIENCE, AND IT WAS BIGGER.** The chat shared the Director's `DIRECTOR_BUDGET_SECONDS` (**135s**) while `CHAT_TURN_TIMEOUT_MS` in `client/src/api.js` gave up at **90s** — so every turn slower than ninety seconds died the same way: the tab aborted a request the server was still correctly serving, the model call ran on to the end, `chat_turns` was **billed and counted**, and what the user read was *"The server didn't respond within 90s. It may be stuck (a database it needs can do this)"* — a true sentence about the wrong component, over three identical unanswered messages. `llm_json.py` grew `CAPABILITY_BUDGET_SECONDS = {"chat": 70}` and `budget_seconds(capability)`; the reason it prints now names **the var that actually set the clock**, not the Director's. ⚠ **THE CAPABILITY'S NUMBER IS A CEILING, NOT A FALLBACK** — raising `DIRECTOR_BUDGET_SECONDS` on a deployment must not drag the chat back past the tab, so the chat takes the SMALLER of the two and `CHAT_BUDGET_SECONDS` is the only way to say *let this one run longer* (raise the tab's two numbers with it). ⚠ **THREE NUMBERS, ONE ORDER**: `llm_json` 70s **<** `API_CHAT_TURN_TIMEOUT_S` 90s **==** `CHAT_TURN_TIMEOUT_MS` 90000. Pinned by `tests/director_timeout_check.py`, which reads all three out of their own files. ⚠ **THOSE THREE VALUES MOVED — SEE F12** (`120 < 150 == 150000`). The ORDER is the rule here; the numbers are not, and a row that names stale ones gets believed. ⚠ Note `server/config.py`'s `CHAT_TURN_TIMEOUT_S` was a **mirror of the tab and nothing else** — defined, documented as the server's, and read by no code. Do not mistake a documented constant for an enforced one. | PAKKA |
| **F8** | Any wait the user sits and watches | ⚠ **A SPINNER SAYS "SOMETHING IS HAPPENING"; ONLY A NUMBER SAYS "AND IT STILL IS".** The panel showed `Thinking…` — the same two words, unchanged — for ninety seconds, so a healthy 40s turn and a wedged one looked identical, and what the user did was **send the same message three times**: three turns billed for one question, and each retry made the next prompt bigger and the model slower at exactly the moment it was already too slow. Three things, together: a seconds counter after 5s (`.ec-elapsed`, **tabular figures** or the line twitches once a second), a **Stop waiting** button after 10s, and a duplicate guard in `send` — the same sentence typed twice in a row **replaces** the unanswered attempt instead of stacking on it (⚠ only when nothing answered it; the same request after a reply is a real second ask). ⚠ **STOP STOPS THE WAIT, NOT THE SPEND.** The request is genuinely aborted (`AbortController` relayed through `fetchWithRetry`, and a stop is flagged `e.stopped` so it is never dressed up as *the backend isn't running*), but the server was already asked and the turn still counts — **the line it writes says so**, because a Stop button most people would read as *cancel the charge* has to correct that itself. | PAKKA |
| **F9** | Any wait you will later be asked to EXPLAIN — every `llm_json` call, the ✨ chat above all | ⚠ **A CALL THAT DOES NOT SAY HOW LONG IT TOOK CANNOT BE DIAGNOSED, AND "IT TOOK 90 SECONDS" IS A COMPLAINT, NOT A FINDING.** F7 and F8 made the clock honest and the wait visible, and left the actual question open for a fortnight — was that ONE slow model call, THREE attempts, or one fast call plus a repair? — because `llm_json` logged the **start** of an attempt and nothing else. A turn that SUCCEEDED slowly produced one line with no duration anywhere in it, and those are the turns people complain about. Now every model call is timed and every timing is logged **on success too**: `attempt N/3: the model answered in X.Xs`, a separately timed `repair call took X.Xs`, `attempt N/3 FAILED after X.Xs`, and a closing `DONE in X.Xs — N attempt(s), N model call(s), of a Ns budget (VAR)`. ⚠ **ATTEMPTS AND MODEL CALLS ARE DIFFERENT NUMBERS** — a repair is a second paid round trip *inside* attempt 1, so "attempt 1/3" can be two calls and twice the wall clock; the tally prints both or that reading is invisible. ⚠ **AND THE ROUTE TIMES ITSELF SEPARATELY FROM THE MODEL** (`turn_started` in `server/editor_chat.py`): when the two numbers match the model is the story, when they diverge this app is, and nothing measured the difference before. ⚠ **HOW LONG IT TOOK TO *FAIL* IS THE DIAGNOSIS, NOT A DETAIL** — a 429 back in 0.4s is a quota wall, the same sentence after 60s is the SDK having retried it for us, and "raise the budget" is the wrong fix for the second. ⚠ **THE SIZES RIDE ON THE FIRST ATTEMPT LINE** (`system=`, `prompt=`, `schema=`, and `images=N (X.XKB)` **only when there are pictures** — `images=0` on every Director line is noise, `images=5` is the finding, see E112), so "is the prompt the suspect?" is answered by the log instead of measured by hand once and written into a markdown file where it goes stale. ⚠ **START THE STOPWATCH AFTER THE LOG LINE, NOT BEFORE IT**: building that line calls `model_id()`, which **imports `script_breakdown` on a cold worker** — a full second charged to the model in the first draft of this, and the first call of a process is the one people screenshot. Pinned by `tests/director_timeout_check.py`. | PAKKA |
| **F10** | Deciding whether to RETRY a model call that just failed | ⚠ **WHAT THE NEXT ATTEMPT WILL NEED IS WHAT THE LAST ONE TOOK — not a flat fifteen seconds.** `_worth_retrying` asked only "is `MIN_ATTEMPT_SECONDS` left?", which is a fine floor and a terrible estimate for exactly the calls that run out of budget. ⚠ **MEASURED LIVE, 2026-09-04, `gemini-3.5-flash` on the Developer API, same board and same turn seconds apart: text only 2.6s · the same turn with 5 stills 34.6s · with 12 stills 27.1s.** So a ✨ chat LOOK is **~10× a text turn**, and after a 35s attempt fails with 31s left the old rule said "15s is enough, go again" and bought a call that WILL be cut off — a paid request with a guaranteed-useless answer. The estimate is now `max(MIN_ATTEMPT_SECONDS, however long the last attempt actually took)`, which the stopwatch (F9) made available. ⚠ **THE FLOOR STILL PROTECTS FAST FAILURES** — a 503 that came back in 2s is not punished for a slow call's sins, and that is the whole reason the floor stays. ⚠ **AND A CALL ABANDONED WITH TIME ON THE CLOCK MUST STILL BLAME THE CLOCK**: `_with_clock` used to infer "we ran out of time" from `_time_left()` alone, which is false in exactly this new case (31s left, stopped anyway), so the caller passes `out_of_time` rather than letting the sentence guess. ⚠ **AND THE COUNT OF PICTURES IS NOT THE DRIVER — 12 stills were FASTER than 5.** Do not "fix" `MAX_LOOK_SHOTS`; it was measured and it is not the problem. | PAKKA |
| **F11** | Any provider error on its way to a person — every `except` in `llm_json` | ⚠ **A PYTHON DICT IN A CHAT BUBBLE IS NOT AN ERROR MESSAGE, IT IS A LEAK.** The generic branch interpolated the SDK's exception whole, so a busy model printed this into the ✨ conversation, verbatim: `Text API error during editor chat: 503 UNAVAILABLE. {'error': {'code': 503, 'message': 'This model is currently experiencing high demand…'}}`. ⚠ **AND THE DISTINCTION IT DESTROYED IS THE ONE THAT MATTERS** — "the model is busy, try in a minute" and "your key is out of quota" need two different things from the reader, and both arrived as the same wall of braces; only 429 had ever been given a sentence. Faults are now classified (`_FAULTS` / `_explain`): 429 quota, **503 busy**, 504 cut off, 403 credentials, 404 model id, 500 provider. ⚠ **THE RAW TEXT IS NOT THROWN AWAY, IT IS MOVED** — the log keeps the status code and the provider's own words (`raw: …`), because whoever is debugging needs them and the person waiting on a reply does not. ⚠ **AND THE FALLBACK IS CLIPPED TO ONE LINE**: the rule is not "know every error", it is **never paste an object into a conversation**. *Paid for live:* three consecutive 503s on `gemini-3.5-flash`, 2026-09-04. | PAKKA |
| **F12** | ✨ AI Editor · a turn that asks for REAL WORK — a look, a long board, a per-shot pass | ⚠ **THE ORDER BEING RIGHT IS NOT THE SAME AS THE NUMBER BEING BIG ENOUGH.** F7 fixed *which clock stops first* and left the chat on **70s** — and 70 was too small to do the work. *Reported live, with a screenshot:* **“add music and sound effects in this storyboard story wise” on a FOURTEEN-shot board** came back as *“The read operation timed out. It ran out of time — 70s is all one call gets (CHAT_BUDGET_SECONDS).”* Nothing was stuck, the key was fine, and the turn was **billed anyway**. ⚠ **THE BUDGET HAD BEEN SIZED AGAINST THE TAB, NEVER AGAINST MEASURED WORK** — and the work is three costs stacked: a **LOOK** is **27–35s** measured (2026-09-04, `gemini-3.5-flash`), a **REPAIR** is a **second paid call inside the same attempt**, and a sound plan naming every shot is a long ANSWER on top of both. **35 × 2 = 70 EXACTLY**: the old budget left *not one second* for the mend. Now **120s**, and the tab's pair went with it — `CHAT_TURN_TIMEOUT_MS` and `API_CHAT_TURN_TIMEOUT_S` to **150**. ⚠ **A BUDGET IS CHECKED AGAINST MEASURED WORK, NOT AGAINST ITSELF, AND AGAINST BOTH SHAPES OF TURN** — they fail in opposite directions: too small kills the SLOW turn, and spending it all on one hopeless attempt starves the FAST turn's retries (F10). `tests/director_timeout_check.py` now pins both, plus the trap below. ⚠ **AND 120 IS AS FAR AS THE CEILING GOES ALONE**: `budget_seconds` returns the **smaller** of it and `DIRECTOR_BUDGET_SECONDS` (**135**), so a ceiling raised past 135 silently stops being the chat's number at all — the turn gets the Director's, **under the Director's name**, and the sentence the user reads then names a var that changes nothing for them. Past 120: raise the Director's too, or set `CHAT_BUDGET_SECONDS`, which overrides both (documented in `.env.example` now, where neither var had ever been written down). ⚠ **A LOOK IS TWO HTTP TURNS AND GETS TWO FULL BUDGETS** — the user's patience is the sum, which is what F8's counter and Stop are for. | GUZARISH |

---

## G · Working on this repo

| # | Scenario | The rule | Status |
|---|---|---|---|
| **G1** | Patching any file | **Build the new bytes, write a temp file, rename over the target.** ⚠ Never open the target for writing before the new content exists — that destroyed ~442 lines of un-committed `AGENTS.md` once. | PAKKA (convention) |
| **G2** | Finishing a change | **Do not run the Playwright browser suite unless asked.** Run the relevant `tests/*_check.py` instead. | PAKKA (convention) |
| **G3** | Reporting a prompt change | **A prompt is a request, not a guarantee.** Say GUZARISH, and say that only a live run can confirm it. Never write "fixed" for a prompt-only change. | PAKKA (convention) |
| **G4** | Reaching for a library | **`client/package.json` has exactly two runtime deps and that is a decision.** Every widget here is hand-written. | PAKKA |
| **G5** | A file the user just restored by hand | **Ask before `git checkout` / overwrite.** | PAKKA (convention) |
| **G6** | Adding state to a hook's dependency array | ⚠ **Declare the `useState` ABOVE the hook that lists it.** A dependency array is evaluated DURING RENDER and `const` is not hoisted, so a state declared further down throws `Cannot access 'x' before initialization` on the first render — **a white page.** *Found by shipping one.* | PAKKA |
| **G9** | Arming a one-shot effect from an async handler | ⚠ **Use state, not a ref.** A ref does not re-render, so arming it AFTER the value the effect keys on has already settled means the effect ran once against a null ref and never runs again — the work silently never happens. *Shipped and caught in a browser; no build and no source check could see it.* | PAKKA |
| ~~**G8**~~ | Every reply, while the user is testing | ⚠ **Carry the WHOLE open list in the sign-off, not just this turn's leftovers.** Asked for outright: *"jo jo issue and bug mil raha hai sab mujhe REMAINING mein batana."* A bug reported once and then dropped from the summary reads as fixed. The table above is the source — read it before writing the sign-off, and add to it the moment something new is found. | ~~PAKKA~~ — **superseded by G10** |
| **G10** | Every reply, while the user is testing — **replaces G8** | ⚠ **REMAINING is THIS TURN's leftovers, kept short. The whole open list lives in the "Still open" table below and is handed over WHEN ASKED, not every time.** G8 said the opposite and was right at the time — a bug dropped from the sign-off reads as fixed. It was overruled by the person reading it: *"remaining mai pura nhi do, pahle jaise dete the waise do — ye bahut jayda ho ja raha ek baar dekhne mai. tum rulebook mai likh ke rakho, jab mai mangunga to dena."* ⚠ **THE OBLIGATION DID NOT GO AWAY, IT MOVED**: anything found and not fixed still gets a row in "Still open" the moment it is found, or the sign-off becomes the only record and G8's fault comes back through the other door. | PAKKA (convention) |
| **G11** | Starting the app to work on ONE page | ⚠ **Three pages, three ports, but ONLY ONE API server (8000) - ever.** `.vscode/tasks.json` now starts each page on its own Vite port (workflow **5173**, admin **5174**, marketing **5175**) so stopping one leaves the other two up, and because the browser scopes the signed-in session PER PORT you can be an admin on 5174 and an ordinary user on 5173 at once, with 5175 permanently signed out - which is the only state the marketing page is ever seen in. ⚠ **The backend is NOT duplicated per page, and must not be**: `server/main.py`'s startup sweep closes out every job it finds QUEUED, so a second server booting would kill the jobs the first one is in the middle of running. The shared-server task is port-guarded and idempotent for exactly this reason. ⚠ **`--strictPort` on every client task is load-bearing, not tidiness** - without it a busy 5173 makes Vite silently fall to **5174**, which is the admin panel's address, and two pages then fight over one port with no error anywhere. Stop with the matching STOP task (`.vscode/stop-port.ps1`, kills by PORT, never by process name, so nothing else in node or python on the machine can be hit). | PAKKA |
| **G12** | Patching a source file from a script | ⚠ **Match an EXACT string, or walk LINES. Never a multi-line regex — and never `re.S`.** *Paid for in this repo, not read somewhere*: a pattern meant to replace two tab entries in `AdminPanel.jsx` began `[ ]*//.*NEXT TO BRAND` under `re.DOTALL`, so `.*` ran from the FIRST comment in the file down to the anchor and **took the header, every import and six of the nine tabs with it** — the file went 5,360 → 3,143 characters in one silent, successful-looking write. ⚠ **G1 DID NOT CATCH THIS AND CANNOT.** Temp-file-then-rename protects against a half-written file; it happily renames a *complete* file with the wrong contents into place. G1 is about the WRITE, this is about the MATCH. ⚠ **ASSERT THE COUNT, NOT THE PRESENCE**: `s.count(old) == 1` before replacing, and after the edit assert that everything which must have SURVIVED is still there — every tab id, every import, every render branch. A patch that only checks what it added cannot see what it removed. ⚠ **AND THE RECOVERY IS `git show HEAD:<path>`, NOT `git checkout`** (G5): rebuild from the committed text, re-apply the edit, then `git diff` and read it — the diff must contain the intended change and nothing else. That is what proved no uncommitted work had been lost. | PAKKA |
| **G13** | Writing a `tests/*_check.py` that drives the real API | ⚠ **Pin EVERY store path AND `API_OUTPUT_DIR` into a temp directory, and set them BEFORE the first `server` import**, because `server/config.py` reads the environment at import time. ⚠ **Getting a variable's NAME wrong is silent** — `config.py` falls back to its default and the test writes into the developer's own workspace. Paid for twice in one afternoon: `.local_usage.json` defaults to the **git-tracked** repo root, so the first run of `interchange_check.py` spent two of the developer's monthly projects and left two fake accounts in a tracked file; and `OUTPUT_DIR` is really `API_OUTPUT_DIR`, so eight real project folders were written into `output/` before an assertion happened to look for one of them on disk. ⚠ **The failure is not a crash — it is a green suite that quietly edits the repo**, so after a new API test check `git status` AND `output/`, not just the tally. ⚠ **An assertion that reads the disk is what catches this**; one that only reads the HTTP response never will. ⚠ **AND THE RULE WAS WRITTEN, THEN NOT APPLIED TO THE THIRTEEN SUITES THAT ALREADY EXISTED.** Measured, not estimated: the production user collection held **331 accounts of which 329 were test junk** — `_infill×76`, `_vo×49`, `_imggen×48`, `_hide×42`, `_vidgen×34`, `_chunk×33`, `_resume×27` — one prefix per unpinned suite, alongside 611 jobs. Every run of any of them registered in the real cluster and spent real quota, and it surfaced only when `hidden_lane_check.py` failed on its own third project with *“You've used 2 of your 2 projects this month”*: billing refusing a test suite, which reads like a broken test. ⚠ **A CONVENTION NOBODY CAN FORGET BEATS ONE EVERYBODY MUST REMEMBER** — `tests/_sandbox.py` is one `pin(…)` call, sweeps its own directory at exit, and lifts the quota by OVERRIDING THE TIER rather than by making the user an admin (an admin skips the feature gates these suites exist to exercise, and several register a second, deliberately ordinary account). ⚠ **AND THE LIST ITSELF IS THE FAULT, NOT THE MISSING ENTRY.** A fourteenth suite turned up when the whole folder was run and `git status` read afterwards — which is what the line above tells you to do: `features_check.py` named five store paths and stopped, so `API_LOCAL_USAGE_PATH` defaulted to the **git-tracked** `.local_usage.json` and a green suite wrote a row into the developer's own counters. Everyone who wrote one of these knew the rule and listed the paths they believed their suite touched. Do not add an entry to your list; call `pin()`. ⚠ **AND A HARD-CODED `"output"` IS THE SAME BUG WEARING A HAT**: `voiceover_fit_check.py` read the path the server wrote by building it from a literal, so it passed only while the suite was polluting the repo. Ask `config.OUTPUT_DIR`. | PAKKA (convention) |
| **G14** | A test that re-runs a guard against a deliberately broken copy | ⚠ **Each row must carry the SOURCE it guards, not share one.** `interchange_check.py` §12 hard-coded `apply_src` as the good source for every guard in the loop, so the first guard written about a different file read `False` on correct code and reported ITSELF broken — a red line pointing at the wrong thing, which is worse than no line. The loop now takes `(name, good, broken, guard)`. ⚠ **And the guard is only worth writing if the fault it catches would ACT NORMAL** — e.g. `onClick={readFile}` where `readFile(experimental)` is expected: React passes the click event, an event object is truthy, and every ordinary read silently asks the server to guess. | PAKKA (convention) |
| **G15** | Deriving state from a prop in ANY component the user drags, scrubs or trims in (`Timeline.jsx` above all) | ⚠ **USE AN EFFECT, NOT REACT'S “ADJUST STATE DURING RENDER”. THE PATTERN IS REAL, DOCUMENTED, AND IT BREAKS POINTER DRAGS ON THIS BAR.** Per-project row heights (E83) needed “another project → its own rows”, and the first version did it the documented way: a bare `if (state.id !== prop) setState(…)` in the render body, so the first paint after a switch is already right. It cost `tests/editor_lane_restack_check.py` two checks — *“the overlay row is above the captions now”* and the blend-mode pixel sample under it — because a render-phase `setState` makes React **throw the in-progress render away and start again**, and every drag on this bar is measured against boxes `readView` reads back out of the DOM on a layout effect keyed on that same state. The drop landed on the wrong row. ⚠ **AND IT WAS FOUND ONLY BY RUNNING THE BROWSER SUITE** — the unit tests, the build and `editor_row_height_check.py` were all green through it. ⚠ **BISECT, DO NOT ARGUE**: commenting out the three lines turned the suite green, restoring them turned it red, restoring it as a `useEffect` turned it green — three runs, and no theory needed. ⚠ The effect is declared **after** the write it hands over from, so a pending save still goes under the id it belongs to. | PAKKA |
| **G16** | Writing or fixing a Playwright selector | ⚠ **SELECT BY WHAT THE THING IS, NEVER BY A SIZE OR STYLE CLASS.** `editor_director_check.py` clicked the free door as `.dir-actions button.small` and started failing on a change that was deliberate and correct — the `small` came off when the two brief-screen buttons were levelled, because two buttons side by side are one size (**E102**). The panel had not regressed; the suite had. It is `.dir-actions button:not(.primary)` now, with the label asserted beside it so it cannot silently start clicking something else. A suite pinned to decoration fails on cosmetics and goes quiet on behaviour — which is backwards. | PAKKA |
| **G17** | A test that takes a screenshot | ⚠ **IT GOES IN `test_shots/`, VIA `tests/_shots.py` — NEVER A PATH THE SUITE PICKS FOR ITSELF.** Every browser suite used to name its own file in the repo root — `bin_probe_failed.png`, `restack_probe_bands.png`, `row_routing_failed.png` — so one afternoon's failing run left a dozen PNGs sitting next to `server/` and `client/`, `git status` read **53 pending changes**, and the debugging trail started getting committed as source. `row_routing_failed.png` actually made it into a commit that way. ⚠ **`.gitignore` KNEW ONLY ONE SPELLING** — `*_probe_failed.png` — so every shot named anything else (a reference frame from a *passing* run, a `_bands`/`_blend` variant) walked straight past it. A pattern that has to be extended for each new filename is the bug; a folder that is dropped whole is the fix. ⚠ **AND `output/` WAS NOT THE ANSWER EITHER**, though it is ignored: it is where the app writes real renders, so test shots buried in it are indistinguishable from a user's work and get swept away with it. ⚠ Use `shot("name.png")`, or `shot("name.png", "suite")` for a set that belongs together; the helper makes the folder, so **delete your `os.makedirs`** and never write a screenshot path by hand. Reported outright: *"jab bhi tum testing karo ss ek folder mai rakho aur usko git ignore kar ke rakho."* | PAKKA |
| **G7** | Any change to a screen | ⚠ **`npm run build` passing is not evidence that the screen renders**, and neither is a `tests/*_check.py` — those read the file as TEXT. esbuild never evaluates the module. Run `tests/workflow_mount_check.py` (starts Vite itself, no backend, ~1 min) after touching a workflow component, and **never report a UI change as working on a green build alone.** | PAKKA |
| **G18** | ANY browser suite, and any test that talks to a real API | ⚠ **A TEST THAT SPENDS A QUOTA MUST BRING ITS OWN ACCOUNT, AND ITS OWN COPY OF EVERY STORE IT WRITES TO.** `tests/e2e_animatic.py` looked isolated — memory job store, temp users file, its own ports — and was not: the quota counter kept writing into the repo's own `.local_usage.json`, one `projects` slot per run, under a FIXED address (`pw@example.com`). ⚠ **THE THIRD RUN OF THE MONTH THEN FAILS, AND NOT WHERE THE FAULT IS**: `POST /animatics` answers **402 Payment Required**, Save-as silently does nothing, the top bar says *"⚠ Not saved"*, and the run dies thirty seconds later at whatever `wait_for_selector` came next — reading exactly like a broken editor. The row also lands in `git status` as a change to a TRACKED data file, and was committed once. ⚠ **TWO FIXES, BOTH NEEDED.** The address is minted per run (`pw-<uuid>@example.com`), which also makes "how many projects does this account have?" honest by construction; and the documented command now points `API_LOCAL_USAGE_PATH`, `API_LOCAL_EVENTS_PATH` and `API_LOCAL_DRAFTS_PATH` somewhere temporary alongside the jobs and users. **Before you add a suite, list every store the app writes to and give it a temp path** — `server/config.py` names them all in one block. Same family as `profile_check.py`'s 324 leftover accounts in the Still-open list. | PAKKA |

---

## Still open — known, reported, NOT fixed

⚠ Carried over from the user's own testing list. Marked ✅ where re-verified in
the repo, and left unmarked where it is the user's report taken at face value.

| Area | What is wrong | Verified |
|---|---|---|
| Concept | A typo like "virl" for "viral" silently switches off A5, and nothing on screen says the hook rule did not fire. A decision on how to surface it is pending. | ✅ keyword-only trigger |
| Props step | `StoryboardAssets.jsx` has no `N shots` badge — the cast step (E5) has one; props does not. | ✅ |
| Concept card | ⚠ **E1, SITE FIVE.** The key-scene rows in `ScriptToStoryboard.jsx` are `<input>` — one line, no growth — and these are *the lines that become the panels*. A 150-character scene scrolls off the right edge and cannot be read without dragging inside the box. Seen on run 3, scene 5. | ✅ |
| Concept | No rule says what a GOOD call-to-action looks like; the prompt only says to use the CTA the creator asked for. | ✅ |
| Board layout | The fourth card sits alone in a row. | — |
| Board layout | Small cards stretch. ⚠ **Half-fixed by accident**: pinning the draw button to the tile foot moved the empty space above the button instead of below it. The stretch remains. | — |
| Board | Draft name changes on its own; thumbnail reverts; cast description does not save. | — |
| Board | "Ganesha idol" name ties between a cast entry and a prop. | — |
| Board | Shot 5 has no speaker; the PDF slug line is wrong. | — |
| Script writer | ⚠ **A PROP CAN BE IN TWO PLACES AT ONCE.** The first full run put the Ganesh idol in the *living room* (scenes 2, 5) and the *puja room* (scenes 1, 3, 4) — carried into one and standing in the other. `location` drives the background of every image, so the same idol would have been drawn in two rooms. **Nothing in the prompt says a thing stays where it was put.** | ✅ |
| Breakdown | All 15 shots came back `movement: static`. Not a rule breach — the rule says most shots should be — but the same rule asks for a slow push-in on *"a realisation or a reaction landing"*, and *"Ananya's eyes light up"* is exactly that. Under-applied, not absent. | ✅ |
| Cast step | ⚠ **A REFERENCE CAME BACK WITH TWO PEOPLE IN IT** — MOTHER rendered as a mirrored pair. A reference is injected into every panel that character appears in, so one bad sheet corrupts **8 panels**, and the user is the only thing standing between it and the spend. `qa.py` audits the finished board for magenta placeholders, greyscale and aspect — **it cannot see a doubled figure**, and nothing checks a reference BEFORE it is used. | ✅ |
| Concept | **A3 and A4 regress on a freshly generated card** — see the live-run log in section A. Diagnosed, not fixed: A2 ("every beat gets a scene") beats A4 because the arc's own first beat is *Ghar ki taiyari*, and A3 has no room because six beats fill six scenes. The fix is to reach the STORY DIRECTION, not to add a seventh paragraph. | ✅ |
| Board | ⚠ **THE FILM'S SUBJECT DRIFTED ACROSS 15 PANELS.** The Ganesh idol is drawn differently in shots 3, 5, 10 and 13 — every CHARACTER is consistent because each got a reference, and the idol got none. ~~The breakdown returned an empty asset list.~~ ⚠ **THAT DIAGNOSIS WAS WRONG, and only the props field (E8) showed it**: the breakdown HAD listed them — shot 1 carries *"Puja Room, marigold garlands, oil lamps"*, shot 3 *"Ganesh idol, Living Room"*. So the props step was reachable all along and no reference was ever drawn there. **Why is not established**: nothing on the review step showed that assets existed or that they were worth a reference, so the step could be walked past without knowing what was being skipped. The props step still has no `N shots` badge and no equivalent of the cast page's warning. | ✅ |
| Review step | ⚠ **CHANGING `location` DOES NOT REWRITE THE IMAGE PROMPT.** Both reach the panel, and the prompt text wins: shot 11's location was set to Puja Room while its description still read "in the living room", and the panel came back a living room. The field looks like the fix and is only half of it. | ✅ |
| Cast step | A vague reference description lets the outfit drift even WITH a reference — ANANYA's says "a colorful traditional lehenga", and she wears three different outfits across the 15 panels. The colours have to be named. | ✅ |
| Cast / props steps | ⚠ **THE SAVED-REFERENCE MAP IS KEYED TWO DIFFERENT WAYS.** A draft's map is written by `saveRefFields`, so its keys are already `refKey`'d (trimmed, lower-cased); a BOARD's map is whatever `StoryboardCast` sent up, which is the character's own name — `ANANYA`. Both steps look themselves up lower-cased, so a board's map matched **nothing**: four empty cards and a "(skip refs)" button over references already paid for and sitting on the server. Normalised inside `restoreSavedRefs` now, which is the one place both paths pass through — but the two producers still disagree. | ✅ |
| Board | ⚠ **A PROP REFERENCE HOLDS IN A SCENE AND SLIPS IN A CLOSE-UP.** First board drawn WITH a `Ganesh idol` reference: shots 3, 6, 10, 11, 12, 13 and 14 all show the same small clay idol — the reference plainly worked. **Shot 5, the hero close-up where the idol fills the frame, came back a different, grander idol** (taller jewelled crown, different skin tone). Two things stack: a frame filled by the object gives the model the most room to reinterpret, and that shot's own description says *"majestically … serene and grand"* — adjectives that ASK for a grander object than the reference. ⚠ So the shot that most needs the reference is the one it holds worst, and nothing warns. | ✅ |
| Board | Reported the other way round the same day and worth keeping: **a prop/background reference OVERRULES the prompt text.** Shot 11's description still read *"in the living room"* while its props named `Puja Room`, and the panel came back a puja room. Useful — but it means the two can disagree with no sign on screen of which one won. | ✅ |
| Editor · monitor | ⚠ **AFTER A 🎬 RUN THE LOGO OVERLAY DRAWS ABOUT 2.5× TOO BIG IN THE PROGRAM MONITOR AND IS CROPPED AT THE LEFT** (`ID_logo_RGB_XL.png`, on an overlay lane spanning the whole film). Reported with two screenshots at the same 0:04. **NOT explained by the run's own edits**, and that is checked rather than assumed: the Director has no overlay verb at all (`ACTIONS` in `actions.js`), `sceneAt` resolves an overlay from its OWN keyframes, `overlayRect` sizes it from its own box in frame units, and the biggest transform any plan step can write is **±17%** (`ZOOM_TO`/`PAN_SCALE` × the 1.4 amount cap) — nowhere near 250%. The shot picture in the same screenshot measures ≈1.15×, which IS the Ken Burns move working correctly. Needs a reproduction: does **Revert it all** put the logo back, and is it big everywhere on the timeline or only across a transition? | — |
| Editor · AI chat | ✅ **ANSWERED, 2026-09-04, LIVE — and it was never the plain turn.** `gemini-3.5-flash`, Developer API, same board, seconds apart: **text 2.6s / 5.2s / 4.2s**, **a LOOK 34.6s**, **12 stills 27.1s**, and **three consecutive 503s** in between. So a text turn is ~5s and was never the complaint; **a look is ~10× that**, and a look meeting one transient fault is 35 + backoff + 35 — which is exactly where the 90s reports landed, and the look feature shipped the same day they started. ⚠ **STILL NOT PROVEN**: that this is what the ORIGINAL reporter hit — nobody has reproduced their session, and 30s is not 90s without a retry in the middle. Two faults it did prove are fixed (F10, F11). ⚠ **AND THE PROMPT IS CONFIRMED INNOCENT, RE-MEASURED NOT INHERITED**: system 9.9KB, vocabulary 5.9KB, schema 5.3KB, ~5.9k tokens — and the schema is **13 levels deep / 79 type nodes**, which is the one untested suspect left if a text turn is ever slow again. | — |
| Tests | `tests/plan_script_check.py` fails with **403 on POST /plans** for a new account. Confirmed NOT ours by running it on a stashed clean tree. Nobody has looked at it. | — |
| Tests | `tests/profile_check.py` fails its last line — *"only the real account remains (got 324)"*. It counts the Mongo users collection and expects 1; **324 throwaway accounts from earlier test runs were never cleaned up.** Environment residue, not code — but it makes the suite read red, which is how a real failure gets ignored. | ✅ |

---

**Last updated:** 2026-08-30 — **E38**: a fixed `max-width` is only half a responsive page. Explore was pinned at 1560px, so a 27" monitor got a narrow column with 700px of dead ground either side and two picture cards blown up to 1000px inside it. Give the cap a RANGE with the old number as its FLOOR (nothing under a laptop width moves), keep a ceiling, and cap the WALL separately from the page — spelling its left edge out, because every row around it is centred.

**Previously:** 2026-08-30 — **E34**: a rail must carry a row for the page it is standing on, marked active — the public rail never named Explore, so nothing was highlighted and Home was a one-way door. ⚠ And **adding a MODE to a shared component rots every "string X is not in this file" assertion**: three went stale at once. Re-assert the rule as *behind the guard*, not *absent from the file*, and prove the new check fails without the guard.

**Previously:** 2026-08-30 — **E33**: a rail beside a many-row page needs ONE wrapper and a two-cell grid, never per-row placement plus `grid-row: 1 / -1` — which **does nothing without explicit rows** and collapsed the page into a single 100dvh row, opening on a blank screen. ⚠ The thing that moved was the header's HEIGHT, not its top, so `nav.top < 160` would have passed on the broken page. ⚠ And **a source check is not a layout check**: `npm run build` and thirty green source assertions coexisted with the bug, two of them asserting the declarations that caused it. New `tests/explore_layout_check.py` measures the geometry and reproduces the blank screen to prove it can still fail.

**Previously:** 2026-08-30 — **E32**: a screen that needs a rail the app already has gets a MODE on the existing component, never a look-alike. Paid for in one round trip — the public page's hand-built rail came back the same day missing the mark, the name and the collapse toggle, all three of which were free from the real one. ⚠ And watch what the mode HIDES: dropping the avatar dropped the `margin-left: auto` that was holding the brand row's right edge.

**Previously:** 2026-08-30 — **E31** and **G12**. **E31**: two panel tabs that edit ONE screen become one tab with sections — Banners and Showcase are now sections of an **Explore** tab, asked for outright, and the old strip's own comment had already admitted they were the same page. **G12** is paid for in this session: a multi-line regex under `re.S`, aimed at two lines of `AdminPanel.jsx`, ate the header, the imports and six of nine tabs. **Match an exact string or walk lines; assert the count before, and assert the survivors after.** G1 cannot catch this — it protects the write, not the match. Also this session: the public Explore page gained a left rail, every row a sign-in gate carrying its workflow id.

**Previously:** 2026-08-30 — section E gained **E30**: two controls side by side, one changing the THING and one only its COVER, must say which is which ON the control. *"maine admin panel se image daala hai — magar video khul raha hai, image nahi"* — nothing was broken (proved end to end: each item serves its own file, byte for byte); "Replace still" had set the cover and the item was still a video. Second report in a row from the same row of buttons, after **E28**.

**Previously:** 2026-08-29 — section E gained **E28** and **E29**. **E28**: a wall that accepts video **makes its own thumbnail** — reported as *"no thumbnail show in my upload video"*, and the cause was a wrong fact written into a comment (`ffprobe` inspects, **ffmpeg extracts**, and the repo already shipped ffmpeg), so a capability that existed was designed around. Black frames are still refused. **E29**: a page whose job is to be shown to people **gets an address** — `?explore`, copied from `?admin`, dropped on sign-in. Both pinned in `tests/showcase_check.py` (§11 with real ffmpeg-built clips, §12 on the route).

**Previously:** 2026-08-29 — section G gained **G11**: the VS Code tasks were split into THREE independent start/stop pairs, one per page (workflow 5173, admin 5174, marketing 5175), over ONE shared API server — asked for directly: *"divide my task.json in three part for start and stop … which makes it easy for me to handle."* ⚠ The reason the backend is not split with them is in the rule: a second server on boot closes out the first one's in-flight jobs. New `.vscode/stop-port.ps1` (kills by port, pure ASCII like `start-app.ps1`, and for the same cp1252 reason). Verified end-to-end: Vite bound to 5174, answered HTTP 200, and the STOP task freed the port.

**Previously:** 2026-08-29 — section E gained **E10** and **E11** from the
Explore page: a second dashboard was built, and the six workflow groups were
moved out of `Home.jsx` into `dashboard_feed.js` rather than copied. Pinned by
the new Chromium `tests/explore_mount_check.py` (114 checks, both themes, the rail, the admin
screens and the whole signed-in shell included). **E12** followed the same day: the app now OPENS on
Explore, from one named constant rather than from five separate handlers. Then
**E13** (a slide may not resize the page it sits on) and **G10**, which strikes
**G8**: the sign-off carries this turn's leftovers, and the full open list is
handed over on request instead of every time. Then the offer pop-up brought
**E14** (how an interruption has to behave), **E15** (its words live in the admin
panel) and **E16** (gold means the action, never navigation). Then the collapsed
rail grew names under its icons: **E17** (short names for every workflow, hidden
ones included) and **E18** (measure an ellipsis with a `Range`, not with
`scrollWidth`). Then the billboards themselves moved into the panel: **E20**
(customer-facing copy belongs to the admin, with today's behaviour as the
fallback), **E21** (how to add an uploaded picture safely) and **E22** (don't
complain about a field nobody has touched). **E23** closed the masonry hole
the wall had been reported for twice.

**Previously:** 2026-08-28 — section A gained A1–A4, then A10–A11,
across three live runs of one Hinglish Ganesh Chaturthi brief. ⚠ **No run
has yet had all six right**: run 2 held A1–A4 and dropped the visarjan; run
3 holds A1/A2/A10/A11 and regressed on A3 and A4. Then two things the
testing itself exposed, both now **PAKKA**: **E6** (a list whose order is
its meaning needs ↑/↓, not just add and delete) and **F4** (the concept
card survives a refresh, because re-generating it returns a different
film — which had been costing a whole re-test every time anything shipped).

⚠ Still unlooked-at at the user's request: the close-up-heavy framing,
deferred until the review step can be read.

⚠ **`tests/workflow_mount_check.py` has now caught FOUR faults** that a green
build and a full green Python suite both shipped: the white page, an offer link
on a screen the only route to which destroys what it offers, a props field that
ate the space inside "Ganesh idol", and a signature stamp armed on a ref that
never fired. Run it after touching a workflow component.

⚠ **G6 and G7 were both bought with a white page.** The concept-draft
change built clean, passed every Python check, and rendered nothing.
`tests/workflow_mount_check.py` exists now and caught a SECOND fault the
same afternoon — an offer link on a screen the only route to which
destroys the thing being offered.
